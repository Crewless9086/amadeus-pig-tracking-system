# Codex repository routing

Lifecycle: `POINTER_ONLY / NON_DOCTRINE`.

Before changing or dispatching work, verify the current worktree, branch, HEAD,
upstream, current `origin/main`, and local changes. Then read
`docs/09-vault-brain/10-source-map/ACTIVE_DOCS_SOURCE_MAP.md` and load the
mandatory governance and owner pack selected there. The source map, focused
Vault files, and its two registered cross-system exceptions define authority;
this file only makes that route discoverable to Codex.

Use `docs/06-operations/CONTROL_TOWER_MISSION_REGISTER.md` for current-state
navigation and follow its latest handover link. Treat handovers, evidence,
planning files, compatibility pointers, static agent cards, and archives as
evidence rather than reusable doctrine.

Preserve registered worktrees, dirty files, untracked evidence, mission
identities, approvals, holds, delivery history, and active observers before
changing classifications. A documentation branch or documentation commit does
not authorize application merge, deployment, provider publication, database
change, farm write, permission change, payment, or physical operation.

During a recorded Control Tower ownership transition, remain read-only until
the current handover's verification gate passes and the owner explicitly
transfers coordinating ownership. Only one session may dispatch work or operate
the release lane.
