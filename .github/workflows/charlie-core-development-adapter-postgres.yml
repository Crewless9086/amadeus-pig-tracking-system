name: CHARLIE CORE Development Adapter PostgreSQL

on:
  pull_request:
  push:
    branches: [main]

jobs:
  development-adapter-postgres:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: charlie_core_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d charlie_core_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/charlie_core_test
      PYTHONDONTWRITEBYTECODE: "1"
      CHARLIE_TEST_ISOLATION: "1"
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - run: python -m pip install -r requirements.txt
      - name: Apply authoritative CORE queue migrations
        run: |
          python scripts/apply_supabase_migration.py supabase/migrations/202606300001_create_charlie_mission_queue.sql
          python scripts/apply_supabase_migration.py supabase/migrations/202607270003_create_charlie_owner_execution_holds.sql
          python scripts/apply_supabase_migration.py supabase/migrations/202608020001_create_charlie_many_to_one_replacements.sql
          python scripts/apply_supabase_migration.py supabase/migrations/202608040001_create_charlie_development_mission_adapter.sql
      - name: Run exact development mission adapter gates
        run: python -m unittest tests.test_charlie_development_mission_adapter_postgres
