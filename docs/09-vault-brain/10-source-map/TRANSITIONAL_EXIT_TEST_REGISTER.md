# Transitional Exit-Test Register

Status: `BATCH_28_RECONCILED / TWO NAMED EXITS BLOCKED BY CURRENT DEPENDENCIES`
Date: 2026-08-18
Baseline: `3819db4a27fd2680d57242742b7b7f8490d4008a`

This register prevents transitional implementation documents from becoming
either accidental doctrine or permanent unowned clutter. The generated
physical-cutover manifest binds every retained document to exactly one test.

## GS-LEGACY-RETIREMENT-V1

Scope: all 32 Markdown contracts under `docs/03-google-sheets/`.

Current result: `BLOCKED_CURRENT_RUNTIME_DEPENDENCY`.

Fresh repository evidence still shows `gspread` setup/import tooling, hybrid
irrigation fallback behavior, document-generation fallback descriptions and
legacy sales/order comparison consumers. These documents remain technical
implementation references only. They never override Supabase, backend rules or
focused Vault doctrine.

This exit passes only when one reviewed evidence packet proves all of:

1. A fresh route-and-worker inventory finds zero production Google Sheets
   reads, writes, fallbacks, formulas or administrator setup dependencies.
2. Each affected domain has exact Supabase/backend read and write ownership,
   shadow equivalence, failure-path coverage and rollback proof.
3. The owner-approved stability window completes without fallback use.
4. Source-map consumers, tests and operational runbooks no longer require the
   32 paths, after which the entire set is archived together.

## N8N-LEGACY-RETIREMENT-V1

Scope: all 40 Markdown contracts under `docs/04-n8n/`.

Current result: `BLOCKED_CURRENT_RUNTIME_DEPENDENCY`.

Fresh repository evidence still shows an order-approval webhook, committed
workflow exports, contract-check and live-state scripts, customer-channel
integration, document-delivery attribution and legacy implementation-source
consumers. These documents remain technical implementation references only.
They never own farm truth, agent doctrine, business rules or new workflows.

This exit passes only when one reviewed evidence packet proves all of:

1. Authenticated provider inventory classifies every deployed workflow as
   replaced, retired or intentionally retained outside the retirement set.
2. Every retained business effect has one canonical backend action, provider
   receipt, replay contract, failure recovery and rollback path.
3. No application route, environment binding, script, test or operational
   workflow requires the 40 documentation paths.
4. A provider-silent stability window and terminal-independent replacement
   cycles pass, after which the entire set is archived together.

## Enforcement

- Partial retirement of either family is prohibited unless a new manifest
  splits the family with exact consumer and provider evidence.
- An old README, workflow export, Sheet schema or open editor never proves
  current activity, authority or runtime state.
- New business logic may not be added to either transitional family.
- Batch 29 may accept this bounded exception set, but cannot falsely mark either
  exit passed without the evidence above.
