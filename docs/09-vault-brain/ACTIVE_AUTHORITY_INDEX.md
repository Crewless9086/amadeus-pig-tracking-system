# Active Authority Index

Status: compatibility entry point only; not an independent authority list.

The canonical active authority selector is
`10-source-map/ACTIVE_DOCS_SOURCE_MAP.md`. Load that complete tracked file and
apply its common pack plus every mission-relevant deterministic agent pack.

This path exists for owner and terminal instructions that name
`docs/09-vault-brain/ACTIVE_AUTHORITY_INDEX.md`. It must never be expanded into
a second authority map, workflow, mission register or doctrine layer. If the
canonical selector is missing, incomplete or conflicting, fail closed.
