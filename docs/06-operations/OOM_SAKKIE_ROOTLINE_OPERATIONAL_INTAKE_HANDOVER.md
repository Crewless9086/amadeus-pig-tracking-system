# Oom Sakkie ROOTLINE operational intake handover

Status: source-ready; production recovery of Telegram 3213 remains required.

## Defect and correction

Authenticated owner water observations and visible irrigation need previously
fell through to the obsolete `irrigation_status` sheet reader. The existing
operational-specialist boundary now claims these messages first, preserves each
independent fraction with its provider message and timestamp, and dispatches a
typed command-inert packet to ROOTLINE. A new physical observation forces a
fresh governed reassessment; it is never silently combined with or overridden
by the older canonical recommendation.

The source change creates no router, webhook, queue, Telegram path, farm write,
or device transport. Its durable dispatch event preserves both independent
observations and suppresses repeat specialist dispatch after completion. The
intake result cannot send Telegram, write canonical farm observations, retry ON,
or issue a hardware command. A missing or malformed deployed ROOTLINE
consumer produces one visible typed containment instead of the legacy sheet
disclaimer.

## Bound production evidence

- Telegram inbound: `3213`
- Relay execution: `62152`
- Provider timestamp: `2026-08-03T16:22:07+00:00`
- Exact text SHA-256: `a939caa9e42d100990a0f0a1491501a27fedc284310297d6ef8a0242ee04c9ee`
- Incorrect delivered response: Telegram `3214`
- Existing family lifecycle: `OOM-FAMILY-821A45EDA11EAF9D13E77300`
- C commissioning: `ROOTLINE-COMMISSION-70417672399B3525D658F6A2`
- B commissioning: `ROOTLINE-COMMISSION-D248A120ECE1961DB81B6C2E`
- Power-cycle binding: `43fda51c1ecb3f638ef193a134551f5e802b3ffc1c236f46c561689ec69603ed`

## Exact later production proof

After reviewed exact-head integration and deployment, recover 3213 under a new
single-use recovery identity without resetting its consumed original delivery
guard. Prove ROOTLINE receives reservoir `4/4`, storage `2/4`, and current visible
C need with the original timestamp. Freshly revalidate power, observed weather,
forecast, commissioning and channel state. If and only if canonical eligibility
supports it, execute one C/channel-2 segment of no more than 60 minutes through
the existing governed ROOTLINE rails, with one visible start, native auto-OFF,
bounded OFF recovery, verified shutdown, B/borehole/fertilizer OFF, one visible
completion, and no automatic second segment. Replay must yield zero observations,
plans, commands, cards, messages and farm events.

The source contract alone is not business completion. Provider-confirmed
ROOTLINE decision and the supported real segment outcome are required.
