# Oom Sakkie manager-quality composer handover

Status: source prepared for reviewed integration and bounded production proof.

## Outcome contract

One typed presentation boundary now renders ROOTLINE and consolidated farm-manager evidence as concise Telegram HTML. The enabled semantic front door supplies the English/Afrikaans language hint; deterministic typed facts remain authoritative. The composer exposes current decision, short reason, owner action only when genuine, and next reassessment. It omits raw packets, internal identifiers and implementation prose.

The existing authenticated gateway owns delivery and replay. The composer grants no farm write, customer/public action, diagnosis, mating or hardware authority.

## Lifecycle corrections

- HERDMASTER active mortality evidence suppresses obsolete welfare questions. An owner-reported-dead Pig 127 case remains a governed mortality follow-up until recorded; it is never rendered as awaiting breathing evidence.
- Natural grouped weight lines resolve against canonical active/on-farm identity, run the existing bulk-weight preflight and produce one grouped confirmation preview. No weight is recorded by preview.
- ROOTLINE read-only reassessment uses an owner/chat-scoped material decision digest. The authenticated scheduled/evidence-change entrypoint is `POST /api/oom-sakkie/management/rootline/reassess`; it reuses the existing gateway token, allowed-owner policy, family lifecycle and Telegram transport. Unchanged delivered evidence emits no notification. A changed decision stays pending until provider-confirmed delivery, while ambiguous delivery is contained without blind retry. It never creates a command or second irrigation segment.

## Production proof

After exact deployment:

1. Process one genuine current ROOTLINE owner request and require one provider-confirmed compact answer.
2. Process one genuine current farm-manager request and verify a clean herd/farrowing/weighing/irrigation brief with no stale Pig 127 breathing question.
3. Invoke the authenticated reassessment entrypoint from the existing scheduler at ROOTLINE's declared trigger. Prove unchanged delivered evidence sends zero messages.
4. On a genuine material canonical decision change, require exactly one provider-confirmed visible notification and replay zero.
5. Snapshot farm rows and command evidence before/after; require zero unauthorized writes and zero hardware commands.

The scheduler must supply a unique trigger identity and timestamp. A scheduler claim is not owner delivery: the reassessment event becomes delivered only after the family-message rail returns a provider message identity. No new bot, direct specialist send path, schedule family or hardware authority is introduced.

Completed ROOTLINE execution `ROOTLINE-IRRIGATION-2CBB37586FE70DD527D9F54C` remains closed regression evidence and must not be replayed.
