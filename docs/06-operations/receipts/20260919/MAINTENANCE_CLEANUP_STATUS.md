# Approved maintenance cleanup — 2026-09-19

Current mission evidence; NON_DOCTRINE. Mission
`REPOSITORY-CONSOLIDATION-20260918`, coordinator
`CONTROL-TOWER-DESKTOP-SUCCESSOR-20260918`, task
`01a0b9d5-5c55-7e30-a5fc-aea27c93ffd6`.

**Five of the six remaining folders have been retained intact. The sole
remaining root is the original OneDrive checkout. Migration is not complete.**
The daily source and saved project remain `C:\Amadeus\repo`.

## Authority and continuity

Charl's **"Approve clean up."** replied directly to the previous receipt's
six-root maintenance/access decision. It authorized bounded application/Docker
downtime and access resolution, with n8n state and recovery preserved. The
earlier permission hold is superseded for that scope. No repeated cleanup or
takeover approval is needed. The same task and mission own continuation.

Startup verified canonical Git common directory, clean HEAD
`99d6e2dbafd3363107f2cf433feb5b517842d51d`, tree
`d782ec29012db11507acf3d883ef41becd172237`, branch
`codex/workspace-consolidation-20260919`, no upstream, one worktree, 12 ahead/
zero behind freshly fetched unchanged main
`e46743cb3e8d224b60d17eb5920acb113f613a52`. Workspace guard passed.
The previously loaded mandatory governance and CORE owner pack remain active;
no doctrine changed. Earlier source/coordination workers remain frozen.

## Completed maintenance

All five destinations are beneath
`C:\Amadeus\recovery\20260919\retained-originals\tmp`:

| Original name under `C:\tmp` | Disposition |
| --- | --- |
| `omq-farm-brief-20260916` | Existing obsolete-Amadeus classification; intact same-volume retention |
| `pytest-green-api` | Ownership remains unverified; intact retention |
| `pytest-green-correction` | Ownership remains unverified; intact retention |
| `pytest-green-final` | Ownership remains unverified; intact retention |
| `pytest-green-identity` | Ownership remains unverified; intact retention |

The unchanged reviewed retention tool enforced original classification and
completed-retirement proofs, exact scope, no reparse roots/ancestors, NTFS,
same-volume identity, no-overwrite rename and destination/source readback.
All five retain their exact volume/file IDs, Directory attributes and tag 0.
Independent native readback under the non-admin owner token confirmed all five
old names absent and all five destination IDs exact. No unconfirmed move.

Docker's three recorded containers stopped using `docker stop --timeout -1`;
all exited with code 0 and no OOM flag. Docker Desktop then closed normally,
releasing the OMQ handle. After retention, Desktop restarted from the new
checkout and the same three containers resumed. Images, mounts and restart
policies matched pre-maintenance evidence. `charl-n8n-1` returned HTTP 200 at
`http://localhost:5678/healthz`. No container/volume removal, workflow change,
data-directory move, new image pull or service retirement occurred. This proves
local process restoration and health, not business-workflow acceptance.

Windows UAC inspection found that all five formerly unreadable locations
already granted Administrators full control. A second consented administrator
run moved the four pytest roots. Their owner/group/DACL remained byte-for-byte
equivalent in SDDL readback. No ACL, ownership or token-privilege change was
needed. Independent exact-source review of the bounded wrapper passed; its
PowerShell parse had zero errors. Existing retention/Cloud guards were unchanged.

The verified old-project Cursor window received a graceful CloseMainWindow
request and exited. No forced process termination or handle closure was used.
Docker and this editor closure are the only process maintenance performed.

## Remaining original checkout

Exact source:
`C:\Users\charl\OneDrive\1. Amadeus\AGENTS\amadeus-pig-tracking-system`.
Native volume `17629920732283616668`, file ID
`000000000000000000aa0000000240a4`, CloudE tag. After Cursor closed, the native
query reported 13 users: current Codex app `ChatGPT.exe` PID 7708 and its
node/node_repl descendants through current server PID 34284. Process creation
identities were recorded; stale PIDs must never be reused for termination.
Closing this app would interrupt the coordinator itself. No remote handle
closing, process injection, forced rename or silent background retry was used.

The administrator read-only audit passed the previous cache access obstacle but
stopped at a nested junction in `.review_core_evidence_reconciliation`.
Its `venv` junction targets the old checkout's `venv`. The unchanged Cloud
qualifier rejects name-surrogate reparse entries, so this was recorded as HELD;
no move followed a failed audit. A separate original-only opaque-link auditor
was implemented in mission runtime. Parent full-source review, independent
compilation and seven synthetic checks passed: external junction target and
self-loop not traversed, raw link/security metadata captured, ordinary default
and alternate streams hashed, and root-junction/unknown-tag/offline-Cloud cases
rejected. This read-only helper cannot move anything. Targets must never be traversed or altered;
absolute link targets may require restoration to the original paths.

Windows displayed its native UAC prompt for the final read-only audit, then
returned **"The operation was canceled by the user."** No administrator process
launch receipt or audit result was created. The prompt was not retried, and no
alternative privilege or permission route was attempted. This was a Windows
consent cancellation, not an automatic approval-review rejection. Synthetic
tests do not qualify the live original; the previous HELD result remains.

Charl subsequently requested **"Approved do it so I can get the window
prompt"**. The exact pinned audit was relaunched after clean canonical
checkpoint verification. Windows consent succeeded at 17:28:05 UTC,
administrator audit PID 38636. This is a new explicitly requested attempt;
the canceled attempt remains preserved. The audit **passed at 17:32:05 UTC**:
172,892 objects, 154,225 ordinary/Cloud files, 14,457,397,151 hashed stream bytes
and one opaque junction. That link is
`.review_core_evidence_reconciliation\venv`; its raw reparse bytes and own
security were recorded without following its target. Exact receipt
`original-opaque-admin-qualification.json` is 133,155,933 bytes, SHA-256
`9858766a56dcf9ed8a6a5f8d60e46f5b3efd14afdef3d19c8ad88c74f0ba58a4`.

The administrator audit process exited. Fresh native readback afterward
confirmed the same original volume/file ID and 13 users belonging to this
Codex app and its descendants. DELETE still returns STATUS_SHARING_VIOLATION
`0xc0000043`. No move or additional process stop occurred. The audit PASS is
content/link qualification at that observation, not a completed migration;
fresh pre-move proof and exact post-move readback remain required.

Before moving this last root, fully quit and reopen Codex so the old inherited
handles are released. Resume this same task against `C:\Amadeus\repo`, refresh
identities/handles and content
proof, then use a reviewed no-overwrite intact-retention operation. Preserve
the original if any check fails. This is an execution gate under existing
approval, not another request for cleanup permission.

## Evidence and preserved boundaries

Working evidence: `C:\Amadeus\.runtime\consolidation\maintenance-20260919`.
Key records: `omq-retention-apply.jsonl`, `pytest-before.json`,
`pytest-retention-apply.jsonl`, `pytest-after.json`,
`five-tmp-independent-readback.json`, `administrator-retain-pytest-review.md`,
`docker-before.json`, `docker-stopped.json`, `docker-after.json`,
`n8n-health.json`, `cursor-graceful-close.json`,
`original-handles-after-cursor.json`, `original-admin-qualification.json`.
The prepared original-only check and canceled launch are recorded in
`OriginalOpaqueQualify.cs`, `audit-original-opaque-parent.ps1`,
`original-opaque-parent-tests.json`, `original-opaque-parent-review.md`,
`original-opaque-uac-canceled.json` and `original-retention-intent.json`.
The successful follow-up is in `original-opaque-admin-launch.json`,
`original-opaque-admin-qualification.json`, its dated relaunch intent and
`original-post-audit-native.json`. Reviewers are frozen; the audit has exited
and no background retry remains active.

This phase's evidence copies and manifest are preserved separately in recovery
`maintenance-evidence` and `maintenance-evidence-manifest.json`. The exact final
source commit, fresh clean guard, complete-ref source bundle and one-root
inventory belong to `maintenance-workspace-verification.json` and
`maintenance-consolidation.bundle`. Earlier receipts/bundles remain immutable.
The successful original audit is preserved separately in recovery
`original-audit-evidence`. Its follow-up source/evidence verification is
`original-audit-workspace-verification.json`; the incremental
`original-audit-update.bundle` requires the preserved `maintenance-consolidation.bundle`
at base `ea9975b3ce48c099159d19ebb65d3a24866138ae`.

Total intact retained roots: **556** (543 earlier, eight qualified old AGENTS
entries, five maintenance tmp roots). This phase deleted zero source objects.
Prior removal counts remain 250,321 files and 12,741 empty directories,
15,537,781,090 logical bytes; physical savings are unmeasured.
Original all-ref bundles, both encrypted snapshots with their DPAPI keys and
manifests, actual restore proofs, prior receipts and failed findings remain
preserved. Whole-root `preservation_complete:false` remains historical truth;
successful intact moves do not convert incomplete snapshots into complete ones.
The private `.env` remains ignored/untracked in the daily checkout.

Only local documentation changed in Git. No push, merge, deployment, provider
publication, direct database/farm/customer/hardware action or canonical lifecycle
event was issued. Restored services resumed their existing configurations;
their business/provider effects were not inspected. Existing application holds and mission identities remain in the
current register. CORE and specialist operational writers remain owner-stopped;
Docker restoration does not dispatch them. Historical production remains
`3d321e4932cf205d03e759cc71a26edd411bc9d2`; fresh runtime/canonical/provider/
physical acceptance and unattended cycle remain Unknown. No BUSINESS_COMPLETE
or deployed outcome is claimed. No automation or recurring owner obligation
was created. Existing offline test evidence retains its earlier attribution.

Control Tower Check Receipt: one local WIP slot; governance unchanged; fresh
local filesystem/service evidence; independent review and five-path native
readback PASS; source/release workers frozen; release lane unused; all other
operational lanes remain at the recorded holds/Unknown. Dirty history, mission
identities, approvals and observers preserved. Owner workload reduced to one
daily checkout; manual-step reduction unmeasured. One original-root hold remains.

Decision: WAIT at current-app handle release.
Why: the original-only audit passed, but the current Codex app itself still
holds the original source open and prevents its intact move.
Send to exact terminal: CONTINUE—SEND NOTHING; retain this task's lineage.
Expected business result: one recoverable local entrypoint; no deployed business
outcome asserted.
ACTION REQUIRED NOW: Fully quit and reopen Codex, then resume this same task
in `C:\Amadeus\repo`. The administrator check passed; cleanup is already approved.
