# CMQ-20260813-03 OOM typed preview wiring handover

Status: source-prepared

Owner: CORE, with OOM SAKKIE authority and HERDMASTER farm-data review

The existing authenticated typed OOM SAKKIE grouped-weight boundary now runs
the pure canonical grouped preview after, and only after, its established
natural-language interpretation and authoritative readiness preflight accept
the complete request. It reuses the pig and pen evidence already loaded for
that boundary and performs no additional database, provider or Sheets read.

The owner-visible preview uses the same canonical rows, opaque identities,
effective date, confirmation requirement and digest as an equivalent
`application_typed` request. Optional facts remain `Unknown`. The separately
stored protected claim receives the exact established legacy preview payload
shape and values and retains its existing protected-claim digest boundary.
Canonical preview generation does not execute, confirm or complete the claim.

Malformed or conflicting dates and ambiguous, duplicated, inactive or
off-farm animal identities fail before claim creation. Existing OOM natural
interpretation, grouped-weight numeric semantics, response composition and
protected confirmation/execution ownership remain in place.

Only `modules/oom_sakkie/grouped_weight_runtime.py` is wired. Protected-action
claims and execution, writers, Supabase persistence, Sheets fallback,
application adapter, UI, browser or Telegram voice, routes, migrations,
schemas, configuration and production remain unchanged. This is undeployed
source evidence and grants no production or execution authority.
