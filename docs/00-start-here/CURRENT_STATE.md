# Current State

## Purpose

This document is the live operational truth of the project. It summarizes what is documented, what is working, what is risky, and what must be built next.

## System Status

| Area | Status | Notes |
| --- | --- | --- |
| Documentation structure | Stable | `docs/` is now the canonical source of truth. |
| Google Sheets docs | Good baseline | Sheet files, formulas, ownership, field standards, and business rules are documented. |
| n8n workflow docs | Good baseline | Four workflow exports and suite-level rules are documented. |
| Backend order docs | Good baseline | Current API behavior, known gaps, and refactor direction are documented. |
| Live order system | Stabilizing | Reject, customer cancel, first-turn create-with-lines, payment method capture, send-for-approval, lifecycle guards, auto-reservation, outbound notifications, quote/invoice generation, and document delivery are implemented and have been live-verified through the documented phases. |
| Web app | Operational slices improving | Orders list/detail usability is complete through Phase 6.2. Breeding board Phase 8D is live-verified on a real repeat-service case. |
| Pork sales launch rail | Private test build | Sam records meat preorder facts into append-only sales leads. Ledger owner review can now record price/kg, available week, size/range, deposit rule, collection/payment, and final owner approval as an append-only event before any customer send, quote, order, stock, or financial action. |
| Media workflow | Disabled | `1.3` is official but must remain disabled until fixed and tested. |

## Completed Documentation Work

The following documentation areas are now usable as planning inputs:

- `docs/03-google-sheets/`: Google Sheets schema, ownership, formulas, fields, and per-sheet docs.
- `docs/04-n8n/`: n8n suite map, workflow rules, data flow, node responsibilities, protected logic, and four workflow exports.
- `docs/02-backend/`: backend API structure, order logic, data models, module structure, and refactor plan.

## Current Build Priority

The next build work should continue one documented phase slice at a time.

Reason:

- orders remain live and profit-critical
- pig and breeding operations are now becoming daily-use web app workflows
- Oom Sakkie and farm telemetry workflows need careful integration without breaking live sales or Telegram routing
- Google Sheets limits and future Supabase migration need to be handled deliberately, not through rushed rewrites

## Known Critical Order Gaps

### Reject Releases Reserved Lines

Status: implemented and live-verified.

Confirmed behavior:

- `reject_order()` blocks completed orders from being rejected
- linked order lines become `Line_Status = Cancelled`
- linked line reservations become `Reserved_Status = Not_Reserved`
- `ORDER_MASTER.Reserved_Pig_Count` becomes `0`
- `SALES_AVAILABILITY` recovers and makes pigs available again through the formula chain
- `ORDER_STATUS_LOG` records the rejection

### Customer Cancellation Is Implemented And Live-Verified

Current backend behavior:

- `cancel_order()` provides a dedicated customer cancellation action
- `POST /api/orders/<order_id>/cancel` is available
- `Order_Status = Cancelled`
- `Approval_Status = Not_Required`
- `Payment_Status = Cancelled`
- linked non-cancelled/non-collected lines are cancelled
- linked line reservations are set to `Not_Reserved`
- `Reserved_Pig_Count` is reset to `0`
- `ORDER_STATUS_LOG` records customer cancellation when state changes

Current n8n wiring:

- `1.2 - Order Steward` has a `cancel_order` branch calling `POST /api/orders/{order_id}/cancel`
- `1.0 - Sam Sales Agent` uses `pending_action = cancel_order` for two-turn confirmation
- `CANCEL_ORDER` is evaluated before create/update routes
- stale cancel confirmation is cleared through `CLEAR_PENDING`
- Chatwoot order attributes survive escalation and human reply through the full snapshot rule
- cancellation after escalation was live-verified against `ORD-2026-367706`

First-turn draft creation with lines:

- `1.0` now routes complete first-turn requests with `requested_items[]` to `1.2` using `action = create_order_with_lines`
- `1.2` owns the full create + sync operation: create `ORDER_MASTER`, sync `ORDER_LINES`, then return one combined result
- top-level `success` is true only when both create and sync succeed
- live verification passed on 2026-04-29 with `ORD-2026-879091`; three `ORDER_LINES` rows were created with `match_status = exact_match`

Payment method capture:

- `Payment_Method` is stored on `ORDER_MASTER` as the source of truth
- Chatwoot `custom_attributes.payment_method` mirrors the order value for conversation continuity
- `Cash` and `EFT` capture are live-verified
- next-turn readback from Chatwoot is live-verified
- cancel-pending and escalation paths preserve `payment_method`
- backend lock guard is live-verified: `payment_method` cannot be changed once the order is beyond `Draft`
- no-draft handling is live-verified: Sam does not write payment method without an active draft

Send for approval from Sam:

- `1.0` detects customer send-for-approval intent and routes to `SEND_FOR_APPROVAL`
- `1.2` calls backend `send_for_approval` with `neverError` and `continueOnFail` enabled so backend guards return as data
- backend validates draft status, payment method, customer name, collection location, and at least one non-cancelled order line
- happy path live verification passed on 2026-04-30 with `ORD-2026-377DA3`; order moved to `Pending_Approval`, Chatwoot status updated, and Sam said the order was sent for approval, not approved
- already-pending regression check passed
- missing payment method regression failed, then was fixed and live re-tested on 2026-05-04: `Code - Decide Order Route` now sets approval preflight `reply_instruction` when `send_for_approval_intent = true` but `sendForApprovalReady = false`; Sam asks for Cash/EFT, backend is not called, and Draft status is preserved
- backend `400` customer-safe reply regression passed live re-test on 2026-05-04: backend rejected missing `Collection_Location`, `backend_success = false` path returned a safe missing-field reply, and Sam did not say the order was sent

Approve/reject lifecycle direction:

- Phase 1.5 lifecycle guards — **Complete And Live-Verified** (see `NEXT_STEPS.md` §1.5): approval only from `Pending_Approval`, payment lock beyond `Draft`, reject/cancel blocked for `Completed`; outbound notifications stay in Phase 1.9
- Phase 1.8 approval auto-reservation is complete and live-verified
- approval should auto-reserve active order lines, because approval means the farm accepts and commits to the order
- repo implementation now approves first, then attempts auto-reservation; approval does not roll back if reservation fails or partially fails, and backend returns `reserve_warning` for the admin web app
- live mixed-line verification passed on 2026-05-09 with `ORD-2026-102250`: approval succeeded, one active line reserved, one cancelled line skipped, `Reserved_Pig_Count = 1`, and `ORDER_STATUS_LOG` recorded the warning follow-up
- clean all-eligible verification passed on 2026-05-09 with `ORD-2026-7C79A8`: two active lines reserved, `Reserved_Pig_Count = 2`, and no warning returned
- all-ineligible verification passed on 2026-05-09 with `ORD-2026-0FB697`: order approved, cancelled lines remained not reserved, `Reserved_Pig_Count = 0`, and `ORDER_STATUS_LOG` recorded the no-reservation warning
- live `ORDER_MASTER`, `ORDER_LINES`, and `ORDER_STATUS_LOG` headers matched `docs/03-google-sheets/sheets/` during Phase 1.8 verification; `ORDER_MASTER.Payment_Method` is live and required, and `ORDER_MASTER.ConversationId` is live as the Phase 1.9 Chatwoot lookup key
- approval/rejection customer notifications use a separate outbound n8n workflow triggered by backend webhook, not Sam's inbound `1.0` workflow
- Phase 1.9 outbound approval/rejection notifications are complete and live-verified
- new draft orders store `conversation_id`, approvals/rejections call `ORDER_NOTIFICATION_WEBHOOK_URL`, and delivery failures are logged as warnings without rolling back the order transition
- live Phase 1.9 verification passed on 2026-05-09: direct `1.4` webhook test returned `sent = true`; `ORD-2026-36CDE4` approval sent the customer notification and reserved one line; `ORD-2026-C3CEDF` rejection sent the customer notification and cancelled/released one line
- production backend must keep `ORDER_NOTIFICATION_WEBHOOK_URL=https://charln.app.n8n.cloud/webhook/order-notification` configured

### Split Requested Item Sync Needs Hardening

Known risk:

- split items such as `primary_1` and `primary_2` have not always synced correctly
- female/secondary split rows have been missing or not updated in some tests

Required behavior:

- all split item keys must be preserved
- repeated syncs must not create duplicates
- stale lines must be released/cancelled before replacement
- partial matches must not silently appear complete
- line totals must match requested quantity before Sam confirms success

### Sam Needs Safer Order Context

Approved direction:

- Sam should get order context through `1.2 - Amadeus Order Steward` and backend review actions
- direct production access to `ORDER_OVERVIEW` should not be the first choice

Reason:

- backend can verify customer/order ownership
- backend can return only safe, relevant fields
- backend responses are easier to test than uncontrolled AI sheet reads

### Sales Agent Reply Payload — closed (Phase 1.7, 2026-05-07)

**Resolved:** `Code - Slim Sales Agent User Context` feeds Sam **`OrderStateSummary`** + **`StewardCompact`**; see `docs/04-n8n/DATA_FLOW.md` §`1.0` Sales Agent Input Contract. Raw/debug payloads stay upstream of the slim node.

## Web App Current Concern

The app must become useful for daily operations. It should help with:

- viewing orders clearly
- viewing matings in a clear breeding board for pregnancy checks, farrowing preparation, movement planning, and repeat-service decisions
- understanding reservation status
- approving/rejecting/cancelling orders safely, with order detail actions visible only when backend rules allow them
- releasing pigs correctly
- showing logs/history
- producing practical farm printouts, starting with a pre-weighing weekly weight sheet that has blank capture columns and can be printed from a phone or laptop
- reducing manual debugging work

Do not focus on app polish before the related backend behavior is correct.

## Next Decision Point

Pick the next item from `docs/00-start-here/NEXT_STEPS.md`.

Current position:

- Phase 7.3D is complete and live-verified.
- Phase 7.3E weather was triaged; weather is working, while Sunsynk is deferred to a dedicated backend/data/Supabase review.
- Phase 8D is live-verified: Baby's real mating was safely marked `Not_Pregnant` / `Repeat_Service` on 2026-05-20 and remained in Kraam Saal 05.
- Phase 8D follow-up is deployed and live-verified: Baby's new mating `MAT-2026-9EFC4E` now shows formula-derived expected dates correctly in the live API.
- Phase 9.1A is live-verified: real litters `LIT-2026-9E4A` and `LIT-2026-EB92` generated the expected piglet rows with `Purpose = Unknown`.
- Phase 9.1B is deployed and browser-verified: dashboard litter attention reads `LITTER_OVERVIEW`, and litter tiles open the litter detail page.
- Phase 9.1C is deployed and browser-verified: litter detail now shows attention metadata and provides a backend-owned `Mark as Weaned` action that updates `LITTERS` and linked active/on-farm `PIG_MASTER` piglets with the chosen wean date/count. Purpose/classification review remains a later slice. A 2026-05-30 audit confirmed `LIT-2026-OTY0` and `LIT-2026-0LBF` were record-reconciliation attention items and `LIT-2026-8A0F` needs piglet tag numbers. Owner reconciled `LIT-2026-OTY0` and `LIT-2026-0LBF`; dashboard logic now only keeps `Weaned - review purpose` when active/on-farm piglets still have blank or `Unknown` purpose, so older litters with accepted `Sale` / `Grow_Out` / `Breeding` purposes no longer wait on the future auto-classification function. `LIT-2026-8A0F` remains the legitimate visible attention item until its piglets are tagged.
- Lifecycle automation direction is captured in `NEXT_STEPS.md`: future piglet death, post-weaning death, weaning, sales, slaughter, and meat-stream actions should update the correct source records/logs and feed sow/boar/litter/bloodline outcome reporting. Pre-weaning deaths must keep the pig row because they are part of born-alive survival history.
- Phase 9.2A is deployed and owner-verified: pig dropdown APIs and labels now include current pen context and three-slot numeric tag display.
- Phase 9.2B is deployed and owner-verified: `/pigs` now displays numeric-only tags as three digits, sorts the list with numeric-aware tag order, and keeps profile links on unchanged `pig_id` values.
- Phase 9.3 is deployed and owner-verified: the weight form shows the selected pig's current pen beside `Moved To Pen (Optional)` without changing the save payload.
- Phase 9.3B is deployed and owner-verified: `/pig-weights` weight input blocks mouse-wheel value changes, hides number spinners, and shows a top save action after the required fields while preserving the existing save payload.
- Phase 9.4 current slice is deployed and owner-verified: read-only weight report endpoint and `/weight-report` page include visual usability refinements, duplicate prevention is live, and numeric pig tags display correctly as three digits. Full edit/delete/void audit, interactive report rows, sortable report headers, and feed-guidance planning remain parked for later phases.
- Phase 9.5 is deployed/browser-visible: the dashboard monthly sales count now shows total sales exits plus livestock, slaughter/abattoir, and future meat streams from `PIG_MASTER` exits. Follow-up 9.5B is planned for clearer sales stream counts and Rand values once each stream's value source is defined.
- Phase 9.5B planning is updated: keep current pig-exit counts honest, do not fake Rand totals from pig counts, define slaughter/abattoir sale logging first, and align future value cards with a transaction model/Supabase rather than a complex Google Sheets-only patch.
- Phase 9.5B1 is deployed and owner-verified: dashboard wording now says `Sales Exits This Month`, `Livestock Exits`, `Slaughter Exits`, and `Meat Exits`; backend counts are unchanged.
- Phase 9.5B2 planning is captured: slaughter/abattoir sales need a transaction/value source before dashboard Rand totals; preferred future direction is Supabase-backed `sales_transactions` with linked pigs/items rather than a Google Sheets-only patch.
- Phase 9.6A is deployed and browser-verified: `/print-sheets` provides a read-only weekly weight capture sheet with English labels, all-active default, multi-pen filtering, browser print support, and no Google Sheets writes.
- Phase 9.6C is deployed and received live-data corrections locally on 2026-06-08: `/bulk-weights` follows the printable sheet row order, allows local browser draft-saving, skips rows with no weight, validates duplicates/mistakes before commit, and uploads accepted rows as one deliberate batch. Owner reported a 68-row weighing session looked like only 20 rows in the daily report. Code audit found no 20-row cap in bulk preflight/save; the likely mismatch was `/weight-report` hiding historical weight rows for pigs that are no longer currently `Active` / `On_Farm = Yes`. Local fix keeps historical rows visible and flags no-longer-active/on-farm rows instead of dropping them. Owner then found `Upload Batch` stopped when some draft rows already had weights for the date; local fix now partial-saves valid new rows, skips duplicate/existing same-pig/same-date rows, and leaves true failed rows visible for correction. Browser/live retry is still needed.
- Phase 9.7 lifecycle outcome tracking is now the planned next farm-efficiency slice after live checks: start with a current-state audit/action matrix for death/removal, sale/live-stock exit, slaughter/abattoir exit, weaning outcome, and later meat-stream movement. The goal is backend-owned, auditable outcome actions that feed pig, litter, sow, boar, dashboard, and sales reporting without disconnected manual edits.
- Phase 9.7A audit source is created in `docs/02-backend/PIG_LIFECYCLE_OUTCOME_PLAN.md`: born-alive piglet creation, litter weaning, and order-based live-stock sale completion already have backend-owned actions; slaughter transactions do not yet update `PIG_MASTER`; no complete death/removal action was found. Recommended next build is 9.7B controlled death/removal on pig detail.
- Phase 9.7B controlled death/removal action is deployed-visible and dry-run verified against a real active pig, but still awaiting a real write case: `/pig/<pig_id>` has a lifecycle outcome form for active/on-farm pigs, and `POST /api/pig-weights/pig/<pig_id>/lifecycle/death` updates `PIG_MASTER` status/on-farm/exit fields and notes while preserving the pig row and litter/parent links. The endpoint now supports `dry_run = true`; dry-run against `PIG-2026-42B7` returned the planned removal update with `rows_updated = 0`, and readback confirmed the pig stayed `Active` / `On_Farm = Yes`. Real write closure should wait for an actual farm case or an explicitly approved test pig.
- Phase 9.7C slaughter pig-exit confirmation is local and ready for deploy/browser check: `/sales/slaughter/<sale_id>` has an explicit `Pig Exit Confirmation` form for open slaughter sales, and `POST /api/sales-transactions/<sale_id>/confirm-pig-exits` reads the Supabase sale/items then updates linked `PIG_MASTER` rows to `Slaughtered` / off-farm only when all linked pigs are still active/on-farm. Closed paid/completed/cancelled sales are blocked by 9.7E instead of keeping the normal action available.
- Phase 9.7D read-only lifecycle outcome dashboard feedback is deployed and getting layout refinement: the home dashboard herd panel shows `Outcomes This Month` for sold, slaughtered, dead, and removed counts from `PIG_MASTER` exit/status fields. After owner screenshot feedback on 2026-06-02, the outcome counts were compacted into a strip and dashboard secondary cards were changed to stop stretching to the tallest card. No new write path was added. Local JS check, focused tests, route smoke, and full unittest suite passed at 320 tests on 2026-06-02.
- Phase 9.7E closed-action cleanup and lifecycle detail readback is deployed/browser-checked for the visible action and expanded locally with real-data inspection/reconciliation: slaughter sale detail hides `Pig Exit Confirmation` for `Completed`, `Cancelled`, or `Paid` sales, the backend blocks direct confirm calls for those closed states, `/pig/<pig_id>` shows read-only lifecycle history from `PIG_MASTER`, and `/litter/<litter_id>` shows read-only lifecycle outcome counts. Local `.env` loading was added so local Flask/API inspection can use `DATABASE_URL` without affecting Render env precedence. Real sale `SALE-2026-1DE373` was inspected through the local API, linked pig `PIG-2026-C390` was confirmed already `Slaughtered` / off-farm, and closed-sale reconciliation filled `Exit_Order_ID`, normalized `Exit_Reason`, and made the displayed `Exit_Date` parse correctly. Local JS check and full unittest suite passed at 329 tests on 2026-06-02.
- Phase 9.7F litter newborn health action is live-tested complete: `POST /api/pig-weights/litter/<litter_id>/newborn-health` uses `PRODUCT_REGISTER` products and creates `MEDICAL_LOG` treatment records. Live test on 2026-06-03 with `LIT-2026-9E4A` correctly updated 10 active/on-farm piglets, skipped the dead/off-farm piglet, wrote 10 Ecomectin/Antiparasitic rows and 10 Panacur/Deworming rows, and moved the litter attention on to tag-number attention. Process correction from owner/farm: first-days newborn health is Panacur + Ecomectin only; earmarks/eartags/tag numbers belong around weaning. Next planned slices are 9.7G smart wean/tag attention timing with 35-day default weaning age, 9.7H fast pre-weaning death capture/stillborn handling with dead pig rows kept for history, and 9.7I smart return navigation. Future litter print/capture sheet alignment is parked until owner provides a sample.
- Phase 9.7G smart wean/tag attention timing is deployed and owner-verified: backend computes estimated wean date as birth/farrowing date + 35 days, starts tag/wean attention 3 days before estimated wean date, suppresses early tag-number dashboard reminders, and exposes timing fields on litter detail. Newborn health attention no longer requires earmark fields, and the newborn-health UI hides the earmark checkbox so first-days treatment is Panacur/Ecomectin only. Real-data check on 2026-06-04 for `LIT-2026-9E4A` returned estimated wean date `2026-06-22`, attention start `2026-06-19`, and no dashboard attention today. Full local unittest suite passed at 336 tests; owner deployed and confirmed it is working on 2026-06-04.
- Phase 9.7H is browser-accepted: Add Litter now uses `born_alive` for active/on-farm piglet rows and creates `stillborn_count` rows as `Dead` / `On_Farm = No` with birth-date exit date and `Exit_Reason = Stillborn`, preserving dead pig rows for history. Litter detail has a `Piglet Death Capture` preview/apply action backed by `POST /api/pig-weights/litter/<litter_id>/piglet-deaths`; it updates active/on-farm piglets to `Dead` / off-farm with event date, reason, notes, and audit fields while preserving litter/parent links. Count-based selection is allowed only while piglets are untagged/unsexed; sexed piglets require male/female counts, and tagged piglets are blocked until specific-pig selection or the individual pig lifecycle form is used. Full local unittest suite passed at 340 tests on 2026-06-04; owner browser feedback was good enough to continue.
- Phase 9.7I smart return navigation is deployed/working: litter piglet table links carry `return_to=/litter/<litter_id>` and `return_label=Back to Litter`; pig detail uses safe internal return context when opened from a litter; `/litter/<litter_id>` header returns to Dashboard by default; pig profile child pages return to the pig profile when opened from pig detail; sales dashboard rows return from sale detail to Sales Dashboard; slaughter sale ledger rows return from sale detail to Slaughter Sales; breeding analytics drill-ins return to Analytics; breeding detail litter links return to Breeding Detail; and breeding board pig/litter links return to Breeding Board. Owner deployed and confirmed the second sweep is working on 2026-06-04.
- Phase 9.7J litter sex-count capture is browser-checked for first live use: `/litter/<litter_id>` now has a `Sex Count Capture` panel when active piglets still have blank `Sex`, and `POST /api/pig-weights/litter/<litter_id>/sex-counts` previews then saves male/female counts to blank-sex active/on-farm `PIG_MASTER` rows only. Existing sex values are not overwritten; over-counts block with no write. Owner reported `LIT-2026-6EC0` and `LIT-2026-1025` both received male/female counts plus first medical records.
- Phase 9.7H2 planning now includes the next litter workflow request: a future litter-level bulk capture/edit table for tag numbers, weaned weights, and pen fields, with preview-before-save validation and no silent overwrites.
- Phase 8E breeding board sorting/action loop is deployed and owner-verified: `/matings` now sorts action sections by operational urgency/date, closed records by newest actual farrowing/fallback date, and keeps closed/linked-litter records out of action sections. Eligible mating cards include `Add Litter`, linking to `/master/add-litter?mating_id=...`; Add Litter preselects that mating and fills the known mating details through existing form logic. Owner tested it on 2026-06-01 and confirmed it is working for now.
- Phase 8F first read-only breeding analytics slice is deployed and owner-verified: `GET /api/pig-weights/breeding-analytics` and `/breeding-analytics` summarize sow/boar mating and litter KPIs from existing `MATING_OVERVIEW` and `LITTER_OVERVIEW` with no writes. Owner confirmed it is working on 2026-06-01.
- Phase 8F drill-in/data-quality slice is browser-accepted for now: `GET /api/pig-weights/breeding-analytics/<pig_id>` and `/breeding-analytics/<pig_id>` gather the selected sow/boar's mating rows, litter rows, KPI summary, and informational data-quality flags. No recommendations or writes were added. Owner will note refinements during real use before automated mating suggestions are built.
- Phase 10A operating-system map is drafted in `docs/01-architecture/FARM_OPERATING_SYSTEM_MAP.md`, and Phase 10.1 Supabase foundation plan is drafted in `docs/02-backend/SUPABASE_FOUNDATION_PLAN.md`.
- Phase 10.1 owner comments have been captured as guided defaults: use the existing Supabase Pro project as the foundation/staging workspace first, use plain SQL migrations in `supabase/migrations/`, keep Supabase behind the Flask backend, add `/health/database`, migrate orders/sales first, review telemetry after the first database path is proven, and keep Google Sheets visible until the database-backed path is trusted.
- Phase 10.1 first implementation slice is local: `supabase/migrations/` exists as the migration home, backend route `GET /health/database` exists, and the route is designed to be safe when `DATABASE_URL` is missing.
- Local verification passed on 2026-05-21: focused database tests passed, full local unittest suite passed at 132 tests, and `/health/database` returns safe `503` / `not_configured` before `DATABASE_URL` is added.
- Deployed verification passed on 2026-05-21: Render `DATABASE_URL` connects successfully to Supabase and `/health/database` returns `success = true`, `status = ok`, `configured = true`, `database = postgres`, and harmless UTC database time.
- Phase 10.1B baseline is local: `supabase/migrations/202605210001_foundation_migration_log.sql` creates only internal `app_private.migration_log`, and `GET /health/database/foundation` verifies it.
- Phase 10.1B local verification passed on 2026-05-21: focused database tests passed at 6 tests, full local unittest suite passed at 135 tests, and migration contract test confirms no business tables are created.
- Phase 10.1B deployed verification passed on 2026-05-21: owner ran the baseline SQL in Supabase SQL Editor and `/health/database/foundation` returned `success = true`, `status = ok`, migration ID `202605210001_foundation_migration_log`, and applied timestamp `2026-05-21T01:19:31.638474+00:00`.
- Phase 10.2 order/sales schema planning source is created in `docs/02-backend/SUPABASE_ORDER_SCHEMA_PLAN.md`.
- Owner accepted the 10.2 recommended defaults on 2026-05-21.
- Phase 10.2A empty-table migration is prepared in `supabase/migrations/202605210002_create_order_sales_tables.sql`, with backend verifier `GET /health/database/order-schema`.
- Local verification passed on 2026-05-21: focused database tests passed at 9 tests and full local unittest suite passed at 138 tests.
- Phase 10.2A deployed verification passed on 2026-05-21: owner ran the SQL migration and `/health/database/order-schema` confirmed all seven expected order/sales tables with `missing_tables = []`.
- Phase 10.2B import dry-run script is prepared in `scripts/order_sales_import_dry_run.py`; it reads Google Sheets only, writes nothing to Supabase, and reports `writes_to_supabase = false`.
- Phase 10.2B live summary-only dry-run passed on 2026-05-21 with `writes_to_supabase = false`: 26 included orders, 103 included order lines, 27 included intakes, 7 included intake items, 6 included documents, 62 included status logs, and 21 included pricing rows.
- Dry-run follow-up: `ORDER_STATUS_LOG` has 157 rows with missing parent order links and 111 rows linked to excluded test orders; investigate before import mapping.
- Owner decision: unlinked test/status-log data can stay in Sheets but should be excluded from Supabase import if it is not linked to an included main order.
- Status-log diagnostic is prepared in `scripts/order_status_log_diagnostic.py`; it reads `ORDER_MASTER` and `ORDER_STATUS_LOG` only and writes nothing.
- Phase 10.2B status-log diagnostic passed on 2026-05-21 with `writes_to_supabase = false` and `writes_to_sheets = false`: 62 included candidates, 157 missing-parent logs, 111 test-parent logs, and 0 missing-order-id logs.
- Import mapping rule: include only the 62 included-candidate status logs by default; exclude missing-parent/test-parent logs unless owner manually approves exceptions later.
- Phase 10.2C payload mapping is added to the dry-run script; it maps included rows to Supabase-shaped payload samples while still writing nothing to Supabase or Sheets.
- Owner rule applied: unlinked intake rows are excluded from the first import boundary.
- Phase 10.2C live payload sample report passed on 2026-05-21 with `writes_to_supabase = false` and `writes_to_sheets = false`: 26 orders, 103 order lines, 0 order intakes, 0 order intake items, 6 order documents, 62 order status logs, and 21 sales pricing rows mapped.
- Review finding before real import: some mapped orders are cancelled historical customer orders; owner should review whether all 26 included orders are worth importing before any actual insert.
- Owner decision update: first import should include completed real orders only, plus pricing reference data. Draft/pending/approved/cancelled/rejected history stays in Sheets unless manually approved later.
- Completed-only dry-run passed on 2026-05-21 with `writes_to_supabase = false` and `writes_to_sheets = false`: 3 completed orders, 53 linked order lines, 0 intakes, 0 intake items, 0 documents, 11 linked status logs, and 21 pricing rows mapped.
- Phase 10.2D shadow import script is prepared in `scripts/order_sales_shadow_import.py`; default mode is plan-only and `--apply` is required before any Supabase write.
- Phase 10.2D local verification passed on 2026-05-21: focused shadow-import/dry-run tests passed at 12 tests and full local unittest suite passed at 152 tests.
- Phase 10.2D live plan-only run passed on 2026-05-21 with `writes_to_supabase = false` and `writes_to_sheets = false`; counts matched the approved completed-only boundary.
- Phase 10.2D apply passed on 2026-05-21 after one safe rolled-back `NotNullViolation` attempt and timestamp normalization fix.
- Supabase verification confirms batch `IMPORT-20260521-COMPLETED-ORDERS-V1` contains 3 orders, 53 order lines, 0 intakes, 0 intake items, 0 documents, 11 status logs, and 21 pricing rows.
- Phase 10.2E shadow read comparison passed on 2026-05-21: `scripts/order_sales_shadow_compare.py` compared Google Sheets source mapping to Supabase batch `IMPORT-20260521-COMPLETED-ORDERS-V1` with `mismatch_count = 0`.
- Phase 10.2F read-only shadow endpoint is implemented locally: `GET /api/shadow/orders/<order_id>/compare` compares Google Sheets order detail to Supabase shadow order detail and writes nothing.
- Phase 10.2F local verification passed on 2026-05-21: focused shadow route/service tests passed at 32 tests, full local unittest suite passed at 164 tests, and local API smoke for `ORD-2026-0B29D7` returned `mismatch_count = 0`.
- Phase 10.2F deployed verification passed on 2026-05-21 for `ORD-2026-0B29D7`: response returned `success = true`, `status = ok`, `mismatch_count = 0`, and read-only flags remained false.
- No backend read/write cutover, UI change, n8n change, or Google Sheet retirement has started.
- Phase 10.2G sales transaction extension planning is captured in `docs/02-backend/SUPABASE_ORDER_SCHEMA_PLAN.md`: proposed future tables are `sales_transactions` and `sales_transaction_items` to support honest Rand values for livestock, slaughter/abattoir, and future meat/carcass sales.
- Phase 10.2H is deployed and verified: `supabase/migrations/202605210003_create_sales_transaction_tables.sql` creates empty `sales_transactions` and `sales_transaction_items` tables, and `GET /health/database/sales-transaction-schema` verifies them.
- Local verification passed on 2026-05-21: focused database tests passed at 12 tests and full local unittest suite passed at 169 tests.
- Deployed verification passed on 2026-05-21: owner ran the SQL migration and `/health/database/sales-transaction-schema` returned `success = true`, `status = ok`, migration ID `202605210003_create_sales_transaction_tables`, both expected tables found, and `missing_tables = []`.
- No backend/dashboard/order behavior changed.
- Phase 10.2I is deployed and verified: `GET /api/sales-transactions` reads Supabase sales transaction headers only, supports optional stream filtering, and reports `writes_to_sheets = false` / `writes_to_supabase = false`.
- Local verification passed on 2026-05-21: focused sales transaction/database tests passed at 17 tests, local missing-config route smoke returned safe `503`, and full local unittest suite passed at 174 tests.
- Deployed verification passed on 2026-05-21: `GET /api/sales-transactions` returned `success = true`, `status = ok`, `count = 0`, empty `sales_transactions`, and read-only source flags.
- No records, write form, dashboard Rand totals, or order automation were added.
- Phase 10.2J is implemented locally: `POST /api/sales-transactions/dry-run` validates a sales transaction payload and calculates gross, deductions, net total, item count, and pig count without connecting to Supabase.
- Local verification passed on 2026-05-21: focused sales transaction tests passed at 8 tests, local dry-run route smoke passed, and full local unittest suite passed at 177 tests.
- Deployed verification passed on 2026-05-21: dry-run slaughter payload returned `success = true`, `mode = dry_run`, `gross_total = 1200`, `deductions_total = 100`, `net_total = 1100`, and both write flags remained false.
- No real create endpoint, sale IDs, dashboard Rand totals, order automation, or pig status changes were added.
- Phase 10.2K controlled sales transaction create-flow plan is captured in `docs/02-backend/SUPABASE_ORDER_SCHEMA_PLAN.md`: first real write should be `POST /api/sales-transactions` for `Slaughter` only, with atomic Supabase inserts, duplicate pig protection, no Google Sheets writes, no dashboard Rand totals, and no pig/order status changes.
- Real slaughter workflow is captured for 10.2K: pigs are taken to `Bartelsfontein` abattoir for buyer/butcher `JC Slaghuis`; carcass weight is optional because it is not always supplied; payment is normally received about two weeks later by bank transfer/EFT; VAT handling must be treated deliberately.
- Planned slaughter status rule: use `sale_status = Confirmed` and `payment_status = Unpaid` while waiting for butcher payment, then update to `sale_status = Completed` and `payment_status = Paid` once the EFT is received.
- Pig `S10` was reported on 2026-05-21 as recently slaughtered and was marked slaughtered in Google Sheets; it later became the first real JC Slaghuis slaughter/payment close-out verification.
- Phase 10.2K1 is implemented locally: `POST /api/sales-transactions` supports `Slaughter` only, requires `created_by`, writes Supabase header/items atomically, blocks duplicate pig IDs, and writes nothing to Google Sheets.
- Local verification passed on 2026-05-21: focused sales transaction tests passed at 15 tests, local missing-config route smoke returned safe `503`, and full local unittest suite passed at 184 tests.
- Deployed 10.2K1/10.2K2 verification passed on 2026-05-21: synthetic transaction `SALE-2026-F17E16` was created for `PIG-TEST-102K2-20260521`, read back successfully, and duplicate-pig protection returned `409 duplicate_pig` on a second create attempt.
- The synthetic test transaction remains in Supabase and is clearly marked as test data. It is not linked to a real pig/order.
- At the 10.2K1/10.2K2 checkpoint, no real `S10` transaction had been written; this changed later after the create/cancel/payment path was proven.
- Phase 10.2K3 cancellation/void flow is implemented locally: `POST /api/sales-transactions/<sale_id>/cancel` requires `cancelled_by` and `cancel_reason`, marks `sale_status = Cancelled`, sets `payment_status = Cancelled`, appends an audit note, and never hard-deletes rows.
- Local verification passed on 2026-05-21: focused sales transaction tests passed at 20 tests, local missing-config cancel route smoke returned safe `503`, and full local unittest suite passed at 191 tests.
- Deployed 10.2K3 verification passed on 2026-05-21: synthetic transaction `SALE-2026-F17E16` was cancelled, duplicate release was proven by creating `SALE-2026-28EF1B` with the same synthetic pig ID, and the second synthetic transaction was also cancelled.
- Final readback shows both synthetic slaughter transactions are cancelled.
- At the 10.2K3 checkpoint, no real `S10` transaction had been written; this changed later after the form/payment path was proven.
- Phase 10.2L internal slaughter sale form is implemented locally at `/sales/slaughter`.
- The form defaults to the current real workflow values, loads active pigs, creates slaughter transactions through the verified Supabase endpoint, lists recent slaughter transactions, and can cancel non-cancelled rows.
- Local verification passed on 2026-05-21: `node --check static/js/slaughterSale.js`, focused frontend/sales tests passed at 27 tests, local page smoke returned `200`, and full local unittest suite passed at 192 tests.
- Phase 10.2L2 payment/final amount update is implemented locally: `PATCH /api/sales-transactions/<sale_id>/payment` updates non-cancelled slaughter transaction amount, payment status, sale status, payment method, optional carcass weight, and appends an audit note.
- `/sales/slaughter` now has an `Update Payment` action for non-cancelled rows.
- Local verification passed on 2026-05-21: `node --check static/js/slaughterSale.js`, focused sales/frontend tests passed at 23 tests, local missing-config update route smoke returned safe `503`, and full local unittest suite passed at 200 tests.
- 10.2L2 real-value test was parked by owner decision on 2026-05-21 until the real JC Slaghuis sale value was known.
- This follow-up was completed and verified on 2026-05-23; S10/payment completion is no longer owner-pending.
- Next step remains continuing with the selected Phase 10 telemetry/irrigation slices.
- Slaughter form refinement notes are captured in `NEXT_STEPS.md`: improve save-button reachability, align the bottom table with the agreed table/filter layout, plan multi-pig slaughter batches, add payment date handling, and consider estimated carcass weight from latest live weight.
- Shared page template/layout standard is captured in `NEXT_STEPS.md` so new pages stop drifting into different patterns.
- Phase 10.2L3 slaughter form UX polish is implemented locally: `/sales/slaughter` now has a top save action, transaction search, sale-status filter, payment-status filter, clear filters action, filtered transaction count, and clearer status pills.
- Local verification passed on 2026-05-21: `node --check static/js/slaughterSale.js`, frontend contract tests passed at 10 tests, local page smoke returned `200`, and full local unittest suite passed at 200 tests.
- Multi-pig slaughter batch entry is still intentionally not implemented; it should be planned as 10.2L4 before changing transaction/item logic.
- Phase 10.2L4 planning is captured in `docs/02-backend/SUPABASE_ORDER_SCHEMA_PLAN.md`: use one transaction header per slaughter batch and multiple item rows for pigs.
- 10.2L4 should start with payment-date schema planning before multi-pig form implementation, because payment can happen later than slaughter.
- Phase 10.2L4A payment-date schema migration is implemented locally: `supabase/migrations/202605210004_add_sales_transaction_payment_date.sql` adds nullable `payment_date` to `sales_transactions`.
- Backend verifier `GET /health/database/sales-payment-date-schema` is implemented locally.
- Local verification passed on 2026-05-21: focused database tests passed at 15 tests, local missing-config verifier smoke returned safe `503`, and full local unittest suite passed at 203 tests.
- Phase 10.2L4A deployed verification passed on 2026-05-21: `/health/database/sales-payment-date-schema` returned `success = true`, `status = ok`, migration ID `202605210004_add_sales_transaction_payment_date`, applied timestamp `2026-05-21T15:45:04.636332+00:00`, and `payment_date_column_found = true`.
- Phase 10.2L4B backend multi-item create support is implemented locally: one sale header can carry multiple pig item rows, and duplicate `pig_id` values inside the submitted batch are blocked during validation before any database write.
- Local verification passed on 2026-05-21: focused sales transaction create/dry-run/route tests passed at 17 tests and full local unittest suite passed at 206 tests.
- Phase 10.2L4C form multi-pig selector is implemented locally: `/sales/slaughter` now supports add/remove pig rows, per-pig amount, optional carcass weight, optional pig note, calculated batch total, and duplicate-selection blocking.
- Local verification passed on 2026-05-21: `node --check static/js/slaughterSale.js`, focused frontend/sales tests passed at 27 tests, local page smoke returned `200`, and full local unittest suite passed at 206 tests.
- Phase 10.2L4D payment update with batch total/payment date is implemented locally: payment update requires `payment_date` when marking Paid, updates header totals/payment/date/status, keeps single-pig item update behavior, and does not auto-reallocate final batch totals across multiple pig item rows.
- Local verification passed on 2026-05-21: `node --check static/js/slaughterSale.js`, focused update/route/frontend tests passed at 25 tests, local page smoke returned `200`, and full local unittest suite passed at 208 tests.
- Phase 10.2L4E deployed synthetic batch test passed on 2026-05-21: two-pig synthetic batch `SALE-2026-17736A` was created, active duplicate create was blocked with `409`, payment update to `Paid` with `payment_date = 2026-05-21` succeeded, the batch was cancelled, reuse batch `SALE-2026-0C9DE0` proved duplicate-pig release, and the reuse batch was cancelled.
- Deployed `/sales/slaughter` page smoke passed and included the multi-pig row container plus batch total UI.
- Synthetic test pig IDs were `PIG-TEST-L4E-A-20260521180640` and `PIG-TEST-L4E-B-20260521180640`; both synthetic transactions are cancelled.
- Phase 10.2L4 is closed after deployed synthetic verification; manual UI owner smoke is optional, not a blocker.
- S10 / real JC Slaghuis payment completion is now verified and closed for now. Owner entered the real payment/final amount on 2026-05-23 after backend deploy. Supabase shows sale `SALE-2026-1DE373` for pig `PIG-2026-C390` / tag `S10`, buyer `JC Slaghuis`, destination `Bartelsfontein`, carcass weight `68 kg`, final amount `R2892.94`, `payment_status = Paid`, `payment_method = EFT`, `payment_date = 2026-05-23`, and `sale_status = Completed`. Focused sales transaction tests passed at 35 tests.
- Phase 10.3 telemetry review is selected as the next Phase 10 slice.
- Phase 10.3 working source is created in `docs/02-backend/SUPABASE_TELEMETRY_PLAN.md`.
- 10.3 scope is planning-first: inventory weather, Sunsynk, forecast, irrigation, and alert data; design compact backend read models for Oom Sakkie and dashboard use; keep working weather stable; and fix the slow Sunsynk path by moving toward backend/Supabase prepared payloads instead of more agent-over-sheet loops.
- Initial repo inventory found no backend telemetry modules or local telemetry ingestion scripts in this repo; current telemetry knowledge is in n8n workflow exports/docs, so external logger/cron/script locations still need owner confirmation.
- External source folders have been imported and filed under `external_sources/`: Sunsynk logger, local weather station logger, forecast logger, and non-telemetry landing-page source. One forecast `.env` file is present but ignored by git.
- 10.3A partial inventory now captures each logger's env vars, external API, sheet write target, and likely role.
- Owner confirmed telemetry loggers run as Render cron services; irrigation appears to be n8n-run.
- Owner confirmed production spreadsheets for Sunsynk, Weather, and Irrigation.
- Local Google Sheets inventory is blocked until the three telemetry spreadsheets are shared with service account `amadeuspigtrackersystem@amadeus-farm-weather-bot.iam.gserviceaccount.com`.
- Retention/rollup direction is captured: current state plus short raw retention, then daily/monthly/yearly rollups for long-term useful data.
- Service account access is now confirmed for the three telemetry sheets.
- Weather and irrigation tab/header/formula inventory succeeded.
- Sunsynk metadata inventory succeeded, but values reads timed out even on tiny ranges, confirming the current Sunsynk sheet is not a good live answer source for Oom Sakkie.
- 10.3A conclusion: keep weather stable for now, treat irrigation as a later hardware-control/audit design, and prioritize Sunsynk current-state backend/Supabase read model first.
- 10.3B Sunsynk current-state read model is planned: first endpoint should be `GET /api/telemetry/power/current`, returning source freshness, current battery/solar/load/grid/generator state, deterministic flags, backend-prepared summary wording, and explicit stale/unavailable behavior.
- Owner agreed the 10.3B payload direction.
- 10.3C telemetry schema proposal is documented: first migration should be power-first with `telemetry_sources`, `power_readings_5min`, `power_latest_state`, and `telemetry_alerts`; hourly/daily/monthly/yearly rollups wait until energy calculation rules are confirmed.
- Owner agreed to implement 10.3C.
- Phase 10.3C first telemetry power schema migration is implemented locally: `supabase/migrations/202605210005_create_telemetry_power_tables.sql`.
- Backend verifier `GET /health/database/telemetry-power-schema` is implemented locally.
- Migration creates `telemetry_sources`, `power_readings_5min`, `power_latest_state`, and `telemetry_alerts`, and seeds `sunsynk-main-inverter`; it imports no telemetry readings, changes no Render logger, and changes no n8n workflows.
- Local verification passed on 2026-05-21: focused database tests passed at 18 tests, local missing-config verifier smoke returned safe `503`, and full local unittest suite passed at 211 tests.
- Phase 10.3C deployed verification passed on 2026-05-21: `/health/database/telemetry-power-schema` returned `success = true`, `status = ok`, migration ID `202605210005_create_telemetry_power_tables`, all four expected tables found, `missing_tables = []`, and `sunsynk_source.source_id = sunsynk-main-inverter` with `stale_after_minutes = 15`.
- Future Supabase migrations can be run directly from the local workspace when `DATABASE_URL` is available locally and network/database command approval is granted; still inspect SQL first, run exactly one migration file, then verify through the matching backend health endpoint.
- 10.3D ingestion decision is documented: existing Render Sunsynk logger should call the Flask backend, not write directly to Supabase. Backend owns validation, raw/latest writes, summary flags, and Oom Sakkie read model.
- 10.3E backend endpoints are implemented locally: `POST /api/telemetry/power/ingest` protected by `TELEMETRY_INGEST_API_KEY`, and `GET /api/telemetry/power/current` for Oom Sakkie/dashboard current-state reads.
- Local verification passed on 2026-05-21: focused telemetry tests passed at 8 tests, local route smokes returned safe config failures, and full local unittest suite passed at 219 tests.
- 10.3D/10.3E deployed verification passed on 2026-05-21: synthetic ingest returned `success = true`, `status = ok`, `source_id = sunsynk-main-inverter`, `reading_id = PWR-FEC6256BECB7`, and `source.writes_to_supabase = true`.
- Deployed current-state readback passed: `/api/telemetry/power/current` returned battery `82%`, battery state `charging`, solar `3120 W`, load `1240 W`, grid state `not_using_grid`, generator `off`, deterministic flags, and stale summary because the synthetic timestamp was intentionally old.
- Security note: rotate `TELEMETRY_INGEST_API_KEY` before wiring the real Render Sunsynk logger if the current test key was pasted into chat or logs.
- 10.3F Sunsynk logger update is implemented locally in `external_sources/telemetry/sunsynk/amadeus-sunsynk-logger/main.py`.
- Logger now posts to backend ingest when `AMADEUS_BACKEND_URL` and `TELEMETRY_INGEST_API_KEY` are set, while keeping Google Sheets as a transition mirror unless `GOOGLE_SHEETS_ENABLED=false`.
- Logger README added with required Render cron env vars.
- First Render cron recovery test failed in the Google Sheets mirror path with `gspread` 404. The deployed current-state endpoint still showed the old synthetic test reading, so the real logger had not yet refreshed Supabase.
- Logger is now hardened locally so a successful backend ingest is not failed by a Google Sheets mirror error. Recommended next recovery test is to deploy the hardened logger with `GOOGLE_SHEETS_ENABLED=false`, then verify `/api/telemetry/power/current` returns a fresh real reading.
- Render cron source has been moved to the main `amadeus-pig-tracking-system` repo with root directory `external_sources/telemetry/sunsynk/amadeus-sunsynk-logger`; a trailing-space root directory issue was corrected.
- Phase 10.3F deployed verification passed on 2026-05-22: Render cron printed `backend_ingest_enabled = true`, `backend_ingest_success = true`, reading ID `PWR-49F0F62E4F21`, `google_sheets_written = true`, and timestamp `2026-05-22T00:28:20+02:00`.
- `/api/telemetry/power/current` read back the real fresh state with `data_age_minutes = 0`, `is_stale = false`, battery `47%`, battery state `discharging`, load `872 W`, no solar, no grid, and no generator.
- Local syntax verification passed with `python -m py_compile`.
- Next step is updating Oom Sakkie `2.2` to use the backend current power endpoint instead of Google Sheets.
- 10.3G local workflow update is prepared: `2.2 - Amadeus Sunsynk Sub-Agent` now calls `GET /api/telemetry/power/current` and formats the backend payload directly, with no LangChain agent loop and no Sunsynk Google Sheets tools.
- `2.0 - OOM SAKKIE` `Sunsynk_Info_Tool` description now describes the backend/Supabase current-power tool and limits this slice to current/live power state.
- Local JSON parse verification passed for both updated workflow exports.
- 10.3G live verification passed on 2026-05-22 after importing `2.2` and `2.0` into n8n.
- Telegram test `What's the power like now?` returned quickly with current backend/Supabase data: battery `46%` discharging, solar `0.0 kW`, load `1.0 kW`, grid not using grid `0 W`, generator off `0 W`, latest reading `22 May 2026, 00:40`, and data age `4 minutes`.
- This confirms Oom Sakkie current power questions no longer depend on slow Sunsynk Google Sheets reads.
- Next selected slice was the recent power profile endpoint.
- 10.3H local backend slice is prepared: `GET /api/telemetry/power/recent?hours=24` summarizes recent Supabase `power_readings_5min` rows without claiming kWh/cost totals.
- The recent-power endpoint returns sample-based battery range, average/max solar/load, grid/generator active sample counts, approximate active minutes, hourly buckets, and coverage percentage.
- Focused telemetry/workflow tests pass at 11 tests, and the full local test suite passes at 221 tests.
- First deployed check returned `success = true` with expected sections, but it still included the old synthetic test row inside the 24-hour window.
- A local follow-up patch now excludes rows with `raw_payload is null` so synthetic/manual test rows do not skew the recent profile.
- Focused telemetry/workflow tests still pass at 11 tests after the exclusion patch.
- 10.3H deployed verification passed after redeploy: `/api/telemetry/power/recent?hours=24` returned `success = true`, 24 real cron rows, first reading `2026-05-21T22:28:20+00:00`, last reading `2026-05-22T00:20:21+00:00`, battery range `42%` to `47%`, average load `0.9 kW`, maximum solar `0.0 kW`, and no grid/generator activity.
- The synthetic `82%` / `3120 W` row is no longer present in the live response.
- 10.3I live verification passed on 2026-05-22 after importing updated `2.2` and `2.0`.
- Oom Sakkie now answers current/live power questions from `/api/telemetry/power/current` and recent/last-24h/trend questions from `/api/telemetry/power/recent?hours=24`.
- Live Telegram checks passed for current power, last-24h profile, last-night grid use, and solar-total limitation wording.
- `2.2` remains deterministic with no LangChain agent loop and no Google Sheets tool reads. kWh, cost, import, and export total questions return the sample-based profile with a clear limitation instead of invented totals.
- Minor polish note: recent-profile answers can repeat the sample-based limitation because the backend operator notes and workflow formatter both include it. This is harmless and can be cleaned up in a later wording pass.
- 10.3J weather/forecast alignment is now in planning.
- Current `2.1 - Amadeus Weather Sub-Agent` is sheet-backed and uses an LLM router plus LLM answer formatter. It reads `LLM_Latest_Reading`, `Forecast_10Day_Current`, and `Daily_Pivot`.
- Current `2.1.1 - Amadeus Forecast Tool` is a standalone Open-Meteo utility, but the normal `2.1` Oom Sakkie weather path does not appear to call it.
- Recommended direction is the same proven Sunsynk pattern: define backend weather/forecast payload contracts first, then add Supabase schema and ingest/read endpoints, then update `2.1` only after direct backend checks pass.
- 10.3J1 weather/forecast read-model contract is drafted in `docs/02-backend/SUPABASE_TELEMETRY_PLAN.md`.
- First build contract includes `/api/telemetry/weather/current` and `/api/telemetry/weather/forecast?days=3`.
- `/api/telemetry/weather/today` is planned as a follow-up unless it is very low-risk to add after current/forecast are proven.
- The contract defines success/unavailable payloads, units, freshness, deterministic weather/forecast flags, and backend summary wording.
- 10.3J2 is implemented and the Supabase schema has been applied.
- Local migration `supabase/migrations/202605220001_create_telemetry_weather_tables.sql` creates `weather_readings`, `weather_latest_state`, and `weather_forecast_snapshots`, and inserts telemetry sources for `weather-station-main` and `open-meteo-forecast-main`.
- Backend endpoints are prepared for weather current read, forecast read, weather ingest, and forecast ingest.
- Health endpoint `/health/database/telemetry-weather-schema` is prepared.
- Local validation passed on 2026-05-22: syntax compile passed, focused weather/database checks passed, and broader telemetry/database/workflow tests passed at 53 tests.
- The migration was applied directly to Supabase on 2026-05-22 and schema health verified `success = true`, `missing_tables = []`, with both weather/forecast sources present.
- Direct local Supabase read checks returned clean unavailable responses for `/api/telemetry/weather/current` and `/api/telemetry/weather/forecast?days=3` before logger ingest.
- Synthetic local ingest is blocked until `TELEMETRY_INGEST_API_KEY` is configured in the local `.env`; Render already has the key for deployed endpoint testing.
- Deployed backend verification on 2026-05-22 passed for `/health/database/telemetry-weather-schema`, `/api/telemetry/weather/current`, and `/api/telemetry/weather/forecast?days=3`; both read endpoints return clean unavailable responses before logger ingest.
- Synthetic deployed ingest is blocked because the available test key returned `401 unauthorized`; use the current Render `TELEMETRY_INGEST_API_KEY` value for the next ingest/readback check.
- Synthetic deployed ingest/readback passed on 2026-05-22 after confirming the current ingest key.
- `POST /api/telemetry/weather/ingest` wrote reading `WTH-5D66D385B9F5` to Supabase and `/api/telemetry/weather/current` read it back with temperature `14.2 C`, humidity `86%`, wind `5.4 km/h`, rain today `0.4 mm`, `success = true`, and source `weather-station-main`.
- `POST /api/telemetry/weather/forecast/ingest` wrote 3 forecast rows and `/api/telemetry/weather/forecast?days=3` read them back with `returned_days = 3`, rain expected on 2 days, and source `open-meteo-forecast-main`.
- The current weather/forecast values in Supabase are synthetic test values until the real Render weather and forecast loggers overwrite them.
- First owner weather cron run still showed the old Sheets-only output (`Logged Current_Conditions...`), confirming the logger code had not yet been updated for backend ingest.
- Local logger updates are prepared: both weather and forecast Render cron scripts now support `BACKEND_INGEST_ENABLED=true`, keep `GOOGLE_SHEETS_ENABLED=true` as a mirror, post to the backend ingest endpoints, and print JSON results including `backend_ingest_success`.
- 10.3J3 logger verification passed on 2026-05-22.
- `/api/telemetry/weather/current` now reads fresh real weather-station data from Supabase: temperature `14 C`, humidity `96%`, wind `8 km/h`, rain today `0 mm`, data age `0`, source `weather-station-main`.
- `/api/telemetry/weather/forecast?days=3` now reads fresh real Open-Meteo forecast data from Supabase: returned 3 days, rain possible on 1 day, data age `0`, source `open-meteo-forecast-main`.
- This confirms the real weather and forecast Render cron loggers have overwritten the synthetic test values.
- 10.3J4 local workflow simplification is prepared.
- `2.1 - Amadeus Weather Sub-Agent` now has only deterministic backend-read nodes: trigger, route weather question, HTTP read, format answer, and sticky note.
- The old `2.1` Google Sheets and LLM nodes are intentionally removed from the export: `Weather Router (JSON Plan)`, `Weather Answer LLM (JSON only)`, `Precheck - Latest Station Row`, `Read Forecast_10Day_Current`, and `Read Daily_Pivot`.
- `2.0 - OOM SAKKIE` `Weather_Info_Tool` description now identifies `2.1` as a backend/Supabase current-weather and forecast worker.
- Workflow contract tests pass at 15 tests after the local workflow export update.
- 10.3J4 live verification passed on 2026-05-22 after importing updated `2.1` and `2.0`.
- Telegram test `What is the weather like now?` returned current backend weather: temperature `14 C`, humidity `96%`, wind `4 km/h`, gusts `4 km/h`, rain now `0 mm/h`, rain today `0 mm`, pressure `1013.9 hPa`, and latest reading age `0 minutes`.
- Telegram test `What is the weather forecast for the next few days?` returned the 3-day backend forecast for 22-24 May 2026, including rain possible on 1 day and forecast age `1 minute`.
- Oom Sakkie did not mention tools, workflows, Google Sheets, or Supabase in the tested answers.
- Owner selected the next telemetry order: 1) weather today summary, 2) weather alert alignment, 3) irrigation/audit planning.
- 10.3K local weather today summary is prepared.
- Added read-only `GET /api/telemetry/weather/today`, with optional `date=YYYY-MM-DD`.
- The endpoint summarizes existing Supabase `weather_readings` for the selected local day: reading count, first/last reading, coverage estimate, min/max/average temperature, average humidity, max wind/gust, rain total, max rain rate, flags, and operator notes.
- It excludes synthetic test rows where `raw_payload.test = true`, so the earlier synthetic weather ingest does not skew today summaries.
- Local live Supabase readback returned real-only data for 2026-05-22: 5 readings, first reading `2026-05-22T02:49:30+00:00`, last reading `2026-05-22T03:04:54+00:00`, temperature `14 C`, rain total `0 mm`, max wind `9 km/h`, coverage `8.1%`.
- Focused and broader telemetry/database/workflow tests pass at 55 tests.
- 10.3K deployed verification passed on 2026-05-22.
- `/api/telemetry/weather/today` returned `success = true`, date `2026-05-22`, 25 real readings, coverage `30.5%`, first reading `2026-05-22T02:49:30+00:00`, last reading `2026-05-22T04:45:06+00:00`, min/max/avg temperature `14 C` / `15 C` / `14.24 C`, rain total `0 mm`, max wind `9 km/h`, and max gust `10 km/h`.
- Explicit dated check `/api/telemetry/weather/today?date=2026-05-22` returned the same result.
- The old synthetic `0.4 mm` rain row is excluded.
- `2.1` local workflow update is prepared so today/daily/rain-today weather questions route to `/api/telemetry/weather/today`.
- First live Telegram check for `What happened with the weather today?` still returned the current-weather branch. The local workflow files were hardened: `2.0` now passes the exact Telegram message into `2.1`, and `2.1` now checks additional input fields plus `what happened ... today` wording.
- Workflow/weather tests pass at 26 tests after the route update.
- 10.3K live verification passed on 2026-05-22 after importing the hardened `2.0` and `2.1` workflows.
- Telegram test `What happened with the weather today?` returned the today-summary branch: 30 readings, 34.5% coverage, temperature `14 C` to `15 C`, average `14.4 C`, average humidity `94.6%`, rain total `0 mm`, max rain rate `0 mm/h`, max wind `9 km/h`, max gust `10 km/h`, and measurement window `22 May 2026, 04:49` to `07:10`.
- 10.3L weather alert alignment is planned.
- Owner agreed alerts should use the backend/Supabase approach rather than the old Sheets-first alert workflows.
- Backend should own alert rules, cooldowns, duplicate prevention, and alert history in `telemetry_alerts`.
- n8n should later become a thin scheduled caller and Telegram delivery layer.
- Existing `ALERT - Local Weather Station` and `ALERT - Weather Forecast` remain documented and should not be activated as the source of truth during this rebuild.
- Initial alert defaults are documented for station stale, rain, heavy rain, sustained wind, gusts, temperature, forecast rain, and forecast wind.
- First safe recipient should be Charl only.
- Default quiet hours are `21:00` to `06:00` Africa/Johannesburg, with `HIGH` current-condition alerts allowed through.
- 10.3L2 backend evaluator is implemented locally.
- Added protected `POST /api/telemetry/weather/alerts/evaluate`.
- Evaluator supports dry-run mode, returns `sendable_alerts`, `held_alerts`, and `suppressed_alerts`, and writes only sendable alerts in apply mode.
- It does not send Telegram messages.
- Focused weather tests pass at 13 tests; telemetry/database/workflow suite passes at 57 tests.
- Real Supabase dry-run returned `success = true`, `mode = dry_run`, and zero current alert candidates under normal weather conditions.
- 10.3L2 deployed dry-run verification passed on 2026-05-22.
- Production `POST /api/telemetry/weather/alerts/evaluate` with `{"dry_run": true}` returned `success = true`, `status = ok`, `mode = dry_run`, zero candidates, zero sendable/held/suppressed alerts, and `writes_to_supabase = false`.
- Backend-only audit test path is prepared locally.
- `POST /api/telemetry/weather/alerts/evaluate` supports `{"include_test_alert": true}` to create a clearly marked `BACKEND_AUDIT_TEST` candidate.
- In apply mode this writes one backend audit row to `telemetry_alerts`; it does not send Telegram.
- Focused weather tests pass at 14 tests.
- 10.3L2 backend audit apply verification passed on 2026-05-22.
- Production audit dry-run with `{"dry_run": true, "include_test_alert": true}` returned one `BACKEND_AUDIT_TEST` candidate and `writes_to_supabase = false`.
- Production audit apply with `{"include_test_alert": true}` wrote alert `ALT-F20D2245949B`.
- Supabase verification confirmed `ALT-F20D2245949B` exists with `area = weather`, `alert_type = BACKEND_AUDIT_TEST`, `severity = info`, `status = Open`, `details.test = true`, and `details.safe_to_ignore = true`.
- No Telegram message was sent.
- 10.3L4 n8n weather alert delivery is planned.
- New workflow should be `ALERT - Weather Backend Delivery`.
- It must call the backend evaluator and treat backend as the only source of truth.
- It must ignore `BACKEND_AUDIT_TEST` and any alert with `details.test = true`.
- First recipient scope is Charl only.
- Legacy Sheets-first alert workflows remain inactive/documented.
- 10.3L4 local workflow export is built:
  - `docs/04-n8n/workflows/ALERT - Weather Backend Delivery/workflow.json`
  - `docs/04-n8n/workflows/ALERT - Weather Backend Delivery/README.md`
- The workflow is inactive by default.
- It calls `POST /api/telemetry/weather/alerts/evaluate`.
- n8n Cloud denied `$env` access in the HTTP node, so the workflow now uses a manually configured `X-Amadeus-Telemetry-Key` header value matching Render's `TELEMETRY_INGEST_API_KEY`.
- It filters out dry-run responses, `BACKEND_AUDIT_TEST`, and `details.test = true`.
- It sends through `Telegram - Oom Sakkie` to Charl-only chat ID `5721652188` for the first trial.
- Workflow contract tests pass at 16 tests.
- Manual n8n dry-run/audit-test verification passed on 2026-05-23: the backend returned one `BACKEND_AUDIT_TEST` candidate in dry-run mode, `Code - Extract Sendable Alerts` emitted zero items, and no Telegram delivery occurred.
- Scheduled dry-run verification passed on 2026-05-23: execution `47520` ran from the schedule, backend returned `success = true`, `mode = dry_run`, zero candidates/sendable/held/suppressed alerts, and `Code - Extract Sendable Alerts` output zero items, so Telegram delivery was not reached.
- Live scheduled verification passed on 2026-05-23: workflow was active with `dryRun = false` and `includeTestAlert = false`; execution `47527` ran from the schedule, backend returned `success = true`, `mode = apply`, zero candidates/sendable/held/suppressed alerts, `source.writes_to_supabase = true`, and no written alert IDs; `Code - Extract Sendable Alerts` output zero items, so Telegram delivery was not reached because there were no real alerts.
- Weather backend alert delivery is live-verified. `ALERT - Weather Backend Delivery` remains active as the only live weather alert delivery workflow.
- Legacy weather alert cleanup is complete: `ALERT - Local Weather Station` and `ALERT - Weather Forecast` are inactive/archived in n8n and their repo exports have been removed.
- 10.3N Sunsynk/power alert backend alignment is live-verified: `POST /api/telemetry/power/alerts/evaluate` is implemented, backend rules/cooldowns/quiet-hours handling now cover not logging, battery low/medium/high, grid active, generator active, and `POWER_BACKEND_AUDIT_TEST`, and `ALERT - Power Backend Delivery` has been imported, manually dry-run tested, and live-tested.
- Manual n8n dry-run and audit dry-run executions passed on 2026-05-23: the backend returned real power candidates, `POWER_BACKEND_AUDIT_TEST` was filtered out, `Code - Extract Sendable Alerts` emitted zero items while `dryRun = true`, and no Telegram delivery occurred.
- Live n8n execution `47565` passed on 2026-05-23: backend mode was `apply`, `POWER_BATTERY_LOW` was written to Supabase as `ALT-C758569F3D95`, Telegram delivery succeeded, and `POWER_GRID_ACTIVE` was held by quiet hours.
- Owner made `ALERT - Power Backend Delivery` live and removed the old `ALERT - Sunsynk` workflow from n8n. Repo cleanup also removed the legacy `docs/04-n8n/workflows/ALERT - Sunsynk/` export so it does not get reintroduced by mistake.
- Owner planning notes are captured in `SUPABASE_TELEMETRY_PLAN.md`: future Sunsynk cost/value reporting should use `R9.10/kWh` as a planning default; n8n remains a thin integration layer for now; human alerts must be separated from automation triggers for irrigation/pumps/power safety.
- 10.3O irrigation inventory/control-boundary planning has started. Current export shows `2.3.1 - Build Daily Irrigation Plan` is active and writes irrigation plan/state/log rows, while `2.3.2 - Run Irrigation Controller` is inactive but contains direct IFTTT start/stop HTTP nodes that can control real hardware if activated.
- Current irrigation guardrail: do not activate `2.3.2`, do not edit IFTTT start/stop nodes, do not add Oom Sakkie irrigation start/stop commands, and do not build backend hardware-control endpoints until the command/audit/safety model is agreed.
- Owner clarified the long-term irrigation goal: read-only backend status first, then a smart adaptive irrigation system that uses zone configuration, crop/planting context, season runtimes, priority, forecast, current weather, and power state to build/pause/reschedule plans and inform the owner when zones run, stop, change, or get reprioritized.
- Owner added that irrigation is a core operating system and must support future devices/triggers. Fertilizer injection and fertilizer tank mixing valves should be modeled separately from normal irrigation zones, and future tank full/empty states should become structured sensor inputs rather than ad hoc workflow logic.
- 10.3P read-only irrigation status endpoint is deployed and verified. `GET /api/telemetry/irrigation/status` reads the existing irrigation sheet, returns `mode = read_only`, disables all hardware-control flags, and verified today's plan from `Amadeus_Irrigation_Logs` without writing to Sheets or Supabase. A next-zone clarity patch is prepared: the endpoint keeps `STATE.next_zone_id` as the displayed next zone when present, but also returns computed priority/water-score next-zone fields and a mismatch flag for cautious Oom Sakkie wording.
- 10.3Q is live-verified. `2.3.3 - Irrigation Status Tool` is active as the read-only Oom Sakkie irrigation status worker, updated `2.0` has `Irrigation_Info_Tool`, and owner confirmed status and control-style tests work. `2.3.2 - Run Irrigation Controller` remains inactive and must not execute during Oom Sakkie status tests.
- 10.3R irrigation Supabase data model is deployed and verified. Render `/health/database/irrigation-schema` returned `success = true`, `status = ok`, `missing_tables = []`, all eight expected irrigation tables, and source row `irrigation-controller-main`. No data import, dashboard, Oom Sakkie control, IFTTT call, or `2.3.2` change has been made.
- 10.3S irrigation sheet-to-Supabase dry-run is complete. `scripts/irrigation_import_dry_run.py` read `Amadeus_Irrigation_Logs` and wrote nothing to Supabase or Google Sheets. Real dry-run mapped 2 zones, 73 daily plans, 146 plan items, 77 events, and 1 state snapshot, with no duplicate zone IDs and no link issues. Next decision is whether to build a controlled apply/import path or first refine state snapshots, auxiliary devices, and tank sensor modeling.
- Owner decision: irrigation `STATE` is the latest truth, not an append-only state history. The controlled import path should upsert `irrigation_state_snapshots` by `state_snapshot_id` and use `irrigation_events`, daily plans, and plan items for historical questions. Detailed state history can be added later as a separate model only if needed.
- 10.3T controlled irrigation Supabase import is applied and verified. Import batch `IMPORT-20260523-IRRIGATION-SHEET-V1` contains 2 zones, 73 daily plans, 146 plan items, 77 events, and 1 latest-state row. Verification confirmed `IRRSTATE-MAIN` is the only state row, with `current_status = IDLE`, `current_zone_id = C12345`, and `next_zone_id = C12345`. No Google Sheets writes, n8n changes, hardware commands, IFTTT calls, or `2.3.2` activation occurred.
- Owner clarification: irrigation's operational plan should be today's active plan only, refreshed/rebuilt daily. Historical imported plan rows can stay in Supabase for audit/review, but the current status/dashboard read path must not combine historical plans into the live plan view.
- 10.3U Supabase-backed irrigation status read path is locally ready. The existing endpoint can now read from Supabase when `IRRIGATION_STATUS_SOURCE=supabase`, or try Supabase then fall back to Sheets when `IRRIGATION_STATUS_SOURCE=auto`. Default remains `google_sheets` until the daily planner/sync path writes refreshed plans into Supabase. Local live-style Supabase check returned today's selected plan `IRRPLAN-2026-05-23` and read-only safety flags.
- 10.3V irrigation daily sync is applied locally for `2026-05-23`. `scripts/irrigation_daily_sync.py` plan-only mode scoped the sync to 1 daily plan, 2 plan items, 1 latest-state row, 1 event, and 2 zones; `--apply` wrote batch `SYNC-IRRIGATION-2026-05-23` to Supabase. Supabase-backed status then returned `IRRPLAN-2026-05-23` with only today's two plan items and read-only safety flags. This is still manual; no scheduled sync has been created.
- 10.3U/V deploy default-source check passed. Render `/api/telemetry/irrigation/status?date=2026-05-23` still uses `source = google_sheets`, returns today's two planned zones, returns latest `STATE`, and keeps `read_only = true`, `can_control = false`, and `hardware_commands_enabled = false`. `IRRIGATION_STATUS_SOURCE=auto` has not yet been enabled on Render.
- 10.3U/V auto-source check passed after setting `IRRIGATION_STATUS_SOURCE=auto` on Render. Deployed response returned `source = supabase`, `today.daily_plan_id = IRRPLAN-2026-05-23`, today's two current plan rows only, and read-only safety flags. A local polish fix dedupes repeated recent events caused by historical import plus daily sync containing the same logical `PLAN_CREATED` event; redeploy needed for that display cleanup.
- 10.3U/V is live-verified after redeploy. Supabase source remains active, duplicate recent event display is cleaned up, and safety flags remain `read_only = true`, `can_control = false`, and `hardware_commands_enabled = false`.
- 10.3W telemetry rollup planning has started. Recommended defaults are now captured: keep power/weather raw readings for at least 90 days initially, keep irrigation events/plans long-term, keep daily/monthly/yearly rollups permanently, mark kWh/Rand values as estimated unless confirmed Sunsynk energy counters are available, and do not delete old telemetry Sheets until backup/import/compare checks are accepted.
- Owner accepted 10.3W defaults: 90-day raw retention for power/weather, delete old telemetry Sheets after backup/import/compare acceptance rather than leaving permanent clutter, include fertilizer/tank placeholders in irrigation rollups now, and keep `R9.10/kWh` as the planning tariff until a tariff table exists.
- 10.3W2 is applied and locally verified. Migration `202605230003_create_telemetry_rollup_tables` created empty daily/monthly/yearly rollup tables for power, weather, and irrigation; `/health/database/telemetry-rollup-schema` returned `success = true`, `status = ok`, all nine expected tables, and `missing_tables = []`. No rollup generation, schedule, dashboard, sheet deletion, or irrigation control path has been added.
- 10.3W3 plan-only daily rollup generator is ready. `scripts/telemetry_daily_rollup_plan.py --date 2026-05-23` returned one candidate row each for power, weather, and irrigation daily rollups with `writes_to_supabase = false`; power/weather coverage was `40.97%` because the day was still in progress, and irrigation event count deduped to `1`. Tests passed at 64 telemetry/database tests.
- 10.3W4 one-date daily rollup apply is complete for `2026-05-23`. Supabase now has one daily power, weather, and irrigation rollup row for that date. The apply was manual and unscheduled; no monthly/yearly rollup, dashboard, sheet cleanup, or hardware-control path was added. Because the date was still in progress, power/weather coverage is low and should be treated as a write-path test rather than a final closed-day summary.
- 10.3W5 read-only daily rollup compare endpoint is ready. `GET /api/telemetry/rollups/daily?date=2026-05-23` returns stored daily rollups plus current raw/source counts and correctly flags current-day drift because new power/weather samples arrived after the test rollup apply. This confirms the app can inspect rollup quality before scheduling.
- 10.3W6 after-day-close guard is ready. `scripts/telemetry_daily_rollup_plan.py --apply` now refuses today/future ZA dates unless `--allow-partial` is explicitly supplied. Normal current-day apply returned `day_not_closed`; explicit partial apply refreshed the test rows, and the compare endpoint then showed matching current/stored sample counts while still warning low coverage.
- 10.3W7 schedule command is verified. Owner selected `00:15` Africa/Johannesburg. Script now supports `--previous-day`; command `python scripts/telemetry_daily_rollup_plan.py --previous-day --apply` successfully applied `2026-05-22` without partial override, and compare endpoint confirmed power/weather stored counts match raw counts with power quality `complete` and weather quality `usable`.
- Backend containing the 10.3W7 rollup script was deployed by owner on 2026-05-25. Render cron `amadeus-telemetry-daily-rollups` was created on 2026-05-25 and built successfully. It runs `python scripts/telemetry_daily_rollup_plan.py --previous-day --apply` at `10:15 PM UTC` / `00:15 Africa/Johannesburg`. No `render.yaml` / Render Blueprint exists in this repo, so the cron is dashboard-managed.
- 10.3W8 first scheduled Render cron run is verified on 2026-05-26. `/api/telemetry/rollups/daily?date=2026-05-25` returned `success = true`, `status = ok`, and all three rollups found. Power stored/current samples matched at `288/288` with `100%` coverage and quality `complete`; weather matched at `287/288` with `99.65%` coverage and quality `complete`; irrigation matched with `0` events and `0` plan items. Power kWh/Rand values remain estimated until confirmed Sunsynk energy counters are available.
- Phase 10 farm home/dashboard first local slice is ready and owner-reviewed on 2026-05-26. The `/` dashboard now uses the wide `ops-shell` / `ops-dashboard` operational template rather than the narrow centered `page-card` layout. It is read-only and uses existing APIs for weather, forecast, power, irrigation, daily rollups, herd/litter attention, sales exits, and order attention. Local focused tests and route smoke passed. Owner accepted the layout direction after stale local port `5000` processes were replaced and telemetry data loaded correctly. Minor visual polish remains before deploy, especially tight metric wrapping.
- Dashboard polish was applied locally on 2026-05-26: compact metric tiles auto-fit, values no longer split mid-word, machine labels are shown as readable labels, and the dashboard script cache-buster was refreshed. Local port `5000` was restarted and confirmed to serve the updated dashboard plus JSON telemetry endpoints.
- Phase 10 farm home/dashboard live verification passed on 2026-05-26. Owner confirmed the live home page is good after deploy.
- Dashboard slaughter sale count/value source is live-verified on 2026-05-26. The dashboard summary now reads monthly non-cancelled Supabase `sales_transactions` and exposes count plus net Rand value by stream; the home dashboard displays Livestock, Slaughter, and Meat as `count / value`. Old pig-exit monthly counts remain available as separate audit fields. Owner confirmed the live dashboard now displays the slaughter sale value.
- `/sales-dashboard` sales overview/reporting is deployed and owner-accepted for now: it provides period/month/year/custom filters, stream filters, sales totals across livestock/slaughter/meat, a clickable transaction ledger, and keeps stock availability as a secondary section. Owner will note refinements during real use.
- Phase 11A read-only pig allocation readiness is local and ready for deploy/browser check: `docs/08-business-modules/PORK_BUSINESS_INTEGRATION_READINESS_MAP.md` defines the integration map, `GET /api/pig-weights/pig-allocation-readiness` returns explainable readiness buckets with `writes_to_sheets = false` and `writes_to_supabase = false`, `/pig-allocation` shows a full-width filterable planning table, and the dashboard has a `Pig Allocation` shortcut. No purpose/status/allocation writes were added.
- Owner clarified the Phase 11A business model on 2026-06-04: allocation should be driven by wean weight, average daily gain, litter/parent quality, current demand, and fallback outlets. Fast growers from good litters are reviewed for breeding first, then meat; current grow-out pigs mostly go to slaughter/abattoir until meat sales are live; slow growers become livestock sale candidates to reduce feed cost; meat candidates that pass the meat window without preorder should fall back to abattoir slaughter. Next practical refinement is adding growth and litter-quality context to `/pig-allocation`.
- Phase 11A growth/litter-quality refinement is local and ready for deploy/browser check: `/pig-allocation` and `GET /api/pig-weights/pig-allocation-readiness` now include `Needs Classification`, wean date/weight, latest weight freshness, average daily gain, growth class, litter survival/quality, and sow/boar context. Fast growers from good litters are flagged for breeding review before meat/slaughter; slow growers are flagged toward livestock sale. Still read-only with no purpose/status/allocation writes.
- Phase 11A growth-band refinement is local and ready for deploy/browser check: growth now uses lifetime ADG where possible, keeps post-wean ADG as secondary context, and displays clearer bands from `Extremely Slow` through `Exceptional`, with the target direction of `0.500 kg/day` or better. Full local unittest suite passed at 345 tests on 2026-06-05.
- Phase 11A readiness timing refinement was browser-checked by owner and looked good on 2026-06-05; owner started deployment. `/pig-allocation` now shows estimated meat and abattoir readiness dates from lifetime ADG, and the first abattoir/slaughter range is `80-95 kg` so it no longer overlaps the young meat window. API smoke confirmed the new timing fields and `slaughter_target_min_kg = 80`.
- Phase 11A outlet recommendation refinement is local and ready for verification: `/pig-allocation` now includes read-only `Recommended Action`, `Outlet Priority`, and `Marketing Readiness` signals. Extremely slow/slow growers point toward livestock sale, meat-window pigs point toward meat preorder/interest generation, and abattoir/slaughter remains the fallback. This is the first signal layer for the future weekly self-selling system, but no Telegram, Meta/Facebook, Supabase, or Google Sheets write automation was added.
- Phase 11A allocation settings read-model refinement is owner-reviewed and making sense: allocation thresholds are centralized in backend defaults, the API returns raw `thresholds` plus human-readable `business_rules`, and `/pig-allocation` displays the active rule strip above the table. This prepares for a later approved settings page or `SYSTEM_SETTINGS` integration without adding any writes.
- Phase 11A suggested-purpose signal is owner-reviewed and making sense: `/pig-allocation` now shows read-only suggested purpose, reason, and confidence beside the stored `Purpose`. This clarifies what the system would recommend without updating any pig records.
- Phase 11B Herdmaster purpose review is the active operational focus as of 2026-06-15: `Weaned - review purpose` means active/on-farm piglets in a weaned litter still have blank or `Unknown` purpose. The planned/implemented path is a `/purpose-review` table, optionally focused by `?litter_id=...`, where Herdmaster suggestions are reviewed by humans before a backend-owned action updates only `PIG_MASTER.Purpose`, `Updated_At`, and an audit note in `General_Notes`. This phase intentionally does not write sales, orders, slaughter, meat allocation, Telegram, Meta, Chatwoot, WhatsApp, or Supabase records. Ledger Sales Lead Rail work resumes after one real purpose review is visible and tested.
- Phase 11B attention timing is refined: weaned litters do not create purpose-review dashboard attention until 14 days after weaning. If post-wean weights are still missing then, attention routes to weight capture as `Post-wean weight needed`; once post-wean weights exist, attention routes to `/purpose-review?litter_id=...` as `Purpose review due`. Litter detail now also shows direct links to the correct next work page for these action types.
- Phase 11B local build status: the purpose-review table, recheck/no-write analysis, owner approval apply path, mark-weaned latest-weight carryover, `PIG_MASTER` wean-field enrichment, 14-day post-wean attention delay, and tighter purpose-review table layout are implemented and locally tested. This is not yet farm-proven: the next real weaning/post-wean-weight cycle must confirm the mark-weaned data carryover, weight-needed routing, purpose-review timing, and approved `PIG_MASTER.Purpose` writes before the function is treated as fully live-verified.
- Phase 11 Sales Lead Rail resume status 2026-06-15: reviewed Supabase migrations `202606140001_create_oom_sakkie_sales_campaigns.sql` and `202606140002_create_oom_sakkie_sales_leads.sql` were applied successfully; local API read checks returned clean campaign, outreach draft, send-design, and lead queues with all customer-send/Chatwoot/n8n/order/stock authority flags false. Oom Sakkie Ledger Sales Workbench now includes an owner `Record Lead` form that writes only append-only lead tracking records through the existing gated `/api/oom-sakkie/sales-leads` route. This still needs a real owner/inbound lead test before it is called live-verified.
- 2026-06-16 working position: the Oom Sakkie farm-command UI exact-match pass is intentionally parked and not accepted yet. The next build focus is sales launch readiness: prove one real owner-approved inbound/manual lead through the existing append-only sales lead rail, confirm it appears in Oom Sakkie/Ledger summaries, and use that evidence to design the first preorder/deposit or Sam handoff contract. Until that reviewed contract exists, sales remains owner-review/manual-follow-up only with no customer send, public post, quote/order creation, deposit request, stock reservation, allocation write, Chatwoot/WhatsApp/n8n call, or financial action.
- Phase 11C first real lead proof is live-recorded: owner-approved lead `OSK-SALES-LEAD-733F06EE5501FB3B` (`Jan - half carcass interest`) was inserted into the append-only sales lead rail with status `interested`, source `direct_known_buyer`, WhatsApp state `manual_owner_only`, interest `Half carcass / Set A / Riversdale`, and next action `Charl to confirm price/kg and available week`. Readback through the `launch_test` queue returned the lead and all authority flags false: no customer send, Chatwoot/n8n/WhatsApp call, quote/order creation, stock change, dispatch, runtime/prompt change, public output, farm-data write, physical control, or financial action. Next build should define the preorder/deposit or Sam handoff data contract from this real lead.
- Phase 11C preorder/deposit review contract is local and verified against Jan's real lead: `GET /api/oom-sakkie/sales-leads/<lead_id>/preorder-contract` builds a read-only packet from the lead, and the Ledger Sales Workbench exposes `Build Preorder Contract`. The packet for `OSK-SALES-LEAD-733F06EE5501FB3B` correctly shows core context present (`Half carcass`, `Set A`, `Riversdale`) and money-path fields still missing: price/kg, available week, estimated size/weight, deposit rule, payment method, delivery/collection, and final owner approval. This is review-only; it does not send Jan a message, request a deposit, create a preorder/order, reserve stock, write allocation, call Chatwoot/n8n/WhatsApp, or perform financial action.
- Phase 11C Sam meat-intake contract is local-ready: `docs/02-backend/SAM_MEAT_INTAKE_CONTRACT.md` defines the role split and payload for Sam's `meat_preorder` lane, and review-gated `POST /api/oom-sakkie/sales-leads/sam-meat-intake` maps bounded Sam/Chatwoot meat-interest facts into the same append-only sales lead rail. It accepts core fields such as customer, conversation, product type, cut set, location, timing, delivery/collection, payment preference, and notes; returns missing core/money-path fields plus Sam's next safe question; and keeps all customer-send, Chatwoot/n8n outbound, quote/order/preorder/deposit, stock/allocation, dispatch, runtime/prompt, farm-write, public-output, physical-control, and financial-action flags false. The next build is n8n contract planning for `1.0 Sam` detection/extraction and a Steward/backend call, not enabling live customer automation yet.
- Phase 11C Sam n8n handoff plan is documented: `docs/04-n8n/workflows/1.0 - Sam-sales-agent-chatwoot/MEAT_INTAKE_HANDOFF_PLAN.md` now defines how Sam should detect `meat_preorder`, collect bounded facts, preserve lightweight Chatwoot state, and avoid live-pig order routing. `1.2 - order-steward` README now explicitly blocks half-carcass/full-carcass/cut-set/assisted-slaughter requests from `create_order_with_lines`. A default-off token-gated remote private-test route now exists at `POST /api/oom-sakkie/channels/chatwoot/sam-meat-intake` using `OOM_SAKKIE_SAM_MEAT_INTAKE_REMOTE_ENABLED` and `OOM_SAKKIE_SAM_MEAT_INTAKE_REMOTE_TOKEN`; it records tracking-only leads and keeps customer-send/Chatwoot/n8n/order/stock/financial flags false. Next build: wire `1.0 Sam` detection/extraction to this endpoint in workflow JSON, still inactive until env setup and owner live-smoke approval.
- Phase 11C Sam remote ingest verification: route syntax passed, focused disabled/missing-token/bad-token/success tests passed, and the broader local Oom Sakkie group passed with 439 tests OK. The route remains off unless the explicit env flag and long token are configured.
- Phase 11C `1.0 Sam` meat-intake workflow branch is wired in the repo export and default-off: `Code - Build Sam Meat Intake Payload`, `IF - Sam Meat Intake Ready`, `HTTP - Sam Meat Intake Lead`, and `Code - Attach Sam Meat Intake Result` now sit after the existing intake result and return to the normal Sam classifier/context path. The branch only calls the backend when n8n variable `SAM_MEAT_INTAKE_HANDOFF_ENABLED` is truthy and `SAM_MEAT_INTAKE_REMOTE_TOKEN` is configured; it targets `POST /api/oom-sakkie/channels/chatwoot/sam-meat-intake`, passes bounded meat-preorder fields, and explicitly avoids live-pig `create_order_with_lines`. Next step: import/smoke this workflow only after backend env and n8n variables are configured, then verify one controlled Chatwoot meat lead is saved tracking-only with no customer-send/order/stock/financial authority.
- Phase 11C live-smoke tooling is ready: `scripts/sam_meat_intake_remote_smoke.py` performs a bad-token no-write check first and only creates a tracking lead when `SAM_MEAT_INTAKE_SMOKE_CREATE=1` is explicitly set. `docs/06-operations/SAM_MEAT_INTAKE_LIVE_SMOKE_CHECKLIST.md` now gives the backend env, n8n variable, import, remote route smoke, Chatwoot smoke, readback, and stop-condition checklist. Real remote smoke has not been run yet because the backend env token and n8n variables must be set outside the repo.
- Phase 11C WhatsApp smoke 2026-06-16: first controlled WhatsApp message reached live `1.0 Sam` and successfully saved tracking-only meat lead `OSK-SALES-LEAD-D583E2649366146A` through the remote backend route. The reply was not accepted because Sam asked for a live-pig weight range; execution review showed `Code - Decide Order Route` overwrote the meat-intake `reply_instruction`. The workflow was patched and uploaded to n8n workflow `V73HaIqVpzv44SFc`: meat `reply_instruction` is preserved, `MeatIntakeContext` is passed into the Sales Agent prompt, and explicit meat-preorder rules now block live-pig weight-range/order wording. Retest required with one natural WhatsApp follow-up before calling Sam meat intake live-verified.
- Phase 11C Sam meat-intake local smoke passed: a review-gated contract smoke created tracking-only lead `OSK-SALES-LEAD-38BB181ADB706DD0` from `sam-contract-smoke-001`, returned missing money-path fields `price_per_kg`, `deposit_rule`, and `owner_final_approval`, and kept all authority flags false. The smoke lead was immediately marked with append-only `closed` event `OSK-SALES-LEAD-EVENT-2700C3F912FEEDE4` so it remains audit evidence and not an active opportunity.
- Oom Sakkie product vision was captured on 2026-06-15 in `docs/00-start-here/PRODUCT_VISION.md`: the owner-facing `/oom-sakkie` page should become a one-screen command center with a live presence, agent dock, click/voice-open specialist dashboards, and the long System Workbench treated as an audit/admin drawer rather than the daily owner workflow. First UI slice exposes clickable agents, compact live summary cards, and specialist priority queues from trusted read-only backend data for Ledger, Herdmaster, Gatekeeper, Rootline, Butcher, Beacon, Sam, and Quartermaster without changing action authority.
- Oom Sakkie agent asset foundation is local-ready: `static/assets/agents/` now contains the canonical per-agent folders, starter `agent.json` metadata, a central `agent_registry.json`, and `docs/00-start-here/AGENT_ASSET_REGISTER.md`. The UI can read portrait metadata and falls back to initials until real images are uploaded.
- Oom Sakkie portrait integration is local-ready: the command-room presence, dock tile, and specialist avatar can use the uploaded static Oom Sakkie portrait with CSS motion/status rings. ElevenLabs voice candidate `Mikey Slater - Motivational Speaker` / `3y3q5VpFXdeyf5ooB12e` is recorded as a candidate but is not production-approved yet.
- Phase 11A meat planning read-model is owner-reviewed and accepted for now: `GET /api/pig-weights/meat-planning` and `/meat-planning` group allocation-derived candidates into ready-now, next-14-days, next-30-days, future, and fallback-abattoir views. It shows minimum preorder demand needed now/within 30 days, but creates no meat orders, deposits, allocations, posts, or writes.
- Phase 11A temporary demand scenario is owner-reviewed and accepted for now: `/meat-planning` has browser-only expected demand inputs for now and next 30 days, calculating surplus/shortfall against the meat pipeline without saving anything.
- Oom Sakkie voice operating agent PRD was added on 2026-06-06 at `docs/01-architecture/OOM_SAKKIE_VOICE_OPERATING_AGENT_PRD.md` and updated after owner clarified the local farm PC/screen goal plus Trillion inspiration. Claude review was logged on 2026-06-06: revise first, avoid split-brain behavior with existing n8n `2.0`, and build a narrow Phase 10.6A read-only kiosk MVP before voice or advanced agents. Owner confirmed backend-as-brain on 2026-06-06: Flask/backend is the single long-term Oom Sakkie brain; n8n/GateKeeper remains Telegram I/O, callback routing, and scheduled work; Telegram cutover happens only after kiosk/backend is proven, using a feature-flagged parallel run. Backend `/api/oom-sakkie/message` should use exact-match/rule routing before any LLM classifier so proven power/weather/farm-attention phrasings stay deterministic.
- Phase 10.6A Oom Sakkie kiosk MVP is owner-tested: added backend-owned `modules/oom_sakkie/`, typed read-only tool registry, rule-first classifier, `POST /api/oom-sakkie/message`, `GET /oom-sakkie`, and Supabase trace migration `202606060001_create_oom_sakkie_traces.sql`. The migration was applied locally against Supabase; local smoke confirmed `hello` returns clarification, the three MVP questions route to `farm_attention_summary`, `power_current`, and `weather_today`, and traces store successfully. Full local unittest suite passed at 353 tests. Owner tested `/oom-sakkie` and confirmed it answered expected questions correctly. Telegram is unchanged.
- Phase 10.6B Oom Sakkie read-only tool expansion is owner-tested: added `dashboard_summary`, `pig_allocation_readiness`, `meat_planning`, and `sales_dashboard` to the typed registry and rule-first router. Direct local smoke with `.env` loaded confirmed `how is the farm`, `show me pig allocation`, `what pigs are ready for meat`, and `sales dashboard overview` route correctly and store traces. Meat planning surfaces the read-only/no-save warning. Owner tested the expanded questions and confirmed they answered as expected. Telegram is unchanged.
- Phase 10.6C Oom Sakkie trace visibility is local-ready: added `GET /api/oom-sakkie/traces`, Supabase trace readback, and a Recent Checks panel with refresh on `/oom-sakkie`. Local HTTP smoke returned recent trace rows, and the full local unittest suite passed at 356 tests. This supports daily review before any Telegram migration. Telegram is unchanged.
- Phase 10.6D Oom Sakkie trace feedback is local-ready: added Supabase migration `202606060002_create_oom_sakkie_trace_feedback.sql`, append-only feedback records linked to `oom_sakkie_traces`, `POST /api/oom-sakkie/traces/<trace_id>/feedback`, latest-feedback readback on trace lists, and compact review controls in the Recent Checks panel. Local HTTP smoke posted `correct` feedback to a real recent trace and confirmed `latest_feedback` returned. This records review metadata only; no farm-data writes, voice, Telegram, LLM router, or Sam/customer actions were added.
- Phase 10.6E Oom Sakkie review summary is local-ready: added `GET /api/oom-sakkie/traces/review-summary`, a 14-day kiosk summary strip for checks/reviewed/issues/unreviewed, and recent reviewed problem trace readback. Local HTTP smoke returned 20 kiosk traces, 1 reviewed trace, 0 problem traces, and per-tool counts. This is a quality signal for the kiosk trial only; Telegram remains unchanged.
- Phase 10.6F Oom Sakkie trace review filters are local-ready: `GET /api/oom-sakkie/traces` now accepts `review=all|unreviewed|issues|reviewed`, and `/oom-sakkie` has filter buttons above Recent Checks. Local HTTP smoke confirmed all filters return the expected reviewed/unreviewed/issue slices. This remains kiosk review support only; Telegram remains unchanged.
- Phase 10.6G Oom Sakkie trace detail expanders are local-ready: Recent Checks rows now include an expandable saved-answer view with the stored answer, tool-result summary, stale warnings, and links already returned by the trace API. This helps review traces without re-asking or opening Supabase. Telegram remains unchanged.
- Phase 10.6H Oom Sakkie trace search is local-ready: `GET /api/oom-sakkie/traces` now accepts bounded `q` search text across trace ID, user text, answer, and tool name, and `/oom-sakkie` has a debounced search box with Clear. Search combines with review filters. Local HTTP smoke confirmed `q=power` returned matching rows and a nonsense term returned zero rows. Telegram remains unchanged.
- Phase 10.6I Oom Sakkie voice readiness preflight is local-ready: `/oom-sakkie` now shows secure-origin, microphone API, and browser TTS capability checks without requesting microphone permission or opening audio capture. This prepares for later push-to-talk only; no STT/TTS vendor, wake word, Telegram, or voice loop was added.
- Phase 10.6J Oom Sakkie browser speech draft is owner-tested: `/oom-sakkie` now has a `Talk` button using browser `SpeechRecognition` / `webkitSpeechRecognition` when available. It drafts one spoken utterance into the text input and updates `You Asked`, but does not auto-submit; the owner must review/edit and press `Ask`. No backend STT vendor, TTS, wake word, always-on mic, Telegram change, or farm-data write was added.
- Phase 10.6K Oom Sakkie browser TTS playback is local-ready: `/oom-sakkie` now has `Speak Answer`, `Stop Speech`, and opt-in `Speak replies` controls using browser `speechSynthesis`. Playback uses `en-ZA`, stops any active speech-recognition capture first, cancels when a new question starts, and never restarts the mic automatically. No backend TTS vendor, wake word, always-on mic, Telegram change, or farm-data write was added.
- Phase 10.6L Oom Sakkie Talk & Ask correction window is owner-tested: `/oom-sakkie` now has an optional `Talk & Ask` path that captures one spoken question, fills the existing input, then waits 2 seconds before submitting. `Cancel Send` appears during that window, and editing the text cancels the pending send. The original tested `Talk` draft path remains unchanged. No always-on mic, wake word, backend STT vendor, Telegram change, LLM default router, or farm-data write was added.
- Phase 10.6M Oom Sakkie optional continue conversation is owner-tested: `/oom-sakkie` now has an off-by-default `Continue conversation` toggle. When enabled it also enables `Speak replies`; after an automatic spoken backend answer finishes, the browser starts the existing `Talk & Ask` flow again, including the 2-second cancel/edit window. Manual `Speak Answer` does not continue, and `Stop Speech`, a manual `Ask`, or a new voice capture cancels continuation for the current reply. No always-on mic, wake word, backend STT/TTS vendor, Telegram change, LLM default router, or farm-data write was added.
- Phase 10.6N Oom Sakkie voice loop stop/cap guard is owner-tested: `/oom-sakkie` now has `Stop Conversation`, visible while voice continuation/listening/speaking/pending-send behavior is active. It cancels pending auto-send, browser speech recognition, and browser TTS; turns off continuation; and resets loop state. The browser also pauses automatically after 5 continued turns. No always-on mic, wake word, backend STT/TTS vendor, Telegram change, LLM default router, or farm-data write was added.
- Phase 10.6O Oom Sakkie spoken stop commands are local-ready: during the `Talk & Ask` auto-submit path, browser-only stop phrases such as `stop conversation`, `cancel`, and `never mind` call the existing `Stop Conversation` behavior and are not submitted to `/api/oom-sakkie/message`. A small kiosk hint lists practical stop words. No always-on mic, wake word, backend STT/TTS vendor, Telegram change, LLM default router, or farm-data write was added.
- Phase 10.6P Oom Sakkie local voice session log is local-ready: `/oom-sakkie` now has a `Voice Session` panel that records the latest 12 browser-local voice events, including listening, transcript, auto-send, backend answered, speaking, continuation, stop phrase, loop pause, and speech error events. It has a local Clear action and explicitly states that nothing in the panel is sent to Supabase. No backend endpoint, Supabase table, Telegram change, backend STT/TTS vendor, wake word, always-on mic, LLM default router, or farm-data write was added.
- Phase 10.6Q Oom Sakkie quick read-only checks are local-ready: `/oom-sakkie` now has quick buttons for Attention, Power, Weather Today, Meat Ready, and Sales. Each button sends a fixed safe question through the existing `ask()` path and `/api/oom-sakkie/message`, with the same trace, stale-warning, link, and review-refresh behavior as typed questions. No new backend tool, Telegram change, backend STT/TTS vendor, wake word, always-on mic, LLM default router, or farm-data write beyond existing trace inserts was added.
- Phase 10.6R Oom Sakkie read-only telemetry tool expansion is local-ready: added typed tools `power_recent`, `weather_now`, `weather_forecast`, and `irrigation_status`, plus deterministic routing for recent power, current weather, forecast, and irrigation/watering status questions. Control-style irrigation wording such as `start irrigation` routes to read-only status and warns that no start/stop command was sent. The kiosk quick checks now include Irrigation. No new backend endpoint, write tool, hardware command, Telegram change, backend STT/TTS vendor, wake word, always-on mic, LLM default router, or farm-data write beyond existing trace inserts was added.
- Phase 10.6S Oom Sakkie tool catalog transparency is local-ready: `/api/oom-sakkie/tools` now exposes the runtime typed registry with tool names, descriptions, risk levels, confirmation flags, and schemas, and the kiosk renders an `Available Checks` panel from that endpoint. This is read-only registry visibility only; it does not execute tools, write traces, change Telegram, add backend STT/TTS, add wake word, add always-on mic, add an LLM router, or add any farm-data write.
- Phase 10.6T Oom Sakkie unsupported action guard is local-ready: `/api/oom-sakkie/message` now detects unsupported write/control/message-style requests that do not match an approved read-only tool and returns a read-only refusal with `action_blocked = true`, risk `0`, and a warning that no write/control/message/physical action was performed. Approved read-only rules still win first, so `start irrigation` continues to route to read-only irrigation status instead of performing control. No Telegram change, backend STT/TTS vendor, wake word, always-on mic, LLM router, write tool, physical control, or farm-data write beyond existing trace inserts was added.
- Phase 10.6U Oom Sakkie browser-local session ID is local-ready: the kiosk now creates and reuses a `kiosk-*` session ID in browser `localStorage` and sends it with `/api/oom-sakkie/message` so traces can be grouped by the same local kiosk browser profile. This is only trace grouping; it is not authentication, not user identity, and not multi-turn memory. No Telegram change, backend STT/TTS vendor, wake word, always-on mic, LLM router, write tool, physical control, or farm-data write beyond existing trace inserts was added.
- Phase 10.6V Oom Sakkie runtime policy visibility is local-ready: `/api/oom-sakkie/policy` now exposes the current local kiosk guardrails, including backend-as-brain enabled, Telegram cutover disabled, LLM router disabled, write tools disabled, physical controls disabled, backend voice vendors disabled, always-on mic disabled, browser speech as push-to-talk only, kiosk max risk `0`, and read-only tool counts. The kiosk renders this in a `Safety Status` panel. This is visibility only; it does not execute tools, write traces, change Telegram, add backend STT/TTS, add wake word, add always-on mic, add an LLM router, or add any farm-data write.
- Phase 10.6W Oom Sakkie review packet is local-ready: `/api/oom-sakkie/review-packet` now bundles the runtime policy, tool catalog, 14-day review summary, and recent kiosk traces into one read-only JSON payload for morning Claude/human review. The kiosk has a `Review Packet` link that opens the packet in a new tab. This endpoint executes no tools and writes no traces; it only reuses existing read-only registry, policy, and trace-read functions.
- Phase 10.6X Oom Sakkie Claude-review hardening is local-ready: user text is now capped to 2,000 chars before trace writing; tool outputs and responses split `stale_warnings` from `safety_notes`; Supabase migration `202606060003_add_oom_sakkie_safety_notes.sql` adds `safety_notes_json` to traces and has been applied; mixed action/read requests such as `send weather to John` still answer the read-only part but add a safety note that no action was performed; `/api/oom-sakkie/policy` now derives confirmation-required tools and exposes `continue_conversation_max_turns = 5` plus `voice_auto_send_ms = 2000`; route tests cover `/api/oom-sakkie/message` without requiring DB config. No Telegram change, backend STT/TTS vendor, wake word, always-on mic, LLM router, write tool, physical control, or farm-data write beyond trace inserts was added.
- Phase 10.6Y Oom Sakkie review endpoint local-access guard is local-ready: sensitive review/admin endpoints (`/api/oom-sakkie/tools`, `/policy`, `/review-packet`, `/traces`, `/traces/review-summary`, and trace feedback) now require loopback access by default. Private LAN review access is denied unless `OOM_SAKKIE_REVIEW_ALLOW_PRIVATE_LAN=true` is explicitly set. `/api/oom-sakkie/message` is unchanged. `/api/oom-sakkie/policy` exposes the review access mode and the Safety Status panel shows it. No Telegram change, backend STT/TTS vendor, wake word, always-on mic, LLM router, write tool, physical control, or farm-data write beyond trace inserts was added.
- Phase 10.6Z Oom Sakkie trace append-only DB guard is local-ready: Supabase migration `202606060004_lock_oom_sakkie_trace_append_only.sql` adds triggers that block `UPDATE` and `DELETE` on `oom_sakkie_traces` and `oom_sakkie_trace_feedback`, preserving insert-only trace and feedback behavior at the database layer. The migration has been applied locally, and a live-style smoke confirmed new trace inserts still store successfully. No Telegram change, backend STT/TTS vendor, wake word, always-on mic, LLM router, write tool, physical control, or farm-data write beyond trace inserts was added.
- Oom Sakkie future specialist-agent roster planning has moved from scratch notes into `docs/01-architecture/OOM_SAKKIE_AGENT_ROSTER.md`. The planned crew is Oom Sakkie as the user-facing farm brain, plus Sentinel, Forge, Prism, Ledger, Atlas, Rootline, Herdmaster, Butcher, Beacon, Quartermaster, and Gatekeeper as future specialist roles. This is planning only: no live specialists, delegation loop, autonomous worker, write tool, or new user-facing brain has been built.
- Phase 10.7A Oom Sakkie specialist manifest scaffolding is local-ready: added typed planned-only specialist manifests in `modules/oom_sakkie/specialists.py`, loopback-protected `GET /api/oom-sakkie/specialists`, and included the planned roster in `/api/oom-sakkie/review-packet`. The route returns `delegation_enabled = false` and `autonomous_loops_enabled = false`; no live delegation, LLM worker, autonomous loop, write tool, Telegram change, or second user-facing brain was added.
- Phase 10.7B Oom Sakkie trace review advisor is local-ready: added `modules/oom_sakkie/review_advisor.py`, loopback-protected `GET /api/oom-sakkie/review-advisor`, and a `Review Advisor` kiosk link. It reads trace summaries, reviewed issue traces, and unreviewed traces to return an advisory `review_queue` plus `suggested_actions`. It explicitly returns `mode = advisory_only`, `autonomous_marking_enabled = false`, and `writes_feedback = false`; it never marks traces, calls a model, executes tools, changes Telegram, creates a specialist loop, or writes farm data.
- Phase 10.7C Oom Sakkie review-surface caveat hardening is local-ready after Claude's pass-with-nits review: `/api/oom-sakkie/policy` now explicitly separates `message_endpoint_access` from `review_endpoints_access`, documents the reverse-proxy remote-address assumption, and the kiosk Safety Status shows `Message access`. The PRD documents that reverse proxies require trusted proxy handling before loopback protection is trusted, and that trace corrections must be append-only feedback/superseding records rather than edits/deletes. Tests now cover empty remote addresses, ignored `X-Forwarded-For`, message endpoint policy fields, specialist manifest invariants, and a DATABASE_URL-gated append-only trigger integration check.
- Phase 10.7D Oom Sakkie kiosk review advisor panel is local-ready: `/oom-sakkie` now includes a user-action-triggered `Review Advisor` panel under the review summary. It fetches the advisory endpoint on page load, after kiosk questions, with Recent Checks refresh, and from its own refresh button. It shows guard state, suggested actions, and prioritized queue items using text-only DOM rendering. It does not auto-poll, auto-mark feedback, call a model, execute tools, change Telegram, create a specialist loop, or write farm data.
- Phase 10.7E Oom Sakkie advisor wording and proxy-test tightening is local-ready: docs now describe the advisor cadence as `user-action-triggered, no auto-polling` instead of `manual refresh only`; route tests now cover the inverse forwarded-header case (`REMOTE_ADDR` public, `X-Forwarded-For` loopback) and still deny review access; frontend contracts now pin that the advisor guard line displays `data.mode`, `data.autonomous_marking_enabled`, and `data.writes_feedback`.
- Phase 10.7F Oom Sakkie advisor trace read consolidation is local-ready: added `list_review_advisor_traces()` to read reviewed issue traces and unreviewed traces in one combined ranked query, and updated the review advisor to use it while preserving the public response shape. This reduces advisor trace-list DB calls without changing safety posture; no auto-polling, auto-marking, model call, tool execution, Telegram change, write tool, physical control, or second user-facing brain was added.
- Phase 10.7G Oom Sakkie advisor SQL window and test hardening is local-ready after Claude's pass-with-nits review: `list_review_advisor_traces()` now accepts `days` defaulting to `14`, filters the advisor trace CTE by `created_at`, and route tests prove `days` is passed through. Tests now also guard `_trace_row` positional mapping and capture executed advisor SQL through a mocked `psycopg.connect` instead of inspecting compiled string constants.
- Phase 10.7H Oom Sakkie kiosk advisor window and voice-loop counter is local-ready after Claude's pass review: the Review Advisor guard line now displays `last 14 days`, and the voice status row shows a `Voice loop 0 of 5` counter only when Continue Conversation is enabled. The counter updates during continued spoken turns and hides when continuation is stopped or disabled. This is UI honesty only; no endpoint, routing, tool, voice-vendor, Telegram, write, physical-control, wake-word, always-on mic, model, or specialist-delegation surface was added.
- Phase 10.7I Oom Sakkie trace-driven router and power answer tightening is local-ready after owner testing and trace review: natural phrasings for slaughter, sales issues, water/pump checks, worry/anything-today, and broad animal/pig-on-farm checks now route deterministically to the existing read-only tools; `what can you do` returns a current read-only capability answer; separated control phrases like `turn the pump on` are detected; and current power wording now includes battery state, grid watts/state, and data age. This uses only existing read-only tools and deterministic rules; no LLM router, endpoint expansion, write, physical-control, Telegram, backend voice-vendor, wake-word, always-on mic, second brain, or live specialist-delegation surface was added. Full local unittest suite passed at 390 tests.
- Phase 10.7J Oom Sakkie capability fallback precedence fix is local-ready after Claude's pass-with-nits review: capabilities/help detection now runs after deterministic tool classification and after unsupported-action blocking, so `help me with the weather`, `I need help with the power`, `can you help me check irrigation`, and `help me understand sales` route to their read-only domain tools instead of the generic capability answer. Bare `what can you do` still returns the capability answer. Full local unittest suite passed at 390 tests.
- Phase 10.7K Oom Sakkie bounded LLM fallback router is local-ready but disabled by default: added an env-gated OpenAI-compatible classifier that runs only after deterministic rules, unsupported-action blocking, and capability fallback have declined. It can only select an existing read-only tool or ask for clarification; unknown/write tool names are rejected, unsupported actions do not call the LLM, and policy exposes `can_write = false`. No write tool, physical control, Telegram cutover, backend voice-vendor, wake-word, always-on mic, live specialist delegation, autonomous loop, or second user-facing brain was added. Full local unittest suite passed at 395 tests.
- Phase 10.7L Oom Sakkie LLM fallback privacy and failure-mode hardening is local-ready: the runtime policy and kiosk Safety Status now declare the outbound LLM endpoint and that user text would be sent when the router is enabled; tests now pin env-gating, no-network-call behavior when disabled, network failure fail-closed behavior, invalid JSON fail-closed behavior, low-confidence LLM clarification behavior, and frontend display of LLM privacy fields. The router remains disabled by default. Full local unittest suite passed at 399 tests.
- Phase 10.7M Oom Sakkie LLM router smoke harness is local-ready: added `scripts/oom_sakkie_llm_router_smoke.py`, which loads `.env`, prints router/privacy status without secrets, refuses to call the network unless router env is fully configured, and when configured sends a fixed smoke prompt set through `handle_message()` using channel `kiosk_llm_smoke`.
- Phase 10.7N Oom Sakkie LLM router local smoke is verified: owner enabled the router with a valid `OPENAI_API_KEY` and `OOM_SAKKIE_LLM_ROUTER_MODEL=gpt-5.4-mini`. Direct diagnostic returned HTTP 200 with model `gpt-5.4-mini-2026-03-17`. Smoke prompts routed safely: `give me the energy situation` -> `power_current`, `check if the outside conditions are a problem` -> `weather_now`, `which farm area should I inspect first` -> `farm_attention_summary`; `delete a pig record` stayed action-blocked before tool execution; `what can you do` stayed on the local capability answer. Focused Oom Sakkie tests passed at 74 tests, full local unittest suite passed at 399 tests, and `node --check static/js/oomSakkie.js` passed. The router is now proven as a bounded read-only fallback, not a primary brain; continue using it locally and review `kiosk_llm_smoke`/kiosk traces before any wider channel rollout.
- Phase 10.7O Oom Sakkie LLM answer composer is local-ready: added `modules/oom_sakkie/llm_answer.py` behind independent env gate `OOM_SAKKIE_LLM_ANSWER_ENABLED`. The composer runs only after a read-only tool returns a deterministic answer, cannot choose tools or actions, and falls back to deterministic wording if disabled, unconfigured, failed, invalid, or unsafe. Local smoke with the composer enabled showed visible improvement in wording, especially turning the raw farm-attention list into an operator-style inspection answer. Full local unittest suite passed at 402 tests and `node --check static/js/oomSakkie.js` passed. To see the smarter wording in the browser, add `OOM_SAKKIE_LLM_ANSWER_ENABLED=true` to local `.env`.
- Usejarvis external source was inspected read-only from `external_sources/jarvis-main/jarvis-main`; review saved at `docs/01-architecture/JARVIS_EXTERNAL_REFERENCE_REVIEW.md`. Decision: do not install/run/copy it. Borrow state/provenance/authority/approval ideas only.
- Phase 10.7P Oom Sakkie alive state and provenance strip is local-ready: `/api/oom-sakkie/message` now returns `pipeline` metadata (`route_source`, `answer_source`, `state`, LLM flags, and tool-checked flag). The kiosk now shows Route / Answer / State, intent confidence, and reason, and the top status pill visually changes for listening/checking/speaking/answered/blocked/error. This is visibility only; no new tools or autonomy were added. Full local unittest suite passed at 402 tests and `node --check static/js/oomSakkie.js` passed.
- Phase 10.7Q Oom Sakkie presence and stronger spoken voice is local-ready: the answer composer now prompts Oom Sakkie as a farm operating co-pilot, not a generic answer reader, leads with operational meaning, avoids generic assistant openers, and keeps the hard fact boundary to backend answer/stale/safety inputs only. The kiosk now has a central animated agent-presence orb with ring/core/scan layers and state-specific idle/listening/checking/answered/speaking/blocked/error behavior wired to the existing status state machine. This is presentation/voice only; no write path, tool authority, physical control, wake word, always-on mic, Telegram cutover, or live specialist delegation was added. Focused service/frontend tests passed at 68 tests with 1 expected skip, and `node --check static/js/oomSakkie.js` passed.
- Phase 10.7R Oom Sakkie capped context briefing composer is local-ready: the answer composer now receives a capped 3000-character JSON excerpt of the read-only tool result context after tool execution, so it can prioritize and brief from structured data instead of merely rephrasing the deterministic summary. Runtime policy and kiosk Safety Status disclose that the enabled answer composer sends capped tool context outbound. Local smoke showed `which farm area should I inspect first?` identifying the litter area and most urgent litter from structured context while keeping `answer_source = llm_composer`. Focused service/frontend tests passed at 68 tests with 1 expected skip, and `node --check static/js/oomSakkie.js` passed.
- Phase 10.7S Oom Sakkie read-only operating brief tool is local-ready: added `farm_operating_brief`, a composite read-only tool that calls existing attention, current power, today's weather, and irrigation-status wrappers in one user-initiated turn. Deterministic routing and LLM-router guidance now send broad brief/status-report prompts to this tool, and the kiosk has a `Brief` quick action. The answer composer is instructed to keep operating briefs to at most three short sentences. Local smoke showed `give me the farm operating brief`, `bring me up to speed`, and `what should i know before i go outside` using `tool = farm_operating_brief` with `answer_source = llm_composer`. Focused service/frontend tests passed at 69 tests with 1 expected skip, and `node --check static/js/oomSakkie.js` passed.
- Phase 10.7T Oom Sakkie operating brief required sections is local-ready: `farm_operating_brief` now returns compact `llm_context` with required sections `attention`, `power`, `weather`, and `irrigation`; `handle_message()` prefers that compact context over verbose raw payload for the answer composer. The composer prompt now explicitly requires all four sections for operating briefs, and the unsafe-output filter now allows negated safety wording such as `No start or stop command was sent` while still rejecting positive action claims. Focused service/frontend tests passed at 70 tests with 1 expected skip, and `node --check static/js/oomSakkie.js` passed. A direct live diagnostic hit Google Sheets quota 429, so avoid repeated live brief smokes until quota cools down.
- Phase 10.7U Oom Sakkie top voice controls is local-ready: moved `Talk` and `Talk & Ask` from the bottom input form into the top presence panel so the owner does not need to scroll to speak. The text input and `Ask` button remain in the bottom form. Added touch-friendly `.oom-presence-actions` styling and frontend contract coverage that the voice buttons stay inside the presence panel, not the bottom form. This is placement only; mic behavior remains explicit button press only.
- Phase 10.7V Oom Sakkie human-approved Learning Queue is local-ready: added deterministic advisory-only `modules/oom_sakkie/learning_advisor.py`, protected `GET /api/oom-sakkie/learning-advisor`, and kiosk `Learning Queue` panel. It turns reviewed trace feedback into improvement proposals such as routing review, data freshness review, answer wording review, or tool gap review. It does not call an LLM, write code, write feedback, edit prompts, change tools, or touch farm data; human approval is required for any follow-up implementation. Focused Oom Sakkie service/routes/frontend tests passed at 83 tests and `node --check static/js/oomSakkie.js` passed.
- Phase 10.7W Oom Sakkie explicit LLM Learning Analyst is local-ready: added `modules/oom_sakkie/learning_llm.py`, env gate `OOM_SAKKIE_LLM_LEARNING_ENABLED`, protected `POST /api/oom-sakkie/learning-advisor/analyze`, and kiosk `Analyze` button. The deterministic Learning Queue remains the default GET path; the LLM analyst only runs on explicit button press, sends capped reviewed issue trace excerpts/proposals to the configured model, validates proposal kinds, forces `approval_required = true`, and cannot write code, feedback, prompts, tools, routes, or farm data. Focused Oom Sakkie service/routes/frontend tests passed at 87 tests and `node --check static/js/oomSakkie.js` passed.
- Phase 10.7X Oom Sakkie trace-driven composer lane guard is local-ready: reviewed kiosk traces showed single-tool answers adding cross-system disclaimers such as `Power and weather were not evaluated here`. The answer-composer prompt now tells non-brief tools to stay in their lane and avoid unrelated systems/no-warning filler. A post-composer guard rejects off-topic single-tool disclaimer fragments, including smart-apostrophe variants such as `weren’t evaluated`, and falls back to deterministic wording. `farm_operating_brief` is exempt because it intentionally covers multiple systems. Focused Oom Sakkie service/routes/frontend tests passed at 88 tests and `node --check static/js/oomSakkie.js` passed.
- Phase 10.7Y Oom Sakkie Learning Build Brief Packet is local-ready: added deterministic `modules/oom_sakkie/learning_packet.py`, protected `POST /api/oom-sakkie/learning-advisor/build-packet`, `Build Brief` buttons on Learning Queue proposals, and a text-only kiosk packet panel. This turns one reviewed learning proposal into a scoped implementation/review brief with files to inspect, verification commands, out-of-scope constraints, and a human-approval rule. It does not call an LLM, write code, apply changes, mutate prompts/tools, write feedback, or touch farm data. Focused Oom Sakkie service/routes/frontend tests passed at 92 tests, `node --check static/js/oomSakkie.js` passed, and full local unittest suite passed at 417 tests.
- Phase 10.7Z Oom Sakkie Human-Approved Implementation Queue is local-ready: added a deterministic auto-prepared implementation queue that builds in-memory review briefs from strong Learning Queue signals only. It exposes protected `GET /api/oom-sakkie/learning-advisor/implementation-queue`, adds a kiosk `Implementation Queue` panel, and lets the owner open prepared briefs without approving or applying anything. Auto-prepare is thresholded to high-priority proposals, repeated tool patterns, or evidence with two or more issue traces. It does not call an LLM, write code, apply changes, mutate prompts/tools, write feedback, or touch farm data. Focused Oom Sakkie service/routes/frontend tests passed at 95 tests, `node --check static/js/oomSakkie.js` passed, and full local unittest suite passed at 420 tests.
- Phase 10.8A Oom Sakkie Approve For Build Gate is local-ready: added `approve_build_request()`, protected `POST /api/oom-sakkie/learning-advisor/approve-build`, and an `Approve for Build` button on generated build briefs. Approval creates a structured `build_request_only` object with `builder_enabled = false`, `writes_code_now = false`, `applies_changes_now = false`, and `requires_next_gate = builder_agent_review_and_patch_approval`. It does not run a builder, edit files, apply patches, deploy, mutate prompts/tools, write feedback, or touch farm data. Focused Oom Sakkie service/routes/frontend tests passed at 99 tests, `node --check static/js/oomSakkie.js` passed, and full local unittest suite passed at 424 tests.
- Phase 10.8B Oom Sakkie Persistent Build Request Queue is local-ready: added `modules/oom_sakkie/build_request_store.py`, Supabase migration `202606070001_create_oom_sakkie_build_requests.sql`, protected `GET /api/oom-sakkie/build-requests`, and kiosk `Approved Build Requests` panel. `approve-build` now attempts to persist the approved build request. The table is append-only, blocks update/delete, and has constraints that keep `builder_enabled`, `writes_code_now`, and `applies_changes_now` false. Persistence records approval only; it does not run a builder, edit files, apply patches, deploy, mutate prompts/tools, write feedback, or touch farm data. Focused Oom Sakkie service/routes/frontend tests passed at 105 tests, `node --check static/js/oomSakkie.js` passed, and full local unittest suite passed at 430 tests.
- Phase 10.8C Oom Sakkie Build Request Event Log is local-ready: added migration `202606070002_create_oom_sakkie_build_request_events.sql`, append-only build request events, latest-event readback on build request history, and a kiosk `Ignore` event action. This gives append-only correction/review state for approved build requests, including smoke records, without editing or deleting the original request. Allowed event types are `approved`, `ignored`, and `review_note`. It does not run a builder, edit files, apply patches, deploy, mutate prompts/tools, write feedback, or touch farm data. Migration `202606070002_create_oom_sakkie_build_request_events.sql` has been applied. Synthetic persistence smoke request `OSK-BUILD-7073A29F701E` was marked `ignored` through the append-only event route. Focused Oom Sakkie service/routes/frontend tests passed at 109 tests, `node --check static/js/oomSakkie.js` passed, and full local unittest suite passed at 434 tests.
- Phase 10.8D Oom Sakkie Forge Handoff Packet is local-ready: added `modules/oom_sakkie/forge_handoff.py`, protected `POST /api/oom-sakkie/build-requests/forge-handoff`, and kiosk `Forge Handoff` button/panel on approved build requests. It creates the exact no-execution packet a future Builder/Forge agent would consume, including objective, evidence, approved scope, verification commands, no-go rules, original brief, and required pre-patch output. It does not run a builder, edit files, apply patches, deploy, mutate prompts/tools, or touch farm data. This is the intended Claude review gate before any actual Builder/Forge execution is added. Focused Oom Sakkie service/routes/frontend tests passed at 113 tests, `node --check static/js/oomSakkie.js` passed, and full local unittest suite passed at 438 tests.
- Phase 10.8E Oom Sakkie Forge Handoff Persisted-ID Hardening is local-ready after Claude's review: added `get_build_request()`, changed `POST /api/oom-sakkie/build-requests/forge-handoff` to accept `build_request_id` only, and updated the kiosk to send only the ID. Forge handoff generation now loads the persisted approved build request before building the packet, closing the synthetic-payload gap. It still does not run a builder, edit files, apply patches, deploy, mutate prompts/tools, or touch farm data. Focused Oom Sakkie service/routes/frontend tests passed at 114 tests, `node --check static/js/oomSakkie.js` passed, and full local unittest suite passed at 439 tests.
- Phase 10.8F Oom Sakkie Patch Proposal Gate is local-ready before the first real Builder run: added `modules/oom_sakkie/patch_proposal_store.py`, Supabase migration `202606070003_create_oom_sakkie_patch_proposals.sql`, protected routes for recording/listing patch proposals and recording patch review events, and a kiosk `Patch Proposal Gate` panel. The owner can paste Builder/Forge output into an append-only proposal row, then record `approved_for_patch`, `rejected`, or `review_note` events. DB constraints and application code keep `applies_patch = false` and `deploys = false`; this gate records review only and does not apply patches, edit files, run subprocesses, deploy, mutate prompts/tools, or touch farm data. Migration `202606070003_create_oom_sakkie_patch_proposals.sql` has been applied. Focused Oom Sakkie service/routes/frontend tests passed at 124 tests, `node --check static/js/oomSakkie.js` passed, and full local unittest suite passed at 449 tests.
- Phase 10.8G Oom Sakkie Patch Gate Review Nits is local-ready after Claude's pass review: updated the Forge Handoff non-local-access test to use the current `build_request_id` payload, confirmed the frontend contract pins the Review Advisor `last 14 days` guard string, and added `get_patch_proposal()` so patch proposal event writes return clean `404 patch_proposal_not_found` before insert when the target proposal does not exist. This remains review-only and does not apply patches, edit files, run subprocesses, deploy, mutate prompts/tools, or touch farm data. Focused Oom Sakkie service/routes/frontend tests passed at 125 tests, `node --check static/js/oomSakkie.js` passed, and full local unittest suite passed at 450 tests.
- Phase 10.8H Oom Sakkie Deploy Approval Gate is local-ready: added `modules/oom_sakkie/deploy_decision_store.py`, Supabase migration `202606070004_create_oom_sakkie_deploy_decisions.sql`, protected routes for recording/listing deploy decisions, and a kiosk `Deploy Approval Gate` panel. Manual deploy approval can only be recorded after the patch proposal's latest event is `approved_for_patch`; otherwise it returns `409 patch_not_approved`. DB constraints and application code keep `runs_deploy = false` and `deploys_now = false`; this gate records approval only and does not deploy, edit files, run subprocesses, mutate prompts/tools, or touch farm data. Migration `202606070004_create_oom_sakkie_deploy_decisions.sql` has been applied. Focused Oom Sakkie service/routes/frontend tests passed at 135 tests, `node --check static/js/oomSakkie.js` passed, and full local unittest suite passed at 460 tests.
- Phase 10.8I Oom Sakkie Workbench Simplification And Work Status Tool is local-ready: collapsed the busy trace/build/patch/deploy controls under a first-viewport `System Workbench` disclosure, added a `Copy Forge Prompt` button so the owner does not manually select prompt text, added an `Approvals` quick action, and added read-only `system_work_status` so Oom Sakkie can answer questions such as `what needs my approval?` from the existing build request, patch proposal, and deploy decision queues. This is status-only: it does not run a builder, copy to external tools automatically, edit files, apply patches, deploy, mutate prompts/tools, or touch farm data. Focused Oom Sakkie service/routes/frontend tests passed at 136 tests, `node --check static/js/oomSakkie.js` passed, and full local unittest suite passed at 461 tests.
- Phase 10.8J Oom Sakkie Workbench Pipeline Clarity is local-ready: the Approved Build Requests panel now splits build work into `Needs Forge Handoff / Builder Plan` and `Already Moved Or Closed`, and patch proposals now split into `Needs Patch Review`, `Approved - Ready For Deploy Decision`, and `Rejected / Closed`. Recording a patch proposal adds a build-request review note so the source build item moves out of the active handoff bucket, and approving/rejecting/deploy-decision actions refresh the adjacent queues so items do not appear stuck in the old step. Added a `Use This Build Request In Patch Gate` handoff bridge and visual work-stage badges/actions. This is UI flow and append-only event logging only; it does not run Builder/Forge, edit files, apply patches, deploy, mutate prompts/tools, or touch farm data. Focused Oom Sakkie service/routes/frontend tests passed at 136 tests, `node --check static/js/oomSakkie.js` passed, and full local unittest suite passed at 461 tests.
- Phase 10.8K Oom Sakkie Deploy Instructions And Business Advisor Seed is local-ready: approved patch rows now show a `What this needs now` instruction card explaining manual patch review/application, verification, and deploy-decision recording; the deploy verification placeholder now asks for exactly what was verified. `system_work_status` now ignores build requests that already moved to Patch Proposal Gate and counts only approved patch proposals without a deploy decision as deploy-ready. Added read-only `business_growth_brief`, a first Business Advisor seed that combines sales stock and meat-planning signals into a commercial focus such as what to sell/promote next. This tool is advice-only and does not draft, post, message customers, sell, reserve stock, edit data, run Builder/Forge, apply patches, or deploy. Focused Oom Sakkie service/routes/frontend tests passed at 138 tests, `node --check static/js/oomSakkie.js` passed, and full local unittest suite passed at 463 tests.
- Phase 10.8L Oom Sakkie Workbench Next Action And Visual Separation is local-ready: added a `Next action` card at the top of the System Workbench that summarizes active build handoffs, patch reviews, and deploy decisions, then states the next record to handle. Workbench sections and records now have stronger visual separation with section cards, active-row borders, badges, and count pills so scrolling is less blended. Added a `Business` quick action for `What should we sell next?`. This is UI guidance only; it does not run Builder/Forge, edit files, apply patches, deploy, mutate prompts/tools, or touch farm data. Focused Oom Sakkie service/routes/frontend tests passed at 138 tests, `node --check static/js/oomSakkie.js` passed, and full local unittest suite passed at 463 tests.
- Phase 10.8M Oom Sakkie Work Status Honesty is local-ready after Claude's 10.8G-L pass: `system_work_status` now surfaces stale warnings when any build/patch/deploy store is unavailable, returns `not_configured` or `degraded` instead of confidently saying there is no work, and still reports best-effort counts from stores it could read. `record_deploy_decision()` now checks the local `psycopg` dependency before loading the patch proposal, so dependency-missing errors come from the top-level deploy-decision store. This remains read-only/status-only and does not run Builder/Forge, edit files, apply patches, deploy, mutate prompts/tools, or touch farm data. Focused Oom Sakkie service/routes/frontend tests passed at 139 tests, `node --check static/js/oomSakkie.js` passed, and full local unittest suite passed at 464 tests.
- Phase 10.8N Oom Sakkie Business Advisor Context Upgrade is local-ready: `business_growth_brief` now separates total listed stock from marketable stock and young/not-ready stock, names ready meat candidates by tag/pen/weight, includes category breakdowns, and returns an owner-facing follow-up question in `llm_context`. Live read-only smoke showed 21 marketable pigs, 34 young/not-ready pigs, and two ready D1 meat candidates: tag 2 at 58.8 kg and tag 3 at 57.4 kg. This remains advice-only and does not draft offers, post, message customers, sell, reserve stock, mutate farm data, run Builder/Forge, apply patches, or deploy. Focused Oom Sakkie service/routes/frontend tests passed at 139 tests, `node --check static/js/oomSakkie.js` passed, and full local unittest suite passed at 464 tests.
- Phase 10.8O Oom Sakkie Role-Specific Spoken Composer Rules is local-ready: the env-gated answer composer now has explicit instructions for `business_growth_brief` to speak like a business advisor, lead with the commercial move, name the stock or ready pigs behind it, and ask exactly one approval-style follow-up question from `backend_context.owner_question` when present. It also has explicit `system_work_status` instructions to state the next owner action first before counts. This improves human-like delivery only when `OOM_SAKKIE_LLM_ANSWER_ENABLED` is enabled; the composer still cannot choose tools, call tools, write records, send messages, sell, post, apply patches, or deploy. Focused Oom Sakkie service/routes/frontend tests passed at 139 tests, `node --check static/js/oomSakkie.js` passed, and full local unittest suite passed at 464 tests.
- Phase 10.8P Oom Sakkie Business Offer Outline is local-ready: `business_growth_brief` now returns `llm_context.offer_brief_outline` as an internal-only commercial outline from existing read-only sales/meat-planning facts. It can name the opportunity, target buyer type, ready-stock basis, and approval-needed next step, while explicitly recording that no customer message, public post, quote, sale, reservation, or stock change was made. Routing now sends `offer brief` / `commercial brief` / `prepare ... offer` phrasing to the same read-only Business Advisor tool, and the kiosk now has an `Offer Brief` quick action using `Prepare an internal offer brief.` The answer-composer prompt now says Business Advisor may summarize this as an internal offer brief outline only, not customer-facing copy. This is a step toward a useful business co-pilot without adding customer messaging, public posting, quotes, stock changes, write tools, Builder/Forge execution, patch application, or deploy. Focused Oom Sakkie service/routes/frontend tests passed at 139 tests, `node --check static/js/oomSakkie.js` passed, and full local unittest suite passed at 464 tests.
- Phase 10.9A Oom Sakkie Agent Runtime Foundation is local-ready: added `modules/oom_sakkie/agent_runtime.py`, a read-only `AgentRuntimeManifest` layer over the planned specialist crew, protected `GET /api/oom-sakkie/agents`, protected `POST /api/oom-sakkie/agents/recommend`, review-packet inclusion, and a kiosk `Agent Crew Foundation` panel. Each planned agent now has personality, role, memory sources, allowed tools, risk limit, output contract, approval rules, and routing hints. The recommendation endpoint only says which planned agent would handle a prompt; it returns `runs_agent = false`, `dispatch_enabled = false`, `autonomous_loops_enabled = false`, and `writes = false`. This is the platform base for future Jarvis-style agent collaboration, but it does not run live specialists, call specialist LLMs, execute tools through dispatch, create writes, message/post/sell, run Builder/Forge, apply patches, or deploy. Focused Oom Sakkie service/routes/frontend tests passed at 145 tests, `node --check static/js/oomSakkie.js` passed, and full local unittest suite passed at 470 tests.
- Phase 10.9B Oom Sakkie Agent Crew Status Tool is local-ready: added read-only `agent_crew_status`, deterministic routing for agent/crew/specialist questions, and an `Agents` quick action. Oom Sakkie can now answer which planned agent would handle a question and why, using the Phase 10.9A recommendation-only runtime. The answer explicitly says no specialist was dispatched, no specialist tool ran, and no write was performed. `handle_message()` now passes bounded user text into tool handlers as context; existing read-only handlers ignore it. This remains recommendation-only and does not run live specialists, call specialist LLMs, execute specialist tools, write data, message/post/sell, run Builder/Forge, apply patches, or deploy. Focused Oom Sakkie service/routes/frontend tests passed at 147 tests, `node --check static/js/oomSakkie.js` passed, and full local unittest suite passed at 472 tests.
- Phase 10.9C Oom Sakkie Agent Activity Stage is local-ready: the owner-provided visual reference is saved at `screenshots/Jarvis screen layout.mp4`; the design intent is Oom Sakkie as controller with specialist workspaces opening visibly and colors shifting by active agent. Local video frame extraction was not available in this environment, so the phase is built from the owner's described target. Successful `/api/oom-sakkie/message` tool responses now include `agent_activity`, a visual-only payload naming the controller, active planned agent, workspace, route reason, and hard false safety flags for live agent execution/dispatch/autonomous loops/writes. The kiosk now shows a first-viewport Agent Activity Stage below the presence orb; the workspace and orb border color change by selected specialist. This remains display-only and does not run live specialists, call specialist LLMs, execute specialist tools, write data, message/post/sell, run Builder/Forge, apply patches, or deploy. Focused Oom Sakkie service/routes/frontend tests passed at 149 tests, `node --check static/js/oomSakkie.js` passed, and full local unittest suite passed at 474 tests.
- Phase 10.9D Oom Sakkie Agent Handoff Lane is local-ready: `agent_activity` now includes a four-step visible lane for each successful read-only tool answer: controller received the request, specialist workspace opened, read-only backend tool supplied facts, and owner approval remains the gate for write/post/sale/control/patch/deploy. The kiosk renders the lane as process cards with DOM nodes/textContent and responsive CSS. This remains visual-only and does not run live specialists, call specialist LLMs, execute specialist tools, write data, message/post/sell, run Builder/Forge, apply patches, or deploy. Focused Oom Sakkie service/routes/frontend tests passed at 149 tests, `node --check static/js/oomSakkie.js` passed, and full local unittest suite passed at 474 tests.
- Phase 10.9E Oom Sakkie Agent Crew Brief is local-ready: added `build_agent_crew_brief(text)`, read-only `agent_crew_brief`, deterministic team-plan routing, and a `Team Brief` quick action. Oom Sakkie can now explain which planned specialists would work together on a broad request and what each would inspect. The output is `mode = crew_plan_only` with all live execution/write flags false. This remains plan-only and does not run live specialists, call specialist LLMs, execute specialist tools, write data, message/post/sell, run Builder/Forge, apply patches, or deploy. Focused Oom Sakkie service/routes/frontend tests passed at 151 tests, `node --check static/js/oomSakkie.js` passed, and full local unittest suite passed at 476 tests.
- Phase 10.9F Oom Sakkie Visible Crew Sequence is local-ready: `agent_activity` now includes a bounded `crew_sequence` when a Team Brief returns a crew plan, and the kiosk Agent Activity Stage renders those planned specialists as visible cards with order, name/personality, what each would inspect, and `runs no | writes no`. The sequence is hidden for normal single-tool checks. This remains visual-only and does not run live specialists, call specialist LLMs, execute specialist tools, write data, message/post/sell, run Builder/Forge, apply patches, or deploy. Focused Oom Sakkie service/routes/frontend tests passed at 152 tests, `node --check static/js/oomSakkie.js` passed, and full local unittest suite passed at 477 tests.
- Phase 10.9G Oom Sakkie Agent Activation Plan is local-ready: added `get_agent_activation_plan()`, read-only `agent_activation_plan`, deterministic activation-roadmap routing, and an `Agent Roadmap` quick action. Oom Sakkie can now explain the locked path from visible planned agents to future live agents: read-only dry-run, human-approved dispatch, draft-only outputs, and only later controlled writes. Sentinel is recommended as the first dry-run candidate because it has the smallest safe surface. This remains read-only and does not run live specialists, call specialist LLMs, enable runtime flags, execute specialist tools, write data, message/post/sell, run Builder/Forge, apply patches, or deploy. Focused Oom Sakkie service/routes/frontend tests passed at 154 tests, `node --check static/js/oomSakkie.js` passed, and full local unittest suite passed at 479 tests.
- Phase 10.9H Oom Sakkie Sentinel Dry-Run Review is local-ready: added `build_sentinel_dry_run_review(tool_catalog)`, read-only `sentinel_dry_run_review`, deterministic Sentinel dry-run routing, and a `Sentinel Dry Run` quick action. Oom Sakkie can now rehearse the first specialist activation candidate by having Sentinel audit the current read-only tool catalog, list blockers before any live dry-run, and keep runtime/dispatch/autonomous-loop/write/specialist-LLM flags false. This remains read-only and does not run live specialists, call specialist LLMs, enable runtime flags, execute specialist tools, write data, message/post/sell, run Builder/Forge, apply patches, or deploy. Focused Oom Sakkie service/routes/frontend tests passed at 155 tests, `node --check static/js/oomSakkie.js` passed, and full local unittest suite passed at 480 tests.
- Phase 10.9I Oom Sakkie LLM Message Guard And Safety Follow-Ups is local-ready after Claude's pass-with-nits review: `/api/oom-sakkie/message` remains reachable wherever Flask is reachable while LLM router/answer features are off, but when an LLM surface is enabled the route must pass the same loopback/private-LAN guard before outbound paid API calls can occur. Runtime policy now exposes `message_endpoint_access.llm_guard_active` and the guard rule. The PRD now treats same-host reverse proxying in front of review routes as a hard deployment rule until trusted proxy handling is deliberately configured. The action guard now catches `irrigate` control phrasing and routes it to read-only irrigation status with safety notes. Added end-to-end unsafe LLM answer fallback coverage and a DATABASE_URL-gated live Postgres CHECK-constraint test for build/patch/deploy no-action flags. Focused Oom Sakkie service/routes/frontend tests passed at 161 tests, `node --check static/js/oomSakkie.js` passed, and full local unittest suite passed at 486 tests.
- Phase 10.9J Oom Sakkie Agent Dry-Run Request Gate is local-ready: added append-only `oom_sakkie_agent_dry_run_requests` and `oom_sakkie_agent_dry_run_events` tables through migration `202606080001_create_oom_sakkie_agent_dry_runs.sql`, plus `modules/oom_sakkie/agent_dry_run_store.py`, protected dry-run request/event routes, read-only `agent_dry_run_status`, deterministic dry-run queue routing, and a `Dry-Run Queue` quick action. Only Sentinel is allowed for this first request-gate slice; DB constraints and application params force `dry_run_enabled = false`, `dispatch_enabled = false`, `runs_specialist_llm = false`, `runs_specialist_tools = false`, and `writes = false`. The migration has been applied, a live-style smoke created request `OSK-AGENT-DRYRUN-B2E07585AD` and event `OSK-AGENT-DRYRUN-EVENT-0542A235E334`, focused Oom Sakkie service/routes/frontend tests passed at 170 tests, `node --check static/js/oomSakkie.js` passed, and full local unittest suite passed at 495 tests. This records owner approval intent only; it does not run Sentinel, dispatch specialists, call specialist LLMs, execute specialist tools, write farm data, run Builder/Forge, apply patches, or deploy.
- Phase 10.9K Oom Sakkie Message Guard Policy Consistency is local-ready after Claude's 10.9I/J pass-with-nits review: `modules/oom_sakkie/policy.py` now reports the same message-guard env set that `modules/oom_sakkie/access.py` enforces. Runtime policy exposes `message_endpoint_access.llm_guard_envs`, and `OOM_SAKKIE_LLM_LEARNING_ENABLED=true` now correctly reports `message_endpoint_access.llm_guard_active = true` even if router/answer composer are off. This is an honesty fix only; access enforcement was already fail-safe. Focused Oom Sakkie service/routes/frontend tests passed at 171 tests, `node --check static/js/oomSakkie.js` passed, and full local unittest suite passed at 496 tests.
- Phase 10.9L Oom Sakkie Sentinel Dry-Run Handoff Packet is local-ready: added `modules/oom_sakkie/agent_dry_run_handoff.py`, `get_agent_dry_run_request()`, and protected `POST /api/oom-sakkie/agent-dry-runs/handoff`. The route accepts only a stored `dry_run_request_id`, loads the persisted dry-run request, and produces `mode = agent_dry_run_handoff_only` with all execution flags false. The builder rejects corrupted requests with truthy `dry_run_enabled`, `dispatch_enabled`, `runs_specialist_llm`, `runs_specialist_tools`, or `writes`. This is packet/prompt only: it does not run Sentinel, dispatch specialists, call specialist LLMs, execute specialist tools, write farm data, run Builder/Forge, apply patches, or deploy. Focused Oom Sakkie service/routes/frontend tests passed at 174 tests, `node --check static/js/oomSakkie.js` passed, and full local unittest suite passed at 499 tests.
- Phase 10.9M Oom Sakkie Sentinel Dry-Run Result Gate is local-ready: added append-only `oom_sakkie_agent_dry_run_results` and `oom_sakkie_agent_dry_run_result_events` tables through migration `202606080002_create_oom_sakkie_agent_dry_run_results.sql`, plus `modules/oom_sakkie/agent_dry_run_result_store.py` and protected result/list/event routes. Result recording requires an existing Sentinel dry-run request and forces `runs_specialist = false`, `dispatch_enabled = false`, `runs_specialist_llm = false`, `runs_specialist_tools = false`, `writes = false`, and `applies_runtime_change = false` at DB and application layers. The migration has been applied, a live-style smoke created result `OSK-AGENT-DRYRUN-RESULT-2FBFA47000FC` and event `OSK-AGENT-DRYRUN-RESULT-EVENT-4DADAE8AD1B3`, focused Oom Sakkie service/routes/frontend tests passed at 181 tests, `node --check static/js/oomSakkie.js` passed, and full local unittest suite passed at 506 tests. This records review text only; it does not run Sentinel, dispatch specialists, call specialist LLMs, execute specialist tools, write farm data, apply runtime changes, run Builder/Forge, apply patches, or deploy.
- Phase 10.9N Oom Sakkie Sentinel Review Queue Status is local-ready: `agent_dry_run_status` now reads both dry-run requests and dry-run results, reports requests waiting for manual review, cancelled requests, results waiting for owner review, and results accepted for learning, and warns if either queue is unavailable. The `Dry-Run Queue` quick action now asks `What is the Sentinel dry-run result queue status?`, and deterministic routing catches Sentinel result/review queue phrasing. This is read-only queue status only; it does not create requests/results, accept/reject results, run Sentinel, dispatch specialists, call specialist LLMs, execute specialist tools, write farm data, or apply runtime changes. Focused Oom Sakkie service/routes/frontend tests passed at 182 tests, `node --check static/js/oomSakkie.js` passed, and full local unittest suite passed at 507 tests.
- Phase 10.9O Oom Sakkie Sentinel Dry-Run Result Review Packet is local-ready: added `get_agent_dry_run_result()`, `modules/oom_sakkie/agent_dry_run_result_review.py`, and protected `GET /api/oom-sakkie/agent-dry-run-results/<dry_run_result_id>/review-packet`. The route requires a persisted result ID, returns result text/findings/latest event/owner options, and exposes a review guard with execution/runtime/write flags false. The packet builder rejects stored results with unsafe execution flags. This is review-packet only; it does not run Sentinel, dispatch specialists, call specialist LLMs, execute specialist tools, write farm data, accept/reject results by itself, apply runtime changes, run Builder/Forge, apply patches, or deploy. Focused Oom Sakkie service/routes/frontend tests passed at 186 tests and `node --check static/js/oomSakkie.js` passed.
- Phase 10.9P Oom Sakkie Sentinel Result Review UI is local-ready: the System Workbench now has a `Sentinel Result Review` panel that loads recent dry-run results, groups them into `Needs Owner Review` and `Reviewed / Closed`, opens a review packet for a selected result, and records append-only owner events (`accepted_for_learning`, `rejected`, `review_note`) from explicit button clicks. Workbench `Next action` now includes pending Sentinel result reviews. This remains UI/event-only; it does not run Sentinel, dispatch specialists, call specialist LLMs, execute specialist tools, write farm data, apply runtime changes, run Builder/Forge, apply patches, or deploy. Focused Oom Sakkie service/routes/frontend tests passed at 186 tests and `node --check static/js/oomSakkie.js` passed.
- Phase 10.9Q Oom Sakkie Agent Learning Evidence is local-ready: added read-only `agent_learning_evidence`, deterministic routing for accepted Sentinel/agent learning questions, and kiosk quick action `Agent Learning`. The tool reads recent dry-run results and reports only those whose latest event is `accepted_for_learning`, including result IDs, request IDs, findings, accepted timestamp, and accepted note. This is evidence summarization only; it does not run Sentinel, dispatch specialists, call specialist LLMs, execute specialist tools, write farm data, apply runtime changes, run Builder/Forge, apply patches, or deploy. Focused Oom Sakkie service/routes/frontend tests passed at 187 tests and `node --check static/js/oomSakkie.js` passed.
- Phase 10.9R Oom Sakkie Agent Learning Ledger UI is local-ready: the System Workbench now has an `Agent Learning Ledger` panel that displays only accepted dry-run evidence (`latest_event.event_type = accepted_for_learning`) from the Sentinel result queue. Ledger cards show result ID, linked request ID, specialist slug, accepted timestamp, result text, findings, and owner acceptance note. This is UI-only accepted-evidence display; it does not run Sentinel, dispatch specialists, call specialist LLMs, execute specialist tools, write farm data, apply runtime changes, run Builder/Forge, apply patches, or deploy. Focused Oom Sakkie service/routes/frontend tests passed at 187 tests and `node --check static/js/oomSakkie.js` passed.
- Phase 10.9S Oom Sakkie Accepted Learning Roadmap Link is local-ready: `agent_activation_plan` now reads the same accepted Sentinel learning evidence as `agent_learning_evidence` and reports the accepted count in the agent roadmap answer. Accepted evidence is still planning context only; it does not unlock dispatch, run Sentinel, call specialist LLMs, execute specialist tools, write farm data, apply runtime changes, run Builder/Forge, apply patches, or deploy. Focused Oom Sakkie service/routes/frontend tests passed at 188 tests, full local unittest suite passed at 518 tests, and `node --check static/js/oomSakkie.js` passed.
- Phase 10.9T Oom Sakkie Visible Agent Roadmap Panel is local-ready: added protected read-only `GET /api/oom-sakkie/agents/activation-plan` and an `Agent Roadmap` panel inside the System Workbench. The panel shows the activation stages, first safe candidate, accepted Sentinel evidence count, and runtime/write guard flags. This is panel data only; it does not run Sentinel, dispatch specialists, call specialist LLMs, execute specialist tools, write farm data, apply runtime changes, run Builder/Forge, apply patches, or deploy. Focused Oom Sakkie service/routes/frontend tests passed at 190 tests, full local unittest suite passed at 520 tests, and `node --check static/js/oomSakkie.js` passed.
- Phase 10.9U Oom Sakkie Sentinel Dry-Run Request Button is local-ready: added `Request Sentinel Dry-Run` button to the Agent Roadmap panel. The button posts a fixed Sentinel-only payload to the existing protected append-only `POST /api/oom-sakkie/agent-dry-runs` endpoint and immediately shows the recorded request ID plus false execution flags. This records owner approval intent only; it does not run Sentinel, dispatch specialists, call specialist LLMs, execute specialist tools, write farm data, apply runtime changes, run Builder/Forge, apply patches, or deploy. Focused Oom Sakkie service/routes/frontend tests passed at 191 tests, full local unittest suite passed at 521 tests, and `node --check static/js/oomSakkie.js` passed.
- Phase 10.9V Oom Sakkie Sentinel Dry-Run Mini-Pipeline UI is local-ready: added `Sentinel Dry-Run Requests` panel, handoff prompt display/copy flow, and `Record Sentinel Result` form inside the System Workbench. The panel reads append-only dry-run requests, opens prompt-only handoff packets for persisted request IDs, and records reviewed result text through the existing append-only result endpoint. This is UI/audit plumbing only; it does not run Sentinel, dispatch specialists, call specialist LLMs, execute specialist tools, write farm data, apply runtime changes, run Builder/Forge, apply patches, or deploy. Focused Oom Sakkie service/routes/frontend tests passed at 191 tests, full local unittest suite passed at 521 tests, and `node --check static/js/oomSakkie.js` passed.
- Phase 10.9W Oom Sakkie Workbench Sentinel Next Action is local-ready: the Workbench `Next action` card now tracks pending Sentinel dry-run requests and prioritizes `Open Sentinel handoff` before result review, build handoff, patch review, or deploy decision. Recording a dry-run result refreshes the request queue, result review queue, and roadmap state. This is UI state only; it does not run Sentinel, dispatch specialists, call specialist LLMs, execute specialist tools, write farm data, or apply runtime changes. Focused Oom Sakkie service/routes/frontend tests passed at 191 tests, full local unittest suite passed at 521 tests, and `node --check static/js/oomSakkie.js` passed.
- Phase 10.9X Oom Sakkie Live-PG Audit Rail Smoke is local-ready: extended the DATABASE_URL-gated integration coverage so a real Postgres can prove the append-only triggers and no-execution CHECK constraints on the Oom Sakkie review/audit rails. The new test inserts a valid build/patch/deploy/Sentinel dry-run audit chain, then asserts `UPDATE` and `DELETE` raise `append-only` on build requests/events, patch proposals/events, deploy decisions, dry-run requests/events, dry-run results, and dry-run result events. The no-execution CHECK test now also rejects `dry_run_enabled = true` and `runs_specialist = true` on the Sentinel dry-run tables. Normal local runs skip the live checks when `DATABASE_URL` is unavailable. Focused Oom Sakkie service/routes/frontend tests passed at 192 tests and `node --check static/js/oomSakkie.js` passed.
- Phase 10.9Y Oom Sakkie Prism Dry-Run Request Gate is local-ready: Prism is now the second approved dry-run specialist on the same append-only request -> handoff -> reviewed result rail as Sentinel. The approved dry-run allowlist is explicit (`sentinel`, `prism`), Prism requests get no-execution guardrails, the handoff packet says Prism is the kiosk/interface design reviewer, and result records preserve the approved specialist slug instead of hard-coding Sentinel. The Agent Roadmap panel now has `Request Prism Dry-Run`; this records owner approval intent only and does not run Prism, dispatch specialists, call specialist LLMs, execute specialist tools, generate assets, edit code, write farm data, apply runtime changes, run Builder/Forge, apply patches, or deploy. Focused Oom Sakkie service/routes/frontend tests passed at 196 tests and `node --check static/js/oomSakkie.js` passed.
- Phase 10.9Z Oom Sakkie Generic Agent Dry-Run Workbench Labels is local-ready: the shared dry-run Workbench rail now uses generic labels (`Agent Dry-Run Requests`, `Record Agent Result`, `Agent Result Review`) because both Sentinel and Prism can use the same request -> handoff -> reviewed result path. JS copy and the Workbench `Next action` card now say `agent handoff` / `agent result review`, while handoff cards use the packet's specialist name so Sentinel and Prism remain clear. This is UI wording only; it does not change routes, DB schema, allowed operations, specialist dispatch, specialist LLM/tool execution, farm writes, runtime changes, patches, or deploys. Focused Oom Sakkie service/routes/frontend tests passed at 196 tests and `node --check static/js/oomSakkie.js` passed.
- Phase 10.9AA Oom Sakkie Agent Dry-Run Browser Behavior Contracts is local-ready: frontend contract coverage now pins the shared agent dry-run/result Workbench behavior after Sentinel and Prism moved onto the same rail. The test asserts there is no `setInterval` background polling, review packet loading is GET-only, request/result/event actions are wired through explicit button handlers, render-only sections do not perform hidden fetches or event writes, and empty states use generic `agent` wording. This is test/UI-copy hardening only; it does not add routes, migrations, tools, LLM calls, specialist dispatch, specialist tool execution, farm writes, runtime changes, Builder/Forge execution, patch application, or deploys. Focused Oom Sakkie service/routes/frontend tests passed at 197 tests and `node --check static/js/oomSakkie.js` passed.
- Phase 10.9AB Oom Sakkie Approved Read-Only Dry-Run Cohort is local-ready: Ledger, Atlas, Rootline, Herdmaster, Butcher, and Quartermaster can now create append-only dry-run request records on the same no-execution request -> handoff -> reviewed-result rail as Sentinel and Prism. The dry-run allowlist still excludes Beacon, Forge, and Gatekeeper because they touch public draft, code/build, or routing-policy surfaces. Each approved specialist gets fixed read-only context defaults, handoff packets name the planned specialist role, and the Agent Roadmap panel has one selected-specialist dropdown instead of a long button row. This does not run specialists, call specialist LLMs, execute specialist tools, write farm data, create public/customer output, enable runtime flags, run Builder/Forge, apply patches, deploy, cut over Telegram, or control hardware. Focused Oom Sakkie service/routes/frontend tests passed at 200 tests and `node --check static/js/oomSakkie.js` passed.
- Phase 10.9AC Oom Sakkie Activation Roadmap Cohort Visibility is local-ready: the activation plan now exposes the same approved dry-run cohort as the request gate: Sentinel, Prism, Atlas, Ledger, Rootline, Herdmaster, Butcher, and Quartermaster. Each candidate reports `dry_run_request_allowed = true` and `allowed_now = false`, so the roadmap can show what may be recorded as a dry-run request while still saying runtime is locked. Beacon, Forge, and Gatekeeper stay absent from the dry-run candidate list. The kiosk Agent Roadmap renders an `Approved dry-run candidates` section with first-slice guidance and guard labels. This is roadmap/display data only and does not run agents, dispatch specialists, call specialist LLMs, execute specialist tools, write farm data, produce customer/public output, run Builder/Forge, apply patches, deploy, cut over Telegram, or control hardware. Focused Oom Sakkie service/routes/frontend tests passed at 200 tests and `node --check static/js/oomSakkie.js` passed.
- Phase 10.9AD Oom Sakkie Specialist Dry-Run Handoff Quality is local-ready: approved specialist handoff packets now include role-specific `focus_questions`, `required_context`, `risk_checks`, and `owner_approval_question` for Sentinel, Prism, Ledger, Atlas, Rootline, Herdmaster, Butcher, and Quartermaster. The generated prompt includes those specialist-specific sections before the no-go rules while still saying the specialist is not running, must not call tools, must not claim inspection, must not produce code, and must wait for owner approval. Ledger packets now check business/profit risks such as fake revenue, unapproved price changes, and customer-facing wording; Rootline packets check physical-control risks such as pump/control commands and stale weather assumptions. This is packet/prompt text only and does not run specialists, call specialist LLMs, execute specialist tools, write farm data, produce customer/public output, run Builder/Forge, apply patches, deploy, cut over Telegram, or control hardware. Focused Oom Sakkie service/routes/frontend tests passed at 201 tests and `node --check static/js/oomSakkie.js` passed.
- Phase 10.9AE Oom Sakkie Specialist Result Evidence Boundaries is local-ready: dry-run result review packets now classify accepted evidence by specialist and show `evidence_kind`, `may_influence`, `must_not_influence`, and `owner_review_question`. Ledger evidence may influence future business brief questions/internal offer planning but must not influence customer messages, price changes, quotes, or invoices. Rootline evidence may influence read-only weather/irrigation inspection questions but must not influence pump/valve commands, irrigation schedules, or physical controls. The kiosk Agent Result Review packet panel now displays these boundaries before the owner accepts/rejects/adds notes. This is review packet/UI display only and does not run specialists, call specialist LLMs, execute specialist tools, write farm data, produce customer/public output, enable runtime flags, run Builder/Forge, apply patches, deploy, cut over Telegram, or control hardware. Focused Oom Sakkie service/routes/frontend tests passed at 203 tests and `node --check static/js/oomSakkie.js` passed.
- Phase 10.9AF Oom Sakkie Per-Specialist Dry-Run Queue Status is local-ready: the read-only `agent_dry_run_status` tool now calculates `specialist_counts` for loaded request/result rows, including requests, requests waiting, results, results waiting, and accepted-for-learning results. The answer summary can now say which specialists have queue items, e.g. Ledger or Sentinel, while still reporting that this is queue status only and no specialist was dispatched. This is read-only queue summarization only and does not run specialists, call specialist LLMs, execute specialist tools, write farm data, change review events, enable runtime flags, run Builder/Forge, apply patches, deploy, cut over Telegram, produce public/customer output, or control hardware. Focused Oom Sakkie service/routes/frontend tests passed at 203 tests and `node --check static/js/oomSakkie.js` passed.
- Phase 10.9AG Oom Sakkie Generic Agent Learning Evidence is local-ready: accepted dry-run learning evidence is no longer Sentinel-only in owner-facing runtime copy. `accepted_agent_learning_snapshot` now returns `accepted_by_specialist`, `agent_learning_evidence` reports accepted agent result(s) by specialist, `agent_activation_plan` can include those counts in its planning summary, and the Agent Roadmap panel says `Accepted agent learning`. This is read-only accepted-evidence summarization only; accepted evidence remains planning context and does not run specialists, call specialist LLMs, execute specialist tools, write farm data, change review events, enable runtime flags, run Builder/Forge, apply patches, deploy, cut over Telegram, produce public/customer output, or control hardware. Focused Oom Sakkie service/routes/frontend tests passed at 203 tests and `node --check static/js/oomSakkie.js` passed.
- Phase 10.9AH Oom Sakkie Roadmap Learning Counts is local-ready: protected `GET /api/oom-sakkie/agents/activation-plan` now returns `accepted_by_specialist`, and the Agent Roadmap panel renders a compact `accepted by specialist` line under `Accepted agent learning`. This is read-only route/UI data only; it does not create requests/results/events, run specialists, call specialist LLMs, execute specialist tools, write farm data, enable runtime flags, run Builder/Forge, apply patches, deploy, cut over Telegram, produce public/customer output, or control hardware. Focused Oom Sakkie service/routes/frontend tests passed at 203 tests and `node --check static/js/oomSakkie.js` passed.
- Phase 10.9AI Oom Sakkie Audit Rail CI Workflow is local-ready: added `.github/workflows/oom-sakkie-audit-rails.yml`, which starts disposable Postgres 16, installs `requirements.txt`, applies the reviewed Oom Sakkie audit migrations through `202606080002_create_oom_sakkie_agent_dry_run_results.sql`, and runs `python -m unittest` with `DATABASE_URL` configured so live-PG append-only/no-execution checks run in CI instead of skipping. It also runs `node --check static/js/oomSakkie.js`. This is CI/test configuration only and does not add runtime authority, routes, tools, specialist dispatch, specialist LLM/tool execution, farm writes, public/customer output, Builder/Forge execution, patch application, deploy, Telegram cutover, or physical controls. Focused Oom Sakkie service/routes/frontend tests passed at 204 tests and `node --check static/js/oomSakkie.js` passed locally.
- Phase 10.9AJ Oom Sakkie Browser Behavior Checklist is local-ready: added `docs/06-operations/OOM_SAKKIE_BROWSER_BEHAVIOR_CHECKLIST.md` to make the multi-specialist Workbench browser pass repeatable. The checklist covers dry-run request buttons, handoff/result review, accepted learning/roadmap counts, no background polling, explicit owner-click POST actions, and voice start/stop with the five-turn loop cap. Frontend contract tests now assert the checklist and audit-rail CI workflow exist and name the core safety expectations. This is documentation/regression coverage only and does not run specialists, call specialist LLMs, execute specialist tools, write farm data, enable runtime flags, run Builder/Forge, apply patches, deploy, cut over Telegram, produce public/customer output, or control hardware.
- Phase 10.9AK Oom Sakkie Audit Rail CI Scope Hardening is local-ready: `.github/workflows/oom-sakkie-audit-rails.yml` now runs the focused Oom Sakkie service/routes/frontend test modules with `DATABASE_URL` configured instead of the full test suite. This keeps the disposable-Postgres audit rail job scoped to the Oom Sakkie migrations it applies while still exercising the live-PG append-only/no-execution tests. This is CI/test configuration only and does not add runtime authority, routes, tools, specialist dispatch, specialist LLM/tool execution, farm writes, public/customer output, Builder/Forge execution, patch application, deploy, Telegram cutover, or physical controls. Focused Oom Sakkie service/routes/frontend tests passed at 204 tests, full local unittest suite passed at 534 tests, and `node --check static/js/oomSakkie.js` passed locally.
- Phase 10.9AL Oom Sakkie Agent Runtime Readiness Tool is local-ready: added `get_agent_runtime_readiness()` and read-only `agent_runtime_readiness` so Oom Sakkie can answer what still blocks live agents. The readiness checklist reports ready gates, manual gates, locked gates, approved dry-run candidates, blocked capabilities, and the next safe action while keeping every runtime/dispatch/specialist-LLM/specialist-tool/write/public-output/control flag false. Deterministic routing now catches `ready for live agents`, `what still blocks runtime`, and similar phrases. This is checklist-only and does not run specialists, call specialist LLMs, execute specialist tools, write farm data, enable runtime flags, run Builder/Forge, apply patches, deploy, cut over Telegram, produce public/customer output, or control hardware. Focused Oom Sakkie service/routes/frontend tests passed at 206 tests and `node --check static/js/oomSakkie.js` passed.
- Phase 10.9AM Oom Sakkie Browser Behavior Smoke Gate is local-ready: added `tests/oom_sakkie_browser_behavior_smoke.js`, a dependency-free Node smoke that executes the real kiosk script in a fake DOM, asserts startup creates no background polling intervals, asserts startup performs no hidden POST requests, and verifies dry-run request/result/message POSTs only happen after explicit owner-click handlers fire. The disposable-Postgres audit-rail workflow now runs this smoke after `node --check`. This is test/CI hardening only and does not add runtime authority, routes, tools, specialist dispatch, specialist LLM/tool execution, farm writes, public/customer output, Builder/Forge execution, patch application, deploy, Telegram cutover, or physical controls. Browser behavior smoke passed locally, focused Oom Sakkie service/routes/frontend tests passed at 207 tests, `node --check static/js/oomSakkie.js` passed, and full local unittest suite passed at 537 tests.
- Phase 10.9AN Oom Sakkie Agent Operating Contracts is local-ready: added `get_agent_operating_contracts()` and read-only `agent_operating_contracts` so Oom Sakkie can explain each planned specialist's focus, allowed read-only tools, memory sources, output contract, must-not-do list, owner gate, and dry-run request eligibility. Beacon, Forge, and Gatekeeper stay locked out of dry-run requests; Ledger cannot send customer messages/change prices, Rootline cannot start pumps/valves/change schedules, Herdmaster cannot write pig/litter/weight/medical records, Butcher cannot book slaughter/create sales, and Beacon cannot post publicly. This is planning-contract data only and does not run specialists, call specialist LLMs, execute specialist tools, write farm data, enable runtime flags, run Builder/Forge, apply patches, deploy, cut over Telegram, produce public/customer output, or control hardware. Focused Oom Sakkie service/routes/frontend tests passed at 209 tests.
- Phase 10.9AO Oom Sakkie Agent Contracts Review Endpoint is local-ready: added protected `GET /api/oom-sakkie/agents/contracts` so reviewers and future UI can inspect the operating contracts directly. The route is loopback/private-LAN review-gated and returns the contract payload plus a `review_guard` with `runs_specialist`, `dispatch_enabled`, `runs_specialist_llm`, `runs_specialist_tools`, `writes`, and `applies_runtime_change` all false. This is read-only route data only and does not run specialists, call specialist LLMs, execute specialist tools, write farm data, enable runtime flags, run Builder/Forge, apply patches, deploy, cut over Telegram, produce public/customer output, or control hardware. Focused Oom Sakkie service/routes/frontend tests passed at 211 tests.
- Phase 10.9AP Oom Sakkie Agent Activation Preflight is local-ready: added `get_agent_activation_preflight()`, read-only `agent_activation_preflight`, deterministic preflight routing, and protected `GET /api/oom-sakkie/agents/preflight`. The preflight combines readiness, contracts, and activation-plan context into ready/manual/locked checks, including browser behavior smoke, GitHub Actions green confirmation, owner browser pass, Claude review, bounded dry-run cohort, and live-dispatch locks. It returns `summary_status = not_ready_for_live_dispatch` and all runtime/dispatch/specialist-LLM/specialist-tool/write/public-output/control flags false. This is read-only preflight data only and does not run specialists, call specialist LLMs, execute specialist tools, write farm data, enable runtime flags, run Builder/Forge, apply patches, deploy, cut over Telegram, produce public/customer output, or control hardware. Focused Oom Sakkie service/routes/frontend tests passed at 215 tests.
- Phase 10.9AQ Oom Sakkie Activation Preflight Wording Hardening is local-ready: `audit_rail_ci` and `browser_behavior_smoke` preflight checks now report `status = configured` instead of `pass`, and the details explicitly distinguish configured safety gates from manual proof that GitHub Actions is green or the owner browser pass has run. This is wording/data honesty only and does not change routes, tools, runtime flags, dispatch, specialist LLM/tool execution, writes, public output, patches, deploys, Telegram, or physical controls.
- Phase 10.9AR Oom Sakkie Agent Authority Matrix is local-ready: added `get_agent_authority_matrix()`, read-only `agent_authority_matrix`, deterministic authority-routing, and protected `GET /api/oom-sakkie/agents/authority-matrix`. The matrix lists future authority areas, risk levels, why each is locked, and required gates for live specialist dispatch, specialist LLM/tool loops, farm data writes, customer/public output, Builder/patch execution, deploy execution, Telegram cutover, and physical controls. It returns `enabled_count = 0`, `locked_count = authority_count`, and all runtime/dispatch/specialist-LLM/specialist-tool/write/public-output/control flags false. This is read-only authority visibility only and does not run specialists, call specialist LLMs, execute specialist tools, write farm data, enable runtime flags, run Builder/Forge, apply patches, deploy, cut over Telegram, produce public/customer output, or control hardware. Focused Oom Sakkie service/routes/frontend tests passed at 219 tests.
- Phase 10.9AS Oom Sakkie Authority Lock Source Alignment is local-ready: authority rows now include `blocked_capability`, and activation-plan blocked capabilities, preflight locked checks, and authority-matrix rows are derived from the same authority source. A drift test proves activation-plan blocked capabilities match the matrix and preflight locked checks carry the same risk level, lock reason, and required gates as the matrix. This is source-consistency hardening only and does not change runtime flags, dispatch, specialist LLM/tool execution, writes, public output, patches, deploys, Telegram, or physical controls. Focused Oom Sakkie service/routes/frontend tests passed at 220 tests.
- Phase 10.9AT Oom Sakkie Authority Unlock Readiness is local-ready: added `get_agent_authority_unlock_readiness()`, read-only `agent_authority_unlock_readiness`, deterministic unlock-readiness routing, and protected `GET /api/oom-sakkie/agents/unlock-readiness`. The report says `summary_status = planning_only_no_unlock_recommended`, `enabled_count = 0`, lists the lowest-risk planning candidates, lists risk-4/5 hard-no authorities such as physical controls, and names required gates before any unlock work. This is read-only planning only and does not recommend or perform any unlock, run specialists, call specialist LLMs, execute specialist tools, write farm data, enable runtime flags, run Builder/Forge, apply patches, deploy, cut over Telegram, produce public/customer output, or control hardware. Focused Oom Sakkie service/routes/frontend tests passed at 224 tests.
- Phase 10.9AU Oom Sakkie Runtime Inspection Invariant Test is local-ready: added a regression test over runtime status, activation plan, runtime readiness, operating contracts, activation preflight, authority matrix, and unlock readiness to assert every authority flag present on those inspection surfaces remains false. This is test-only hardening and does not change runtime behavior or authority. Focused Oom Sakkie service/routes/frontend tests passed at 225 tests.
- Phase 10.9AV Oom Sakkie Dispatch Decision Rail Blueprint is local-ready: added `get_agent_dispatch_decision_rail_blueprint()`, read-only `agent_dispatch_decision_rail_blueprint`, deterministic blueprint routing, and protected `GET /api/oom-sakkie/agents/dispatch-rail-blueprint`. This prepares a reviewable design for a possible future append-only dispatch decision rail, including proposed tables, endpoint shapes, required tests, non-goals, and owner/Claude gates. It is blueprint-only: no dispatch rail migration/store/event endpoint exists, no specialist runs, no specialist LLM/tool execution happens, no runtime flags change, and all runtime/dispatch/write/public/control flags remain false. Focused Oom Sakkie service/routes/frontend tests passed at 229 tests.
- Phase 10.9AW Oom Sakkie Agent Runtime Review Packet is local-ready: added `get_agent_runtime_review_packet()`, read-only `agent_runtime_review_packet`, deterministic review-packet routing, and protected `GET /api/oom-sakkie/agents/runtime-review-packet`. This bundles runtime status, readiness, operating contracts, activation preflight, authority matrix, unlock readiness, and the dispatch rail blueprint into one review-only payload with the Claude handoff prompt included. It does not run specialists, call specialist LLMs/tools, write farm data, enable runtime flags, create public output, apply patches, deploy, cut over Telegram, or control hardware. Focused Oom Sakkie service/routes/frontend tests passed at 233 tests.
- Phase 10.9AX Oom Sakkie Playwright Browser Behavior Gate is local-ready/CI-ready: added `package.json`, `playwright.config.js`, `tests/oom_sakkie_playwright_behavior.spec.js`, and `.github/workflows/oom-sakkie-browser-behavior.yml`. The real-browser spec opens `/oom-sakkie`, stubs all `/api/oom-sakkie/**` calls with read-only JSON, verifies no hidden startup POSTs, verifies no interval polling, and verifies dry-run/result/message POSTs only happen after explicit owner clicks. This is test/CI scaffolding only and does not change routes, tools, stores, migrations, runtime flags, specialist dispatch, specialist LLM/tool execution, farm writes, public output, patches, deploys, Telegram, or physical controls. Focused Oom Sakkie service/routes/frontend tests passed at 233 tests; `node --check tests/oom_sakkie_playwright_behavior.spec.js` and `node --check playwright.config.js` passed. The actual browser run is expected in GitHub Actions or after local Playwright dependency install.
- Phase 10.9AY Oom Sakkie Dispatch Decision Rail is local-ready with migration pending: added `supabase/migrations/202606090001_create_oom_sakkie_dispatch_decisions.sql`, `modules/oom_sakkie/dispatch_decision_store.py`, protected dispatch request/decision routes, and CI migration wiring. This creates an append-only review rail for future dispatch design requests and owner decisions only; it does not run specialists, call specialist LLMs/tools, write farm data, enable runtime flags, produce public/customer output, apply patches, deploy, cut over Telegram, or control hardware. Application code and DB CHECK constraints force `dispatch_enabled`, `runs_specialist_llm`, `runs_specialist_tools`, `writes`, and `applies_runtime_change` false, while DB triggers block update/delete. Focused Oom Sakkie service/routes/frontend tests passed at 242 tests. The migration was not applied to the local/live database in this turn.
- Phase 10.9AZ Oom Sakkie Dispatch Decision Status Visibility is local-ready: added read-only `dispatch_decision_status`, deterministic dispatch-status routing, and dispatch-design counts inside `system_work_status`. Oom Sakkie can now answer which append-only dispatch design requests are waiting for owner/Claude review and whether any were approved for design review/rejected/deferred, while explicitly stating that no specialist dispatch is enabled. This is queue/status visibility only and does not consume dispatch decisions to change runtime behavior, run specialists, call specialist LLMs/tools, write farm data, enable runtime flags, create public/customer output, apply patches, deploy, cut over Telegram, or control hardware. Focused Oom Sakkie service/routes/frontend tests passed at 243 tests, browser behavior smoke passed, JS syntax checks passed, and the full local unittest suite passed at 573 tests.
- Phase 10.9BA Oom Sakkie Dispatch Runtime Review Packet is local-ready: added read-only `dispatch_runtime_review_packet`, which combines the locked `agent_runtime_review_packet` with the read-only dispatch design-status snapshot so Oom Sakkie can prepare one owner/Claude review answer before any code consumes dispatch decisions. This is review assembly only; it does not add routes, stores, migrations, event types, runtime flags, dispatch consumers, specialist LLM/tool execution, farm-data writes, public/customer output, patches, deploys, Telegram cutover, or physical controls. Focused Oom Sakkie service/routes/frontend tests passed at 244 tests, browser behavior smoke passed, JS syntax checks passed, and the full local unittest suite passed at 574 tests.
- Phase 10.9BB Oom Sakkie Jarvis Product Progress is local-ready: added read-only `jarvis_product_progress` so the owner can ask for progress bars/percentages across foundation safety rails, kiosk voice, read-only farm intelligence, agent roster/contracts, dry-run learning, builder gates, live specialist execution, business advisor automation, customer/public selling, and the alive Jarvis UI. The payload reports an overall percentage, per-area bars, next milestone, and blockers while keeping every runtime/dispatch/specialist-LLM/specialist-tool/write/public-output/control flag false. This is planning visibility only and does not run specialists, enable dispatch, call specialist LLMs/tools, write farm data, produce public/customer output, apply patches, deploy, cut over Telegram, or control hardware. Focused Oom Sakkie service/routes/frontend tests passed at 246 tests, browser behavior smoke passed, JS syntax checks passed, and the full local unittest suite passed at 576 tests.
- Phase 10.9BC Oom Sakkie Agent Command Center is local-ready: added read-only `agent_command_center` so the owner can ask who is working / what the agents are doing / show the command center and receive one control-tower view of current Jarvis lanes, queue snapshots, progress, and locked gates. Every lane is explicitly non-executing (`runs_agent`, `dispatch_enabled`, `runs_specialist_llm`, `runs_specialist_tools`, `writes`, and `applies_runtime_change` all false). This is visibility only and does not run specialists, enable dispatch, call specialist LLMs/tools, write farm data, consume dispatch decisions, produce public/customer output, apply patches, deploy, cut over Telegram, or control hardware. Focused Oom Sakkie service/routes/frontend tests passed at 248 tests, browser behavior smoke passed, JS syntax checks passed, and the full local unittest suite passed at 578 tests.
- Phase 10.9BD Oom Sakkie Command Center Quick Action is local-ready: added a `Command Center` quick-check button and mapped `agent_command_center` to Gatekeeper in the visible agent activity stage. This makes the command-center answer easier to reach without adding another heavy panel. It is UI reachability/visual routing only and does not add routes, stores, DB writes, background polling, hidden POSTs, runtime flags, specialist dispatch, specialist LLM/tool execution, farm writes, public/customer output, patches, deploys, Telegram cutover, or physical controls. Focused Oom Sakkie service/routes/frontend tests passed at 249 tests, browser behavior smoke passed, JS syntax checks passed, and the full local unittest suite passed at 579 tests.
- Phase 10.9BF Oom Sakkie Playwright CI Startup Hardening is local-ready: after the GitHub email showed the browser-behavior workflow failed in CI, the workflow was adjusted to use Python 3.12 like the local venv and to start Flask via `python -m flask --app app run --host 127.0.0.1 --port 5000` with debug disabled instead of `python app.py`. This is CI startup hardening only and does not change app runtime authority, routes, stores, DB writes, hidden POSTs, specialist dispatch, specialist LLM/tool execution, farm writes, public/customer output, deploy, Telegram, or physical controls. The next proof is a green GitHub Actions run after push.
- Phase 10.9BG Oom Sakkie Daily Command Brief is local-ready: added read-only `jarvis_daily_command_brief`, deterministic routing for daily command/start-my-day phrasing, Gatekeeper visual mapping, and changed the existing `Brief` quick action to request this broader command brief. It composes the farm operating brief, business growth brief, and Agent Command Center into one owner-ready response, extracts read-only next actions from pending approval/design counts and the Business Advisor owner question, and reports `partial` with stale warnings if any section is unavailable. This is read-only composition only and does not add routes, stores, DB writes, runtime flags, specialist dispatch, specialist LLM/tool execution, farm writes, customer/public output, patches, deploys, Telegram cutover, or physical controls. Focused Oom Sakkie service/routes/frontend tests passed at 252 tests, frontend route contracts passed at 27 tests, browser behavior smoke passed, and `node --check static/js/oomSakkie.js` passed.
- Phase 10.9BH Oom Sakkie Playwright CI Node 24 And Server URL Hardening is local-ready: after the second GitHub email, the browser-behavior workflow now opts into Node 24 (`FORCE_JAVASCRIPT_ACTIONS_TO_NODE24 = true`, `node-version: "24"`) and explicitly uses `/oom-sakkie` as the Playwright webServer readiness URL via `OOM_SAKKIE_PLAYWRIGHT_SERVER_URL`. `playwright.config.js` now separates the test `baseURL` from the server readiness URL. This is CI/test hardening only and does not change app runtime authority, routes, stores, DB writes, hidden POSTs, specialist dispatch, specialist LLM/tool execution, farm writes, public/customer output, deploy, Telegram, or physical controls. Frontend route contracts passed at 27 tests and Playwright config/spec syntax checks passed locally; the next proof is a green GitHub Actions run after push.
- Phase 10.9BI Oom Sakkie Playwright Workbench Visibility Fix is local-ready: after the next GitHub email still showed the Playwright real-browser gate failing, the spec was corrected to open the collapsed `System Workbench` before clicking dry-run/result controls and to assert the Sentinel dry-run button is visible before clicking. This matches the real UI while preserving the owner-click-only POST invariant. This is test-only hardening and does not change app runtime authority, routes, stores, DB writes, hidden POSTs, specialist dispatch, specialist LLM/tool execution, farm writes, public/customer output, deploy, Telegram, or physical controls. Frontend route contracts passed at 27 tests and Playwright config/spec syntax checks passed locally; the next proof is a green GitHub Actions run after push.
- Phase 10.9BJ Oom Sakkie Audit Rail CI Node 24 Warning Cleanup is local-ready: after the audit-rail workflow showed green with a Node 20 actions deprecation warning, `.github/workflows/oom-sakkie-audit-rails.yml` now opts into Node 24 with `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24 = true`. This is CI warning cleanup only and does not change app runtime authority, routes, stores, DB writes, hidden POSTs, specialist dispatch, specialist LLM/tool execution, farm writes, public/customer output, deploy, Telegram, or physical controls. Frontend route contracts passed at 27 tests locally; the next proof is a green GitHub Actions run without the warning after push.
- Phase 10.9BK Oom Sakkie Safety Gate Board is local-ready: added read-only `jarvis_safety_gate_board` so the owner can ask whether safety/CI gates are green and get one honest board covering audit-rail CI, browser-behavior CI, runtime authority locks, and the external GitHub status limitation. The board is also folded into Agent Command Center as a panel/source/snapshot and maps visually to Sentinel. It makes no GitHub/network call and does not add routes, stores, migrations, DB writes, runtime flags, specialist dispatch, specialist LLM/tool execution, farm writes, public/customer output, patches, deploys, Telegram cutover, or physical controls. Oom Sakkie service tests passed at 152 tests with 3 expected live-DB skips, focused service/routes/frontend tests passed at 255 tests, `node --check static/js/oomSakkie.js` passed, and the browser behavior smoke passed.
- Phase 10.9BL Oom Sakkie Owner Review Packet is local-ready: added read-only `jarvis_owner_review_packet` so the owner can ask `prepare Claude review` or `handoff to Claude` and get one batched review-readiness packet before deciding whether to spend a Claude review. It composes existing locked/read-only progress, command-center, safety-gate, and runtime-review surfaces, maps visually to Gatekeeper, and explicitly says the packet is not approval to unlock authority. It makes no Claude/GitHub/network call and does not add routes, stores, migrations, DB writes, runtime flags, specialist dispatch, specialist LLM/tool execution, farm writes, public/customer output, patches, deploys, Telegram cutover, or physical controls. Oom Sakkie service tests passed at 155 tests with 3 expected live-DB skips, focused service/routes/frontend tests passed at 258 tests, `node --check static/js/oomSakkie.js` passed, browser behavior smoke passed, and the full local unittest suite passed at 588 tests.
- Phase 10.9BM Oom Sakkie Review Shortcuts is local-ready: added explicit-click kiosk quick actions for `Safety Gates` and `Review Packet`, using the existing `data-quick-ask` path to reach `jarvis_safety_gate_board` and `jarvis_owner_review_packet` without adding routes or any new runtime authority. This is UI reachability only and does not add background polling, hidden POSTs, stores, migrations, DB writes, runtime flags, specialist dispatch, specialist LLM/tool execution, farm writes, public/customer output, patches, deploys, Telegram cutover, or physical controls. Frontend route contracts passed at 27 tests, focused service/routes/frontend tests passed at 258 tests, `node --check static/js/oomSakkie.js` passed, browser behavior smoke passed, and the full local unittest suite passed at 588 tests.
- Phase 10.9BN Oom Sakkie Quick Checks Grouping is local-ready: grouped the existing quick-action buttons into `Farm`, `Business`, and `Agent Review` clusters so the kiosk is easier to scan without changing any prompt or JavaScript behavior. This is UI clarity only and does not add background polling, hidden POSTs, routes, stores, migrations, DB writes, runtime flags, specialist dispatch, specialist LLM/tool execution, farm writes, public/customer output, patches, deploys, Telegram cutover, or physical controls. Frontend route contracts passed at 27 tests, focused service/routes/frontend tests passed at 258 tests, `node --check static/js/oomSakkie.js` passed, browser behavior smoke passed, and the full local unittest suite passed at 588 tests.
- Phase 10.9BO Oom Sakkie CI Green Evidence is local-ready: installed portable GitHub CLI under ignored `.tools/`, authenticated it as owner `Crewless9086`, and verified the latest GitHub Actions runs on `main` are green. `Oom Sakkie Browser Behavior` run `27225474174` completed successfully on 2026-06-09T17:58:15Z with job `Playwright real-browser behavior gate` successful. `Oom Sakkie Audit Rails` run `27225474133` completed successfully on 2026-06-09T17:58:02Z with job `Unit tests with disposable Postgres audit rails` successful. This is operational evidence only and does not change app runtime authority, routes, stores, DB writes, specialist dispatch, specialist LLM/tool execution, farm writes, public/customer output, patches, deploys, Telegram cutover, or physical controls.
- Phase 10.9BP Oom Sakkie Dispatch Execution Approval Rail is local-ready with migration pending: added `supabase/migrations/202606090002_create_oom_sakkie_dispatch_execution_approvals.sql`, `modules/oom_sakkie/dispatch_execution_approval_store.py`, protected execution-approval routes, CI migration wiring, and live-PG audit-smoke coverage. This is the first approval gate for Claude's recommended Sentinel-only single-shot advisory dry-run path, but it still runs nothing: recording `approved_for_single_dry_run_execution` requires an existing Sentinel dispatch request whose latest decision is `approved_for_design_review`, then writes only an append-only approval record. DB CHECK constraints and application code force `executes_now`, `dispatch_enabled`, `runs_specialist_llm`, `runs_specialist_tools`, `writes`, `applies_runtime_change`, and `dispatches_further` false, and append-only triggers block update/delete on the approval and event tables. No consumer, specialist LLM call, specialist tool execution, farm-data write, public/customer output, deploy, Telegram cutover, or physical-control path was added. Focused Oom Sakkie service/routes/frontend tests passed at 267 tests, browser behavior smoke passed, JS syntax checks passed, Playwright config/spec syntax checks passed, and the full local unittest suite passed at 597 tests.
- Phase 10.9BQ Oom Sakkie Sentinel Single-Shot Advisory Runner is local-ready with migrations pending and env default-off: added `modules/oom_sakkie/sentinel_single_shot_runner.py`, a review-gated `POST /api/oom-sakkie/dispatch-execution-approvals/<approval_id>/run-sentinel-dry-run`, policy disclosure for `OOM_SAKKIE_SPECIALIST_DRYRUN_ENABLED`, Safety Gate Board visibility for the env gate, and migration `202606090003_allow_single_shot_sentinel_dry_run_results.sql`. The runner refuses unless the global env flag and LLM config are present, the approval exists, the approval is Sentinel-only and `approved_for_single_dry_run_execution`, the parent dispatch request is still `approved_for_design_review`, the approval has not already been consumed, and `one_shot_scope.dry_run_request_id` points to a persisted Sentinel dry-run request. It records a unique append-only `consumed_by_single_dry_run_result` event before the outbound call to prevent replay; that consumed event is runner-only and cannot be recorded through the generic review-note route. The runner calls the OpenAI-compatible endpoint once with capped read-only context, rejects unsafe action-claiming output through the existing `_looks_unsafe` filter, and writes the advisory text only to the existing dry-run result rail using the new `single_shot_sentinel_advisory_result` mode. Specialist tools, farm-data writes, public/customer output, deploy, Telegram, physical controls, autonomous loops, and further dispatch remain disabled. Focused Oom Sakkie service/routes/frontend tests passed at 278 tests, browser behavior smoke passed, JS syntax checks passed, Playwright config/spec syntax checks passed, and the full local unittest suite passed at 608 tests.
- Phase 10.9BR Oom Sakkie Sentinel Runner Review Hardening is local-ready: after Claude's pass-with-process-gate review, the authority matrix now reports `specialist_llm_loop` as `single_shot_advisory_only` while keeping `enabled = false`, `specialist_llm_enabled = false`, and every other authority locked. The Safety Gate Board still reports the Sentinel env gate separately, and the default-off runner test now mocks the outbound HTTP path and asserts no network call occurs when `OOM_SAKKIE_SPECIALIST_DRYRUN_ENABLED` is off. This is honesty/test hardening only and does not widen the runner, add UI execution buttons, run tools, write farm data, create public/customer output, deploy, cut over Telegram, or control hardware. Focused Oom Sakkie service/routes/frontend tests passed at 278 tests, browser behavior smoke passed, JS syntax checks passed, Playwright config/spec syntax checks passed, and the full local unittest suite passed at 608 tests.
- Phase 10.9BS Oom Sakkie Effective Single-Shot Visibility is local-ready: the authority matrix now includes `effective_single_shot_enabled`, `effective_single_shot_configured`, mode, specialist, and note fields on the `specialist_llm_loop` area. These fields are derived from the existing Sentinel single-shot policy so an operator can see whether the narrow env-gated path is actually live, while the matrix still keeps `enabled = false`, `enabled_count = 0`, and top-level `specialist_llm_enabled = false`. Tests cover both default-off and env-on/configured reporting. This is visibility only and does not add routes, UI execution buttons, stores, migrations, tools, farm writes, public/customer output, deploy, Telegram cutover, or physical controls. Focused Oom Sakkie service/routes/frontend tests passed at 279 tests, browser behavior smoke passed, JS syntax checks passed, Playwright config/spec syntax checks passed, and the full local unittest suite passed at 609 tests.
- Phase 10.9BT Oom Sakkie Sentinel Single-Shot Runbook is local-ready: added `docs/06-operations/OOM_SAKKIE_SENTINEL_SINGLE_SHOT_RUNBOOK.md` for the first supervised Sentinel smoke. It documents prerequisites, the exact approved request chain, the short env-on window, expected success/failure shapes, replay-block verification, immediate flag-off confirmation, and outcome logging. This is docs-only operational control; it does not enable the env flag, call an LLM, add routes, run tools, write farm data, create public/customer output, deploy, cut over Telegram, or control hardware.
- Phase 10.9BU Oom Sakkie First Sentinel Smoke And Review Packet Fix is local-ready: after owner approval and Claude pass, a temporary local smoke server was started with `OOM_SAKKIE_SPECIALIST_DRYRUN_ENABLED=1` for one request only. Smoke chain: dry-run request `OSK-AGENT-DRYRUN-499E983FAF`, dispatch request `OSK-DISPATCH-REQ-3234DBAB07`, execution approval `OSK-DISPATCH-EXEC-APPROVAL-SMOKE-20260609-CODEX1`, consumed event `OSK-DISPATCH-EXEC-EVENT-6D892274A9`, and result `OSK-AGENT-DRYRUN-RESULT-C63AF980E948`. The Sentinel call succeeded and returned advisory-only findings; no tools ran, no farm data was written, no public/customer output was created, and no runtime change was applied. Replay against the consumed approval returned `409 dispatch_execution_approval_already_consumed`. The normal kiosk policy was verified afterward with `specialist_dry_run.enabled = false`, and the local kiosk is serving at `/oom-sakkie`. The smoke exposed one integration gap: review packets rejected the new honest `single_shot_sentinel_advisory_result` mode because it has `runs_specialist = true` and `runs_specialist_llm = true`. Local fix now allows only that exact Sentinel/status/mode combination while still rejecting dispatch/tool/write/runtime-change flags, and tests cover valid single-shot packet review plus unsafe single-shot flags. Focused Oom Sakkie service/routes/frontend tests passed at 281 tests, browser behavior smoke passed, JS/Playwright syntax checks passed, and the full local unittest suite passed at 611 tests.
- Phase 10.9BV Oom Sakkie Owner Approval Console is local-ready: added a first-screen `Needs Your Approval` console above the detailed System Workbench. It summarizes the current owner-decision queue from already-loaded build requests, patch proposals, deploy decisions, agent dry-run requests, and agent result reviews, then opens the existing Workbench section/action on explicit owner click. It filters dry-run handoff noise when a result already exists for that request, keeps the full audit Workbench intact below it, adds no routes/endpoints/stores/migrations, performs no background polling or hidden POSTs, and does not add runtime flags, specialist dispatch, specialist tool execution, farm writes, public/customer output, deploy, Telegram cutover, physical controls, or financial actions. Focused frontend contracts passed at 27 tests, focused Oom Sakkie service/routes/frontend tests passed at 254 tests, `node --check static/js/oomSakkie.js` passed, and the browser behavior smoke passed.
- Phase 10.9BW Oom Sakkie Controller Board is local-ready: the agent activity stage now shows a compact three-part board for `Controller`, `Specialist`, and `Owner Gate`, populated only from existing `agent_activity` response data. It makes Oom Sakkie feel more like a visible coordinator without adding endpoints, fetches, stores, migrations, POSTs, runner wiring, specialist dispatch, specialist tool execution, farm writes, public/customer output, deploy, Telegram cutover, physical controls, or financial actions.
- Phase 10.9BX Oom Sakkie Primary Command Deck is local-ready: added a first-screen read-only command deck for the most common owner asks (`Start Day`, `Needs approval`, `Agent command center`, `Gate status`) using the existing explicit-click `data-quick-ask` path. It adds no hidden POSTs, polling, routes, stores, migrations, approval/run/deploy buttons, Sentinel runner UI, or authority changes.
- Phase 10.9BY Oom Sakkie Quick Checks Drawer is local-ready: moved the larger farm/business/agent quick-action grid behind a collapsed `More read-only checks` drawer while keeping those shortcuts available. This is presentation only; existing quick-ask prompts and JavaScript binding are unchanged, and no backend or authority surface was added.
- Phase 10.9BZ Oom Sakkie Command UI Browser Gate Hardening is local-ready: the Playwright real-browser behavior spec now asserts the new command deck and quick-check drawer are visible, opening the drawer creates no hidden POST and no interval polling, and command-deck clicks still go through the explicit owner-triggered `/message` path. This is test-only hardening; no runtime, backend, UI authority, or data path changed.
- Phase 10.9CA Oom Sakkie Sentinel Single-Shot Contract Alignment is local-ready: added `modules/oom_sakkie/sentinel_single_shot_contract.py` as the Python source of truth for the Sentinel single-shot result identity and flag shape. The result store, review packet, runner, and tests now use the same mode/status/specialist/flag constants, while the migration SQL remains static and is contract-tested against those constants. This is hardening only: no env flag was enabled, no runner UI was added, no specialist tool execution was added, no farm data was written, and no public/customer output, deploy, Telegram, physical-control, or financial path was created.
- Phase 10.9CB Oom Sakkie Dispatch Execution Consumed-Once Live-PG Test is local-ready: added a DATABASE_URL-gated live Postgres test proving `idx_oom_sakkie_dispatch_execution_approval_consumed_once` rejects a second `consumed_by_single_dry_run_result` event for the same execution approval while still allowing normal review-note evidence. This strengthens the one-shot replay guarantee in CI without changing app behavior or adding any authority.
- Phase 10.9CC Oom Sakkie Consumed-Once Migration Assertion is local-ready: after Claude's review recognized 10.9CA but still listed the consumed-once live-PG test as open, the offline migration-content test now also pins `create unique index if not exists idx_oom_sakkie_dispatch_execution_approval_consumed_once`. This makes the DB one-shot guard visible in both the static test and the DATABASE_URL-gated live Postgres test. Test-only; no runtime behavior or authority changed.
- Phase 10.9CD Oom Sakkie Learning Influence Proposal Rail is local-ready with migration pending: added `supabase/migrations/202606100001_create_oom_sakkie_learning_influence_proposals.sql`, `modules/oom_sakkie/learning_influence_store.py`, protected learning-influence proposal routes, CI migration wiring, and live-PG rail coverage. Accepted agent learning evidence can now be converted into append-only `learning_influence_proposal_only` records for owner review. The rail forces `applies_learning_now = false`, `changes_prompt_now = false`, `changes_runtime_now = false`, `dispatch_enabled = false`, and `writes = false` at both DB and application layers, and events are limited to `approved_for_future_planning`, `rejected`, and `review_note`. This does not apply learning, change prompts, alter routing, enable runtime, dispatch specialists, run tools, write farm data, produce public/customer output, deploy, cut over Telegram, control hardware, or perform financial actions.
- Phase 10.9CE Oom Sakkie Learning Influence Status Tool is local-ready: added read-only `learning_influence_status` routing for self-learning / Sentinel suggestions / learning proposal questions. Oom Sakkie can now report how many learning influence proposals are waiting, approved for future planning, or rejected, while explicitly saying proposals do not change prompts, runtime, tools, or farm data. This is status-only and does not generate proposals from chat, apply learning, change runtime, run Sentinel, dispatch specialists, or write farm records.
- Phase 10.9CF Oom Sakkie Learning Influence Workbench UI is local-ready: added a `Learning Influence Proposals` panel to the System Workbench so accepted evidence can be converted into proposal records and reviewed from the kiosk. The `Prepare Proposals` button calls the existing protected append-only `from-accepted` route from an explicit owner click, then displays how many proposal records were created. Proposal cards show source result, specialist, proposed planning rules, latest event, and false apply/prompt/runtime/dispatch/write guard flags. Owner actions record only append-only review events (`approved_for_future_planning`, `rejected`, `review_note`). The first-screen approval console can surface pending learning proposals, but its button only opens the Workbench panel; it does not approve, generate, or apply learning. This does not change prompts, routing, runtime, tools, farm data, public/customer output, deploy, Telegram, physical controls, or financial behavior.
- Phase 10.9CG Oom Sakkie Learning Influence Browser Gate Hardening is local-ready: extended the VM browser smoke and Playwright real-browser spec so the learning influence `Prepare Proposals` POST and proposal review-event POST are proven explicit-owner-click actions. The tests also re-check that these clicks create no interval polling. This is test-only hardening; it adds no UI authority, no proposal consumer, no prompt/runtime change, no Sentinel runner wiring, no specialist dispatch, no farm-data write, no public/customer output, no deploy, no Telegram, no physical controls, and no financial behavior.
- Phase 10.9CH Oom Sakkie Owner Cockpit UI is local-ready: replaced the busy first-screen approval list with an `Owner Cockpit` that shows one primary decision, a compact next-in-line queue, an OSK ID search/jump box, and an `Audit Trail` button that opens the full System Workbench. The Workbench remains the complete traceability surface for build/patch/deploy/dry-run/learning details. The cockpit allows direct on-the-spot decisions only for append-only evidence/proposal review records: agent dry-run results can record `accepted_for_learning` or `rejected`, and learning influence proposals can record `approved_for_future_planning` or `rejected`. Build handoff, patch review, deploy decision, dry-run handoff, and proposal generation still open detailed Workbench sections and do not run from the cockpit. No Sentinel runner UI was added, no learning proposal consumer was added, no prompt/routing/runtime change was made, and no specialist dispatch, specialist tool execution, farm-data write, public/customer output, deploy, Telegram, physical control, or financial action was added. Verification passed: JS syntax checks, browser behavior smoke, focused Oom Sakkie suite at 293 tests with 1 live-DB skip, and full local unittest suite at 623 tests with 1 live-DB skip.
- Phase 10.9CI Oom Sakkie Owner Cockpit Decision Feedback is local-ready: after the owner accepted result `OSK-AGENT-DRYRUN-RESULT-C63AF980E948`, the backend review packet confirmed `latest_event.event_type = accepted_for_learning`, but the cockpit did not make the transition obvious enough. The cockpit event handlers now await the append-only result/proposal event response, refresh the result/proposal queues, and write a clear cockpit status message such as `accepted_for_learning recorded ... Evidence only; no runtime change.` This is feedback/refresh polish only; it adds no new endpoint, no Sentinel runner UI, no proposal consumer, no prompt/routing/runtime change, no specialist dispatch/tool execution, no farm-data write, no public/customer output, no deploy, no Telegram, no physical control, and no financial action. Verification passed: `node --check static/js/oomSakkie.js`, browser behavior smoke, and frontend route contracts at 28 tests.
- Phase 10.9CJ Oom Sakkie cockpit accepted-result proposal prep is local-ready: after a cockpit `Accept For Learning` click succeeds, the kiosk now calls protected `POST /api/oom-sakkie/agent-learning/influence-proposals/from-result` with the exact clicked `source_result_id`, and the backend only prepares a proposal if that dry-run result's latest event is `accepted_for_learning`. Existing bulk proposal prep remains in the Workbench. This prepares review-only learning influence records; it does not approve proposals, apply learning, change prompts/routes/runtime, run specialists, deploy, send, post, sell, trade, control equipment, or write farm data. Focused service/route/frontend tests and browser behavior smoke passed.
- Phase 10.9CK Oom Sakkie learning/cockpit review packet refresh is local-ready: `docs/00-start-here/CLAUDE_REVIEW_HANDOFF.md` now scopes the current review through 10.9CJ, includes the learning influence store/migration and Playwright browser behavior spec in the inspect list, records current local verification at 297 focused tests and 627 full tests, and records green GitHub Actions evidence for commit `613085b` (`Oom Sakkie Browser Behavior` run `27278530296`, `Oom Sakkie Audit Rails` run `27278529740`). This is review-readiness documentation only; no runtime behavior or authority changed.
- Phase 10.9CL Oom Sakkie owner review packet scope evidence is local-ready: the read-only `jarvis_owner_review_packet` now exposes current review metadata for `Oom Sakkie 10.6 through 10.9CK`, the exact Claude handoff prompt, learning/cockpit focus items, and recorded green CI run evidence. The chat/tool summary names the current scope and green CI gate count. Tests pin that the packet still has no learning influence consumer and does not apply learning, change prompts, or change runtime.
- Phase 10.9CM Oom Sakkie owner review packet chat gate is local-ready: `handle_message()` now has a regression test for `prepare Claude review`, proving the deterministic route selects `jarvis_owner_review_packet`, the answer names the `10.9CK` review scope and recorded green CI gate count, the safety note says no runtime authority is approved, and the visible Gatekeeper workspace still reports no agent run, dispatch, or write authority.
- Phase 10.9CN Oom Sakkie review packet final scope sync is local-ready: the read-only owner/Claude packet now scopes the current review through `10.9CM`, records green CI evidence for commit `5de5d9d` (`Oom Sakkie Browser Behavior` run `27314056906`, `Oom Sakkie Audit Rails` run `27314056931`), and the Claude handoff includes design checks for the packet evidence and `prepare Claude review` chat gate. This is metadata/docs/test hardening only; no learning proposal consumer, runtime unlock, specialist dispatch, tool execution, farm-data write, public/customer output, deploy, Telegram, physical-control, or financial path was added.
- Phase 10.9CO Oom Sakkie learning influence live-PG closure is local-ready: Claude reviewed 10.9CA-CM with verdict `pass` and asked to apply migration `202606100001_create_oom_sakkie_learning_influence_proposals.sql` plus live-PG coverage for the `from-result` 409 guard and idempotent existing-proposal path. The migration was applied with the reviewed script, a DATABASE_URL-gated test now proves non-accepted source results reject with 409, accepted source results create one proposal, and repeated calls return the existing proposal with `created_count = 0`, and route-contract tests pin success/409/idempotency response shapes. The review packet now scopes through `10.9CO`. This adds no learning consumer, prompt/routing/runtime change, dispatch, tool execution, farm-data write, public/customer output, deploy, Telegram, physical-control, or financial authority.
- Phase 10.9CP Oom Sakkie learning consumption threat-model packet is local-ready: after Claude passed 10.9CO and repeated the low-risk CI-evidence drift nit, the owner review packet now treats CI run IDs as dated recorded operator evidence rather than exact live HEAD proof, and Oom Sakkie exposes a read-only `learning_influence_consumption_readiness` tool plus protected `GET /api/oom-sakkie/agent-learning/consumption-readiness`. The packet lists allowed future scope, hard no-go scope, threat scenarios, rollback/idempotency concerns, and required gates for any future learning proposal consumer. This is threat-model/readiness only; no consumer, proposal application, prompt/routing/runtime change, hidden POST/polling, dispatch, specialist tool/LLM execution, farm write, public output, deploy, Telegram, physical-control, or financial authority was added.
- Phase 10.9CQ Oom Sakkie consumption audit rail blueprint is local-ready: after Claude passed 10.9CO/CP and asked to keep provenance/integrity plus blast-radius bounds in the next review, the read-only learning-consumption readiness packet now treats proposal text as untrusted LLM-derived input and requires one allowlisted target field plus a size-capped reviewable diff per future consumption. Oom Sakkie also exposes a read-only `learning_influence_consumption_audit_rail_blueprint` tool and protected `GET /api/oom-sakkie/agent-learning/consumption-audit-rail-blueprint` describing proposed request/event tables, allowlisted target contract, live-PG tests, and route tests for the future audit rail. This is blueprint-only; no migration, store, event writer, consumer, proposal application, prompt/routing/runtime change, hidden POST/polling, dispatch, specialist tool/LLM execution, farm write, public output, deploy, Telegram, physical-control, or financial authority was added.
- Phase 10.9CR Oom Sakkie consumption audit rail implementation is local-ready: after Claude passed 10.9CQ and cleared only the append-only rail implementation, added migration `202606110001_create_oom_sakkie_learning_influence_consumption_audit_rail.sql`, store `modules/oom_sakkie/learning_influence_consumption_store.py`, protected consumption request/event routes, CI migration wiring, offline guard tests, route tests, and live-PG coverage. A consumption request can be recorded only for a learning proposal whose latest event is `approved_for_future_planning`; each request is a one-target review-note artifact that treats proposal text as untrusted and preserves source provenance. The generic event route rejects `consumed_for_patch_proposal`; the DB still has a partial unique index proving one consumed marker per request for a later reviewed consumer path. Both new tables force `applies_learning_now`, `changes_prompt_now`, `changes_runtime_now`, `dispatch_enabled`, and `writes` false and reject update/delete. This adds no learning consumer, prompt/route diff application, runtime change, dispatch, specialist tool/LLM execution, farm write, public output, deploy, Telegram, physical-control, or financial authority.
- Phase 10.9CS Oom Sakkie learning consumer design static guard is local-ready: after Claude passed 10.9CR and identified `allow_consumed=True` as the single hinge to guard hardest, added a static AST regression test proving no production module calls `record_learning_influence_consumption_event(..., allow_consumed=True)`. Oom Sakkie also exposes a read-only `learning_influence_consumer_design_packet` tool and protected `GET /api/oom-sakkie/agent-learning/consumer-design-packet` answering the next consumer-review questions: only a future owner-approved design-review request may consume, first output remains `review_note_artifact_only`, proposal text stays untrusted, one target field is allowed, rollback/manual-application artifacts are required, and manual application remains outside the kiosk. This adds no consumer, no production `allow_consumed=True` caller, no applyable prompt/route diff, no runtime change, no dispatch, no specialist tool/LLM execution, no farm write, no public output, no deploy, no Telegram, no physical-control, and no financial authority.
- Phase 10.9CT Oom Sakkie allow-consumed static guard hardening is local-ready: after Claude passed 10.9CS and suggested defense-in-depth hardening, the AST guard now detects direct imports, aliased imports, module imports, module-attribute calls, positional fourth-argument overrides, `**kwargs`, and any `allow_consumed` value that is not literal `False`. The consumer-design packet wording now documents this stronger trip-wire. This is test/packet hardening only; no consumer, no production `allow_consumed` caller, no applyable prompt/route diff, no runtime change, no dispatch, no specialist tool/LLM execution, no farm write, no public output, no deploy, no Telegram, no physical-control, and no financial authority was added.
- Phase 10.9CU Oom Sakkie source-backed allow-consumed caller evidence is local-ready: after Claude passed 10.9CT, the consumer-design packet no longer hardcodes `allow_consumed_production_callers = []`. It now derives that field from the shared AST scanner used by the no-production-caller regression guard, and synthetic scanner coverage proves alias/module calls, positional fourth-argument overrides, `**kwargs`, and non-literal-false `allow_consumed` values are caught while literal `False` remains allowed. This is evidence/test/packet hardening only; no consumer, no production `allow_consumed` caller, no applyable prompt/route diff, no runtime change, no dispatch, no specialist tool/LLM execution, no farm write, no public output, no deploy, no Telegram, no physical-control, and no financial authority was added.
- Phase 10.9CV Oom Sakkie allow-consumed scanner resilience is local-ready: after Claude passed 10.9CU and flagged two low-priority robustness nits, the shared source-backed scanner now resolves the default `modules` scan root from the repo path instead of process CWD and returns explicit `:parse_error` findings for syntactically invalid scanned files. Tests prove the scan still works after changing CWD to a temporary directory and that parse errors are reported instead of raising from the packet route. This is scanner/test/packet hardening only; no consumer, no production `allow_consumed` caller, no applyable prompt/route diff, no runtime change, no dispatch, no specialist tool/LLM execution, no farm write, no public output, no deploy, no Telegram, no physical-control, and no financial authority was added.
- Phase 10.9CW Oom Sakkie read-only consumer design agreement is local-ready: after Claude passed 10.9CV and said the scanner thread is closed, the existing consumer-design packet now includes an explicit `consumer_design_review_agreement`. It pins the review-note artifact shape, required source provenance, forbidden patch/write/public-output fields, ordered `must_recheck_before_marker` sequence, failure behavior that writes no consumed marker, rollback artifact contract, and the future static-guard update requirement. It also hard-codes `implementation_authorized_now = false` and `allow_consumed_true_authorized_now = false`. This is design/test/packet hardening only; no consumer, no production `allow_consumed` caller, no applyable prompt/route diff, no runtime change, no dispatch, no specialist tool/LLM execution, no farm write, no public output, no deploy, no Telegram, no physical-control, and no financial authority was added.
- Phase 10.9CX Oom Sakkie consumed-once atomicity wording is local-ready: after Claude passed 10.9CW and noted the TOCTOU window between the future consumer's application read and consumed-marker write, the read-only design agreement now names DB partial unique index `idx_oom_sakkie_learning_consumption_consumed_once` as the authoritative atomic race guard. It also requires any unique-violation during the marker write to return `already_consumed` and produce no second review-note artifact. This is design/test/packet wording only; no consumer, no production `allow_consumed` caller, no applyable prompt/route diff, no runtime change, no dispatch, no specialist tool/LLM execution, no farm write, no public output, no deploy, no Telegram, no physical-control, and no financial authority was added.
- Phase 10.9CY Oom Sakkie review-note consumer implementation is local-ready: after the owner explicitly approved the 10.9CW/CX design agreement, added the first narrow learning-influence consumer at `modules/oom_sakkie/learning_influence_consumer.py` plus a protected review route. The consumer is the single reviewed `allow_consumed=True` caller, rechecks `approved_for_design_review`, source proposal `approved_for_future_planning`, target allowlist, and consumed-once state, then writes one consumed marker and returns a review-note artifact with source provenance, untrusted-proposal marking, rollback artifact, and manual-application-outside-kiosk flag. Repeats and DB unique violations return `already_consumed` with no second artifact. It does not apply learning, produce an applyable prompt/route/runtime diff, mutate prompts/routes/runtime/tools/farm data, create public output, deploy, touch Telegram, control hardware, or perform financial actions.
- Phase 10.9CZ Oom Sakkie allow-consumed call-site guard hardening is local-ready: after Claude passed 10.9CY with a low-risk nit, the shared scanner now separates unreviewed offenders from reviewed call sites, requires exactly one reviewed `allow_consumed=True` call site in the consumer, exposes the reviewed call site in the design packet, and matches reviewed paths by exact normalized suffix instead of substring. This is guard/test/packet hardening only; no second consumer, no applyable prompt/route/runtime diff, no runtime change, no dispatch, no specialist tool/LLM execution, no farm write, no public output, no deploy, no Telegram, no physical-control, and no financial authority was added.
- Phase 10.9DA Oom Sakkie browser voice capture and supervised consumer smoke is local-ready: after the owner reported Talk was not capturing reliably, the browser smoke now simulates Web Speech transcripts and proves `Talk` fills the input without sending while `Talk & Ask` sends only after a transcript plus the two-second cancel window. The kiosk now gives clearer microphone/browser diagnostics for blocked permission, unsupported browser, no speech, missing audio input, and recognition service/network failures. The owner-approved supervised live consumer smoke ran against configured Postgres through the protected route: first POST returned `201 learning_influence_review_note_consumer_only`, replay returned `409 already_consumed`, consumed marker count was exactly one, and all apply/prompt/runtime/dispatch/write flags stayed false. This adds no Telegram path, no second consumer, no applyable prompt/route/runtime diff, no runtime change, no dispatch, no specialist tool/LLM execution, no farm write, no public output, no deploy, no physical-control, and no financial authority.
- Phase 10.9DB/DC Oom Sakkie voice and read-only Telegram access is local-ready: the browser now prefers an env-gated backend push-to-talk STT fallback when configured, records only after `Talk` / `Talk & Ask`, stores no audio, leaves always-on mic/wake-word off, and keeps all write/dispatch/runtime flags false. A new token-gated `POST /api/oom-sakkie/channels/telegram/message` gateway accepts Telegram-update-shaped payloads, calls the same read-only backend with `channel=telegram_read_only`, and returns a reply payload for the caller/n8n to send; it does not send Telegram itself. Local smoke returned HTTP 200 with `sends_telegram = false`, `writes = false`, `dispatch_enabled = false`, and tool `farm_attention_summary`. This is an access path only, not Telegram bot cutover, public/customer output authority, write authority, dispatch authority, or physical-control authority.
- Phase 10.9DD Oom Sakkie Telegram gateway hardening is local: after Claude's DB/DC review, `telegram_read_only` is now a deterministic-only channel in `handle_message`, so Telegram ingress cannot call the LLM router or LLM answer composer even if LLM env flags are enabled. Gateway responses and policy report `can_trigger_outbound_llm = false`; token checks now use constant-time `hmac.compare_digest`; the policy documents long random token expectation. The STT record now explicitly acknowledges that enabled backend STT sends owner-clicked audio to the configured OpenAI transcription endpoint while storing no audio locally. No direct Telegram send/cutover, write authority, runtime dispatch, applyable diff, public/customer output, physical control, or financial action was added.
- Phase 10.9DE Oom Sakkie Telegram public-exposure hardening is local: after Claude passed DB-DD with nits, the gateway now refuses to enable unless the token is at least 32 characters and `OOM_SAKKIE_TELEGRAM_ALLOWED_USER_IDS` is configured, repeated bad token attempts trigger a fail-closed auth lockout, and the smoke script requires the same configuration. A true no-HTTP-egress regression patches `urllib.request.urlopen` and proves `telegram_read_only` still makes no outbound HTTP call with all LLM env flags enabled. Gateway policy/result now state `records_audit_trace = true` while keeping `writes = false` for farm/control/public-output writes. No direct Telegram send/cutover, write authority, runtime dispatch, applyable diff, public/customer output, physical control, or financial action was added.
- Phase 10.9DF Oom Sakkie Telegram exposure preflight is local: after Claude passed 10.9DE and noted the in-process/global rate-limit caveat as acceptable for the trial, added protected `GET /api/oom-sakkie/channels/telegram/exposure-preflight`. It reports `private_test_ready` from automated checks (enabled flag, 32+ char token, configured allowed user IDs, constant-time token compare, auth lockout, deterministic-only/no-LLM/no-send/no-write boundaries) and reports `public_exposure_ready` only after explicit operational confirmations for TLS termination and acceptance/replacement of the current in-process global rate limiter. This is visibility/checklist only; it does not expose the route, send Telegram messages, cut over the bot, write farm/control/public output, dispatch, apply diffs, or change runtime authority.
- Phase 10.9DG Oom Sakkie private Telegram relay smoke helper is local: after Claude passed 10.9DF, added `scripts/oom_sakkie_telegram_private_relay_smoke.py`. The helper first checks the protected exposure preflight, then posts one Telegram-shaped message to the read-only gateway and verifies the returned reply payload is caller-send only with `sends_telegram`, outbound LLM, writes, dispatch, prompt/runtime change, physical-control, and public-output flags false. This is a test/operations helper only; it does not send Telegram, configure webhooks, expose Flask, cut over the bot, add write authority, dispatch, or apply runtime changes.
- Phase 10.9DH Oom Sakkie private relay smoke URL guard is local: after Claude passed 10.9DG and noted the token would traverse cleartext if an operator pointed the smoke helper at a non-TLS remote URL, the helper now refuses remote plain-HTTP base URLs before sending the token. It allows local `http://127.0.0.1` / `http://localhost` and remote/private `https://...` only. This is operator-smoke hardening only; it does not send Telegram, configure webhooks, expose Flask, cut over the bot, add write authority, dispatch, public output, or runtime changes.
- Phase 10.9DI Oom Sakkie n8n backend read-only relay contract is local: added `docs/04-n8n/workflows/2.0B - Oom Sakkie Backend Read-Only Relay/` as an inactive callable workflow template for the existing `2 - The GateKeeper` to review later. It has an `Execute Workflow Trigger`, calls the Flask read-only Telegram gateway, validates no-send/no-LLM/no-write/no-dispatch/no-runtime flags, and returns a guarded `send_allowed` / `chat_id` / `telegram_text` payload. It intentionally has no Telegram Trigger, no Telegram send node, no hardcoded token/chat ID, and no second normal-message owner. This is a relay contract only; it does not send Telegram, wire GateKeeper, expose Flask, cut over the bot, add write authority, dispatch, public output, or runtime changes.
- Phase 10.9DJ Oom Sakkie n8n relay transport guard is local: after Claude passed 10.9DI with one low-risk nit, the `2.0B` relay now mirrors the private smoke helper's transport protection. It refuses remote plain-HTTP `OOM_SAKKIE_GATEWAY_BASE_URL` values before the bearer token is used, while allowing HTTPS remotes and local HTTP on localhost/127.0.0.1/::1. This is workflow-template hardening only; it does not send Telegram, wire GateKeeper, expose Flask, cut over the bot, add write authority, dispatch, public output, or runtime changes.
- Phase 10.9DK Oom Sakkie n8n relay import preflight is local: added `scripts/oom_sakkie_n8n_relay_contract_check.py`, a local-only validator for the committed `2.0B` workflow export and README. It verifies inactive import, no Telegram Trigger, no Telegram send node, backend gateway path/env-token references, no committed token/chat ID, localhost/HTTPS transport guard, no-authority flag validation, and README guard wording. The frontend contract test calls the same validator. This is import-safety tooling only; it makes no n8n, Telegram, Flask, OpenAI, Google Sheets, or Supabase call and does not wire GateKeeper, send Telegram, expose Flask, cut over the bot, add write authority, dispatch, public output, or runtime changes.
- Phase 10.9DL Oom Sakkie learning backlog clarity is local: the System Workbench now labels closed dry-runs, accepted evidence, and reviewed learning proposals as append-only audit backlog instead of active self-learning. Dry-run requests that already have recorded results move into the closed request backlog in the Workbench, matching the Owner Cockpit's active queue. The copy now states accepted evidence does not retrain agents or change behavior by itself. This is UI clarity only; it does not delete/archive records, apply learning, change prompts/routes/runtime/tools/farm data, send Telegram, wire GateKeeper, dispatch, deploy, control hardware, or perform financial actions.
- Phase 10.9DM Oom Sakkie GateKeeper backend relay wiring plan is local: after owner approval to test live Telegram replies, added a manual n8n UI wiring plan for replacing only GateKeeper's normal-message call target with imported inactive `2.0B`. The plan keeps GateKeeper as the single Telegram Trigger owner and the only workflow that sends the owner reply, requires backend/n8n env preflight, validates `sends_telegram=false`, no LLM egress, no writes, no dispatch, no prompt/runtime changes before sending, and records rollback steps. Workflow contract tests pin the current GateKeeper pre-wiring shape and the no-trigger/no-send `2.0B` relay shape. No live n8n API update, GateKeeper replacement export, second listener, callback-route change, write authority, dispatch, public/customer broadcast expansion, runtime change, deploy, physical control, or financial action was added.
- Phase 10.9DN Oom Sakkie 2.0B manual execution helper is local: added `scripts/oom_sakkie_n8n_relay_manual_test.py` so the owner can generate a token-free manual n8n payload for uploaded `2.0B` and validate copied execution output before GateKeeper wiring. The validator requires `success=true`, `send_allowed=true`, caller-handled reply transport, non-empty chat/text, and all authority flags false. This is operator tooling only; it makes no n8n, Telegram, backend, OpenAI, Google Sheets, or Supabase call and adds no send, cutover, write, dispatch, runtime, prompt, tool, farm-data, deploy, control, public-output, or financial authority.
- Phase 10.9DO Oom Sakkie n8n live state inspector is local: added a read-only `scripts/oom_sakkie_n8n_live_state_check.py` that uses the configured n8n API key without printing secrets. It confirmed the live GateKeeper exists and is active, `2.0B` exists, and `2.0B` has no Telegram Trigger or Telegram send node. This is inspection only; no workflow update, manual execution, Telegram send, GateKeeper wiring, write, dispatch, runtime, prompt, tool, farm-data, deploy, control, public-output, or financial authority was added.
- Phase 10.9DP Oom Sakkie Render gateway smoke hardening is local: after the owner uploaded `2.0B`, added the n8n env vars, and wired GateKeeper to `2.0B`, the read-only live-state check now confirms GateKeeper targets `2.0B_backend_relay` while `2.0B` still has no Telegram Trigger or Telegram send node. The gateway smoke script now reuses the first allowed Telegram user ID by default and refuses remote plain-HTTP URLs before sending the bearer token. The owner-provided Render base URL is correct; the Render gateway smoke reached `/api/oom-sakkie/channels/telegram/message` and failed closed with `503 telegram_gateway_disabled`, so the remaining blocker is Render env configuration/restart: enable `OOM_SAKKIE_TELEGRAM_GATEWAY_ENABLED=1`, set the same gateway token, and set comma-separated allowed Telegram user IDs for Charl, mother, and father.
- Phase 10.9DQ Oom Sakkie Render gateway live-ready smoke is verified: after the owner enabled the Render gateway env and redeployed, the public Render gateway smoke returned HTTP 200 `answered`, used `farm_attention_summary`, and kept `sends_telegram=false`, `can_trigger_outbound_llm=false`, `writes=false`, and `dispatch_enabled=false`. The live n8n check still shows GateKeeper targeting `2.0B_backend_relay`, and `2.0B` still has no Telegram Trigger or Telegram send node. The next live check is one owner Telegram message through GateKeeper, expecting exactly one caller-sent read-only Oom Sakkie reply.
- Phase 10.9DR Oom Sakkie n8n Cloud variable compatibility fix is local-ready: the live `2.0B` error came from Code-node access to `$env` being denied in n8n Cloud. The workflow export now uses n8n Variables through `$vars` for `OOM_SAKKIE_GATEWAY_BASE_URL` and `OOM_SAKKIE_TELEGRAM_GATEWAY_TOKEN`, the HTTP node reads the token directly from `$vars`, and the workflow no longer emits `gateway_token` into item JSON. Contract tests now fail if `$env` returns to the workflow. The live `2.0B` must be re-imported/updated and the two n8n Variables must exist before the Telegram owner test is retried.
- Phase 10.9DS Oom Sakkie 2.0B request-ready gate is local-ready: the follow-up live `URL parameter must be a string, got undefined` error happened because `relay_env_not_ready` output still continued into the HTTP node without `gateway_url`. The workflow now has `IF - Gateway Request Ready` between Build and HTTP; setup failures route directly to the validator/failure output and do not call HTTP. The n8n Variable `OOM_SAKKIE_GATEWAY_BASE_URL` must be a full HTTPS URL such as `https://amadeus-pig-tracking-system.onrender.com`, not a bare domain or quoted value.
- Phase 10.9DT Oom Sakkie 2.0B base URL normalizer is local-ready: the Build node now strips wrapping quotes and invisible paste characters, extracts a pasted full `http(s)://...` URL when possible, and returns a safe `base_url_diagnostic` if validation still fails. This should make the next live retry either pass or show exactly what public URL shape n8n is seeing, without exposing the bearer token.
- Phase 10.9DU Oom Sakkie backend-owned direct Telegram is local-ready: after owner approval to move Oom Sakkie off n8n, added `POST /api/oom-sakkie/channels/telegram/direct-webhook`. It is default-off, requires `OOM_SAKKIE_TELEGRAM_DIRECT_ENABLED=1`, `OOM_SAKKIE_TELEGRAM_DIRECT_SEND_ENABLED=1`, a configured Telegram bot token, a 32+ character `X-Telegram-Bot-Api-Secret-Token` webhook secret, and `OOM_SAKKIE_TELEGRAM_ALLOWED_USER_IDS`. Allowed owner messages run through `channel=telegram_read_only`, so they stay deterministic and cannot trigger outbound LLM calls; the backend then sends the owner Telegram reply through Telegram `sendMessage`. This removes n8n from the Oom Sakkie owner chat path once the webhook is set, while still adding no farm/control write authority, dispatch, runtime/prompt change, specialist tool execution, customer/public broadcast, physical control, or financial action. Added `scripts/oom_sakkie_telegram_direct_webhook.py` for webhook info/set/delete and `scripts/oom_sakkie_telegram_direct_smoke.py` for a guarded smoke.
- Phase 10.9DV Oom Sakkie direct Telegram activation layer is local-ready: added review-gated `GET /api/oom-sakkie/channels/telegram/direct-parity` to show what the backend direct bot carries over from the n8n relay and what is still intentionally not built. Direct Telegram now handles `/start`, `/help`, `/brief`, `/attention`, `/gates`, `/agents`, `/approvals`, `/progress`, and `/learning` as owner menu/shortcut commands, formats Telegram replies with the check used and safety footer, and adds `scripts/oom_sakkie_telegram_daily_brief.py` plus `send_daily_brief_to_allowed_owners()` for explicit script/scheduler-invoked daily briefs. The proactive sender is default-off behind `OOM_SAKKIE_TELEGRAM_PROACTIVE_ENABLED=1` and `OOM_SAKKIE_TELEGRAM_DAILY_BRIEF_ENABLED=1`, has no in-app background loop, sends only to `OOM_SAKKIE_TELEGRAM_ALLOWED_USER_IDS`, uses `telegram_read_only`, and keeps LLM egress, farm/control writes, dispatch, prompt/runtime changes, physical controls, and financial action false.
- Phase 10.9DW Oom Sakkie live Telegram formatting polish is local-ready: after the owner tested `/help`, `/brief`, `/attention`, `/gates`, and `/agents` successfully, direct Telegram now returns compact structured layouts for `jarvis_daily_command_brief`, `jarvis_safety_gate_board`, and `agent_command_center` instead of dense kiosk paragraphs. The service response exposes bounded `tool_context` from existing `llm_context` so Telegram formatting can use structured sections without parsing prose. Generic/free-text answers still use the normal formatter. This is presentation-only and keeps direct Telegram owner-only, deterministic-only, no outbound LLM, no farm/control write, no dispatch, no runtime/prompt change, no physical control, and no financial action.
- Phase 10.9DX Oom Sakkie agent/Sam direction is logged: Oom Sakkie/Jarvis remains the owner orchestrator and backend command center; specialist agents should first become backend-owned draft/review-only lanes with narrow tool contracts and audit records. Sam should not immediately take over or replace the Chatwoot/n8n sales workflows: keep Chatwoot/WhatsApp and the order-steward workflows as customer-facing transport and proven order plumbing while a future backend Sales/Sam agent starts as a draft/recommendation layer. Customer sends, order writes, quote/document sends, irrigation control, and any specialist LLM/tool execution remain separate reviewed gates.
- Phase 10.9DY Oom Sakkie owner sales offer brief is local-ready: added dedicated `sales_offer_brief` routing plus direct Telegram `/offer`. It builds a Ledger-shaped owner-review brief from the existing read-only sales dashboard and meat planning context, including target, stock basis, owner checks, and approval question. It is not customer-facing copy and explicitly does not send messages, create quotes, reserve or change stock, update orders, run specialist LLM/tools, dispatch, or write farm/control/customer data. This is the first practical sales-agent lane, still review-only.
- Phase 10.9DZ Oom Sakkie daily brief dry-run preview is local-ready: `scripts/oom_sakkie_telegram_daily_brief.py --dry-run` now builds the same direct Telegram daily brief text, reports the allowed-recipient count, and prints the no-authority flags without calling Telegram `sendMessage`. The normal script path still sends only when the existing direct/proactive/daily env gates are enabled. This is operations safety tooling only; no scheduler, hidden send, write, dispatch, runtime change, physical control, customer output, or financial action was added.
- Phase 10.9EA Oom Sakkie owner-review customer draft rail is local-ready: added `sales_customer_draft` routing plus direct Telegram `/draft`. It turns the current owner offer brief into short buyer-interest copy with placeholders and owner checks. It is marked `DRAFT_ONLY`, counted separately from write/authority tools, and explicitly does not call Chatwoot, n8n, WhatsApp, Telegram customer chats, or public channels; it creates no quote/order/reservation/price/stock change and performs no dispatch, specialist LLM/tool execution, runtime change, farm/control write, or financial action.
- Phase 10.9EB Oom Sakkie Ledger sales agent lane is local-ready: added env-gated `ledger_sales_agent` plus direct Telegram `/ledger`. When `OOM_SAKKIE_LEDGER_AGENT_ENABLED=1`, `OPENAI_API_KEY`, and `OOM_SAKKIE_LLM_ROUTER_MODEL` are configured, Ledger may call the OpenAI-compatible chat-completions endpoint with capped owner sales/meat context only through the explicit owner `/ledger` command path to produce owner-review sales strategy, buyer-copy improvements, owner questions, risks, and a next action. When disabled, misconfigured, or not explicitly authorized for that request, it fails closed without a network call. The output is `DRAFT_ONLY`; it cannot send customer messages, call Chatwoot/n8n/WhatsApp, create quotes/orders/reservations, change stock, dispatch agents, change prompts/runtime, write farm/customer/control data, or perform financial/physical actions. Focused Oom Sakkie service/routes/frontend tests passed at 393 tests, 7 skipped.
- Phase 10.9EC Oom Sakkie sales campaign rail is local-ready: added append-only `oom_sakkie_sales_campaigns` and `oom_sakkie_sales_campaign_events` migration plus `sales_campaign_store`, review-gated `/api/oom-sakkie/sales-campaigns` routes, read-only `sales_campaign_status`, Telegram `/campaigns`, and Ledger campaign recording after a successful `/ledger` LLM advisory run. Campaign records hold the opportunity, draft, questions, risks, and next action as owner-review queue state so Jarvis can surface sales work beyond Telegram. The tables and app layer force customer send, Chatwoot/n8n calls, quote/order creation, stock changes, dispatch, runtime/prompt changes, farm-data writes, public output, and physical controls false. The daily command brief treats campaign status as optional until the migration is applied, then can include campaign review actions. Focused Oom Sakkie service/routes/frontend tests passed at 403 tests, 7 skipped.
- Phase 10.9ED Oom Sakkie sales campaign approval and outreach draft queue is local-ready with live migration applied: extended the sales rail with append-only `oom_sakkie_sales_outreach_drafts`, review-gated `/api/oom-sakkie/sales-outreach-drafts`, review-gated `POST /api/oom-sakkie/sales-campaigns/<campaign_id>/outreach-drafts`, read-only `sales_outreach_draft_queue`, Telegram `/drafts`, and owner-only Telegram `/approve_campaign`. `/approve_campaign` approves the first waiting campaign by writing an append-only `approved_for_customer_outreach` event, then queues an internal customer outreach draft for owner review. It still does not send customers, call Chatwoot/n8n/WhatsApp, create quotes/orders/reservations, change stock, dispatch agents, change runtime/prompts, write farm data, create public output, or perform physical/financial action. The live Supabase migration was applied and verified: campaigns read `200 ok` with 1 campaign, outreach drafts read `200 ok` with 0 drafts before owner approval. Focused Oom Sakkie service/routes/frontend tests passed at 410 tests.
- Phase 10.9EE Oom Sakkie Ledger Sales Workbench and send-design rail is local-ready with live migration applied: extended the sales rail with append-only `oom_sakkie_sales_send_design_requests`, review-gated `GET /api/oom-sakkie/sales-send-design-requests`, review-gated `POST /api/oom-sakkie/sales-outreach-drafts/<draft_id>/send-design-requests`, read-only `sales_send_design_status`, deterministic routing for send-design queue questions, and a local Ledger Sales Workbench inside `/oom-sakkie` showing campaigns, outreach drafts, and Sam/Chatwoot send-design requests. Owner-clicked web actions can approve the first campaign into an outreach draft and prepare a send-design request, but startup still performs no hidden POSTs. Live Supabase migration was reapplied, and an idempotent live smoke created `OSK-SALES-SEND-DESIGN-BCD9E5D9C2DD80C9` from draft `OSK-SALES-DRAFT-605CF5027CC5BC44` with `created_count=1`; all customer-send, Chatwoot/n8n, quote/order, stock, dispatch, runtime/prompt, farm-write, public-output, physical-control, and financial-action flags remained false. Focused Oom Sakkie service/routes/frontend tests passed at 417 tests, plus JS syntax and browser behavior smoke passed.
- Future Financial Agent idea is logged as research-only and hard-locked for later: no trading, no broker/exchange/account access, no custody/funds/payment movement, no model-driven orders, no profit-share automation, and no financial advice/recommendations. Any future finance agent requires separate owner + Claude review plus legal/regulatory/tax/risk review and dedicated append-only gates before even paper-trading or simulation is considered.
- `docs/00-start-here/CLAUDE_REVIEW_HANDOFF.md` now has a self-contained current-review packet for the Oom Sakkie 10.6 through 10.9EE batch. In Claude Code, the owner can say `Read docs/00-start-here/CLAUDE_REVIEW_HANDOFF.md and run the current review`; the file itself lists scope, files, commands, design questions, out-of-scope items, and deliverable format. The next review should focus on the env-gated `/ledger` LLM advisory lane, append-only sales campaign/draft/send-design rail, local Ledger Sales Workbench, and live send-design smoke before any actual customer send, order write, stock reservation/change, applyable diff, runtime dispatch, irrigation control, or non-owner bot expansion.
- Oom Sakkie prompt backbone was added on 2026-06-06 at `docs/01-architecture/OOM_SAKKIE_AGENT_PROMPT_LIBRARY.md`. It includes the master orchestrator, kiosk UI, tool-selection, specialist farm-agent, Claude review prompts, and owner-provided Trillion-style playbooks adapted for Amadeus. The captured playbooks cover code monitoring, cloud/local memory, read-only Supabase access, handoff docs, mobile voice PWA, personality persistence, voice latency, security hardening, design sub-agent, living self-knowledge, cost dashboard, and sub-agent factory. Trillion's public site was used for pattern inspiration, but its locked/private prompt playbook was not copied. Immediate recommendation remains read-only orchestrator plus `/oom-sakkie` kiosk first.
- Supabase Security Advisor warning received on 2026-05-27 for `rls_disabled_in_public`. RLS hardening migration `supabase/migrations/202605270001_enable_rls_on_public_tables.sql` was applied by owner and verified on 2026-05-30. Security Advisor now shows `0 errors` and `0 warnings`; remaining `RLS Enabled No Policy` rows are informational and expected because backend-owned `DATABASE_URL` access remains the intended path and browser/Data API access should stay closed by default.
- New Phase 10 follow-up notes are captured in `NEXT_STEPS.md`: herd tile total/breakdown audit, Telegram digest/reminders for farm attention, practical alert timing/rain-summary rules, alert message formatting polish, weather/solar dashboard symbols, future task/reminder/project management, and possible Windy weather-station integration research.
- Herd dashboard count audit is deployed and browser-verified: backend herd counts reconciled to `103` on-farm pigs once `Gilts`, `Piglets`, and `Growers` were included in the visible tile.
- Farm attention Telegram digest backend slice is deployed and smoke-verified: `GET /api/reports/farm-attention-summary` combines existing order attention and litter attention into a read-only digest contract for n8n. Production returned one current item, `LIT-2026-8A0F` for piglet tag numbers, with no order attention items. It sends no Telegram messages and writes nothing to Supabase or Google Sheets.
- Farm attention digest workflow is live-verified: `ALERT - Farm Attention Digest` calls the backend summary endpoint, filters empty digests, suppresses repeated content with workflow static data for non-manual executions, and sends only to Charl. Manual executions `49137` and `49138` sent the expected digest for `LIT-2026-8A0F`; manual execution `49136` proved the no-output gate; scheduled execution `49154` stopped at `Code - Extract Sendable Digest` with zero output and no Telegram send.
- `/sales/slaughter` payment update UX is locally browser-checked: the awkward browser-prompt update path is replaced with an in-page update panel for final amount, payment status/date, payment method, sale status, carcass weight, updated-by, and update note. Backend payment rules are unchanged.
- `/sales/slaughter` wide layout/table polish is local: the page now uses a wider operational canvas with create form and transaction ledger side by side on desktop, and the transaction table groups related fields for easier scanning. Browser review and deploy remain pending.
- `/sales/slaughter` table/detail behavior is local: newest created transactions sort first, completed/paid rows are closed with no update/cancel actions, open-row action buttons are smaller, rows open a read-only sale detail page, and `GET /api/sales-transactions/<sale_id>` returns one sale with items.
- Oom Sakkie asset route correction is local: the agent portrait metadata intentionally uses browser URLs under `/assets/agents/`, while files live under `static/assets/agents/`. Flask now serves `/assets/<path>` from `static/assets/`, and frontend contract tests assert both the registry JSON and Oom Sakkie portrait return HTTP 200. Without this route the kiosk falls back to initials such as `OS` even when the portrait files exist. ElevenLabs voice IDs remain metadata only until a separate approved TTS/proxy integration is built; current speech buttons use browser speech synthesis.
- Oom Sakkie farm-command UI reset is local: `/oom-sakkie` now follows the owner-provided reset brief in `external_sources/CODEX_FARM_UI_RESET_BRIEF.md` with a warm fixed-viewport farm kiosk, centered Oom Sakkie portrait, first-screen at-a-glance and priority rails, all eight specialist agents visible in the dock, and the System Workbench demoted to a secondary drawer. The home screen keeps the existing no-hidden-POST/no-autonomy safety model; customer sends, payments, hardware control, farm writes, and specialist execution remain locked behind existing approval rails.
- Oom Sakkie center-stage polish is local: the answer bubble is now capped with internal scrolling for long replies, the Speak / Daily Brief / Ask Anything controls are always visible in their own strip, and the center background/portrait layering was softened so the farm-command screen does not hide action buttons behind the command deck or dock.
- Oom Sakkie target-kiosk correction is local: `/oom-sakkie` now follows `external_sources/CODEX_FARM_UI_TARGET_SPECIALIST_WORKSPACE_BRIEF.md` as the active visual target. Desktop uses a fixed `100dvh` home/specialist layout with no body scroll at 1366x768, a command bar between the hero stage and warm agent dock, all eight agents visible, warm farm-card dock tiles, and specialist workspaces rendered as readable main-screen farm panels instead of transparent overlays on top of Oom Sakkie. The uploaded Oom Sakkie portrait files were also corrected from JPEG bytes with `.png` names into valid PNG files so browser rendering does not fall back to initials or blank portrait space.
- 2026-06-16 owner decision: the Oom Sakkie UI is not passed and should remain parked for now. Do not treat the latest screenshots or local CSS/template changes as accepted production UI; resume only when the owner explicitly returns to the visual pass.
- 2026-06-16 Sam meat-intake live smoke is mid-pass: first WhatsApp smoke saved tracking-only lead `OSK-SALES-LEAD-D583E2649366146A`; the first reply incorrectly asked live-pig weight range and was fixed by preserving the meat-preorder reply instruction and adding meat-preorder prompt rules. The second reply stayed in the meat lane but asked to reconfirm Riversdale because the extractor did not carry facts forward and treated `not a live pig` as mixed live intent. The repo export and live n8n workflow `V73HaIqVpzv44SFc` now extract Sam meat-intake facts from `ConversationHistory` plus the current message and strip negated live-pig phrases before live-intent detection. Next smoke message is `Yes, Riversdale is correct.`; expected result is no live-pig wording, no price/timing/deposit/order promise, and owner/Ledger tracking only.
- 2026-06-16 Sam meat-intake follow-up build is local-ready: the `Yes, Riversdale is correct.` smoke passed the lane guard and carried forward Charl, half carcass, Set A, Riversdale, next available week, collection, and EFT into the workflow payload while reusing lead `OSK-SALES-LEAD-D583E2649366146A`. Backend code now records every accepted Sam meat-intake handoff as an append-only `status_observed` fact event and Ledger preorder contract readback merges those events with the immutable lead row, so later facts like collection/EFT can become visible without updating the lead row. Cut-menu knowledge remains intentionally blocked until Charl supplies approved Set A/Set B/etc. content; Sam must not invent cut details.
