# Oom Sakkie Daily Farm Manager Loop handover

## Mission truth

Source integration is in progress. This document must not be used to claim a
business outcome until exact-merge deployment, provider-confirmed delivery and
replay verification are recorded here.

The authoritative production owner remains Telegram user/private-chat
`5721652188`. Mum and Dad remain unconfigured. No family, farm, customer,
financial or hardware authority is added by the daily loop.

## Fertilizer commissioning continuation

Telegram 3493 (`Yes, I'm at the fertilizer valve`) was authenticated at
`2026-08-10T06:51:14Z` and bound to the existing lifecycle
`OOM-ROOTLINE-FERTILIZER-CONFIG-20260809`. The deployed boundary stopped at
`specialist_accepted`: no provider send/edit and no hardware command followed.

The reusable correction composes the existing ROOTLINE controller readback,
typed auxiliary eligibility, durable single-use claim and auxiliary execution
coordinator. A fresh contextual answer may start only CH2 on device
`100204d497`, with one ON attempt and a 300-second native fail-stop. CH1 and
unrelated channels receive no authority. Physical recirculation, pump behaviour
and unrelated-output observations are retained separately; mixing can be marked
commissioned only after those facts and verified shutdown. Injection remains
disabled.

If owner presence expires before deployment, the only permitted new question on
the existing lifecycle is: `Are you at the fertilizer valves now for the
five-minute mixer test?`

## Daily manager lifecycle

The existing ROOTLINE scheduler owns a single daily SAST identity:
`OOM-DAILY-FARM-MANAGER-YYYY-MM-DD`. Its first due boundary is 06:45 SAST, the
existing tick immediately before 07:00. It loads HERDMASTER, ROOTLINE, current
litters and governed sale transactions concurrently. Optional source failure is
section-local.

The packet retains every supported task durably and presents at most three
priorities plus a compact watch section and one grouped question. The approved
read-only semantic LLM may reorder only the exact supplied task identities;
deterministic validation rejects additions, omissions or duplicates. Identity,
chronology, permissions, writes, commitments, replay and hardware safety remain
deterministic.

Current watchers include active welfare/mortality work, Monday weighing and
farrowing preparation from HERDMASTER, overdue/current litter work, duplicate
current-litter conflicts, ROOTLINE material work and current sale readiness.
Teena's active `LIT-2026-1350` with expected weaning date 2026-08-06 is surfaced
as overdue unless later canonical evidence proves completion. Zigay's two
current Active litter rows are surfaced as one data-quality conflict; duplicate
weaning tasks are suppressed until that source conflict is resolved.

## Replay and recovery

- The daily claim is bound to local date plus stable material task content.
- An unchanged round sends and edits nothing.
- Provider-ambiguous delivery is quarantined and never retried.
- Every task is recorded as detected and then presented only after confirmed
  provider delivery.
- An active mixer execution suppresses B/C controller acquisition until the
  mixer reaches a terminal outcome.
- Scheduler recovery verifies native shutdown and uses only bounded repeatable
  OFF; it never retries ON.

## Acceptance still required

1. Independent operations/CX and backend/security/privacy/authority approval.
2. Exact-head CI, normal merge, exact-merge CI and exact deployment lineage.
3. Recover the existing fertilizer lifecycle without repeating setup facts.
4. Complete one supervised mixer proof or record one precise scheduler-owned
   commissioning-specific Hold.
5. Produce one provider-confirmed current daily brief containing Teena and all
   other supported current work proportionately.
6. Prove replay and unchanged reassessment produce zero duplicate messages,
   tasks, commands and protected writes.

