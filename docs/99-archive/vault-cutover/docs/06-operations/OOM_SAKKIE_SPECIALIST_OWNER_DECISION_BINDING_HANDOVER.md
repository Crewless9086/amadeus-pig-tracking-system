# Oom Sakkie specialist owner-decision binding handover

Status: SOURCE-READY / UNDEPLOYED
Date: 2026-07-31

## Outcome

The Owner Attention Queue now has one closed typed specialist boundary instead
of assuming every protected decision belongs to SAM Livestock. Existing SAM
projection, cards and callbacks retain their prior path. Unsupported specialist
or decision types fail closed.

The first non-SAM binding is BEACON's exact organic-publication proposal
`BEACON-PROPOSAL-18DEAAD8E896A87FE961F45B`. Oom Sakkie can present and consume
the owner decision, but has no publication, Meta, customer-contact, advertising,
boosting or spending authority.

## Authoritative caption bytes

The caption is pinned from the UTF-8 bytes in
`C:\tmp\oom-sakkie-beacon-organic-proposal-handover-20260731.md`, whose
SHA-256 at preparation was
`9209fd3d885ac0657818b3fb067ccb3fb1f69e11f94b8019a7770f9310739e0b`.

- UTF-8 byte count: `85`.
- Caption SHA-256:
  `58a60223599365b90803570909e09f3828c32768d8b27470dc1304ff27fc17d4`.
- Terminal bytes: `2e20f09f90b7` (period, space, pig emoji).
- Mutated no-emoji proposal identity
  `BEACON-PROPOSAL-9971D111AB23533027A45463` is explicitly rejected.

## Closed specialist binding

Every specialist decision binds:

- specialist identity and supported decision type;
- deterministic external identity and compact Telegram callback token;
- exact evidence and authoritative chronology;
- closed owner choices and specialist outcome callbacks;
- next-action ownership;
- expiry and authoritative revalidation;
- exact-once receipt and in-place, buttonless resolution.

The BEACON binding additionally pins the asset/hash, Library Accept and
public-use event identities, caption bytes/hash, `facebook_organic`, exact
single-image order, zero spend, and `2026-08-01 08:00-09:00 SAST` timing.
Authenticated preview references are transient presentation data and are not
persisted into the binding or audit event.

Approval produces only a BEACON-owned, bounded publication-handover outcome.
Correction and decline produce no public-action authority. All outcomes keep
`publish`, Meta/customer calls, advertising, boosting and spending false.

## Verification

- Focused queue/adapter/specialist suites: 52 passed.
- Full Oom Sakkie discovery: 605 passed, 7 skipped.
- Coverage includes UTF-8/emoji serialization and Telegram rendering,
  deterministic identity, altered bytes, unsupported specialists, stale and
  expired decisions, duplicate cards, replay, exact-once consumption,
  in-place resolution, BEACON callback authority and SAM regression behavior.

## Required production proof

After independent review, exact-head CI, normal merge, exact-merge CI and exact
deployment lineage:

1. revalidate the exact BEACON asset, private storage/readback and authenticated
   preview, both approval events, claims and absence of correction/publication;
2. confirm no existing proposal/card identity;
3. deliver one provider-confirmed Telegram card with three choices;
4. persist the exact card/message identity once;
5. replay the delivery intent and prove zero additional cards;
6. preserve summary `3142`, SAM Level 1, PR #642 and all unrelated runtime;
7. release the serialized lane without waiting for the owner response.

No Telegram send, deployment, publication or customer/farm/media mutation was
performed by this source preparation.
