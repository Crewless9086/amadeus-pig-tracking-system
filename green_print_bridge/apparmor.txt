#include <tunables/global>

profile amadeus-green-print-bridge flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/nameservice>
  #include <abstractions/ssl_certs>
  signal,
  capability chown,
  capability setgid,
  capability setuid,
  capability dac_override,
  capability net_bind_service,
  network inet stream,
  network inet6 stream,
  # The pinned Home Assistant base image owns PID 1 through S6. Keep this
  # startup surface explicit so the custom profile cannot block the inherited
  # OCI entrypoint before the bounded Green initializer is reached.
  /init rix,
  /bin/sh ix,
  /bin/busybox ix,
  /command/** ix,
  /package/admin/execline*/** rix,
  /package/admin/s6*/** rix,
  /package/prog/skalibs*/** rix,
  /etc/s6-overlay/** r,
  /etc/services.d/** r,
  /run/ rw,
  /run/s6/ rwk,
  /run/s6/** rwkix,
  /run/service/ rwk,
  /run/service/** rwkix,
  /run/s6-rc*/ rwk,
  /run/s6-rc*/** rwkix,
  /run/s6-linux-init-container-results/ rwk,
  /run/s6-linux-init-container-results/** rwkix,
  /run/uncaught-logs/ rwk,
  /run/uncaught-logs/** rwkix,
  /usr/bin/install ix,
  /bin/chown ix,
  /bin/chmod ix,
  /usr/bin/python3 ix,
  /usr/bin/python3.12 ix,
  /sbin/su-exec ix,
  /usr/bin/lp ix,
  /usr/bin/lpstat ix,
  /usr/bin/cancel ix,
  /usr/sbin/cupsd ix,
  /init-green.sh rix,
  /run.sh rix,
  /opt/green/** r,
  /etc/cups/** r,
  /etc/cups/ssl/site.crt rw,
  /etc/hosts rw,
  /etc/ssl/certs/** r,
  /config/private-ca.crt r,
  /data/** rwk,
  /run/cups/** rwk,
  /tmp/green-spool/** rwk,
  /var/spool/cups/** rwk,
  /var/log/cups/** rwk,
  /proc/** r,
  /sys/devices/system/cpu/** r,
  deny /home/** rwklx,
  deny /tmp/** x,
  deny /usr/sbin/lpadmin x,
}
