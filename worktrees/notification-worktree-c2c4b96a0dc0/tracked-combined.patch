diff --git a/modules/oom_sakkie/manager_case_sources.py b/modules/oom_sakkie/manager_case_sources.py
index 4371430d..c05549e8 100644
--- a/modules/oom_sakkie/manager_case_sources.py
+++ b/modules/oom_sakkie/manager_case_sources.py
@@ -94,18 +94,21 @@ def _rootline(now):
                     where event_source='oom_sakkie_rootline_reassessment'
                       and review_json->'rootline_reassessment'->>'delivery_state'='delivered'
                       and review_json->'rootline_reassessment'->>'operating_date'=%s
-                      and review_json->'rootline_reassessment'->>'material_digest'=%s
-                      and review_json->'rootline_reassessment'->>'result_id'=%s
-                      and review_json->'rootline_reassessment'->>'evidence_generation'=%s
                       and review_json->'rootline_reassessment'->>'owner_user_id'=%s
                       and review_json->'rootline_reassessment'->>'chat_id'=%s
                       and coalesce(review_json->'rootline_reassessment'->>'provider_message_id','')<>''
+                      and ((%s<>'' and review_json->'rootline_reassessment'->>'owner_plan_fingerprint'=%s)
+                           or (review_json->'rootline_reassessment'->>'material_digest'=%s
+                               and review_json->'rootline_reassessment'->>'result_id'=%s
+                               and review_json->'rootline_reassessment'->>'evidence_generation'=%s))
                     order by created_at desc,review_event_id desc limit 1""",
-                    (local_date, str(observation.get("material_digest") or ""),
+                    (local_date, str(observation.get("owner_user_id") or ""),
+                     str(observation.get("chat_id") or ""),
+                     str(observation.get("owner_plan_fingerprint") or ""),
+                     str(observation.get("owner_plan_fingerprint") or ""),
+                     str(observation.get("material_digest") or ""),
                      str(observation.get("result_id") or ""),
-                     str(observation.get("evidence_generation") or ""),
-                     str(observation.get("owner_user_id") or ""),
-                     str(observation.get("chat_id") or "")))
+                     str(observation.get("evidence_generation") or "")))
                 delivered_row = cur.fetchone()
     retry_at = now + timedelta(minutes=5)
     retry_text = retry_at.astimezone(ZoneInfo("Africa/Johannesburg")).strftime("%Y-%m-%d %H:%M SAST")
@@ -127,11 +130,10 @@ def _rootline(now):
     missing = ["provider_confirmed_family_delivery_bound_to_current_plan"]
     missing.extend(f"current_plan_{key}_identity" for key, value in identities.items() if not value)
     return [_candidate("rootline:current-plan", "ROOTLINE", "urgent",
-        [f"event:{event_id}", f"operating_date:{local_date}",
+        [f"operating_date:{local_date}",
          f"material:{identities['material'] or 'missing'}",
          f"result:{identities['result'] or 'missing'}",
-         f"generation:{identities['generation'] or 'missing'}",
-         f"observed:{observed.isoformat()}"], missing,
+         f"generation:{identities['generation'] or 'missing'}"], missing,
         "ROOTLINE current-plan delivery exception: provider-confirmed family delivery is missing for the exact current-date material, result and generation.",
         ("Automatic acquisition owner: the existing Oom Sakkie ROOTLINE delivery lifecycle must "
          f"obtain and persist exact Telegram provider confirmation; retry at {retry_text}. "
diff --git a/modules/oom_sakkie/rootline_reassessment_lifecycle.py b/modules/oom_sakkie/rootline_reassessment_lifecycle.py
index fa0b0633..7a4f9326 100644
--- a/modules/oom_sakkie/rootline_reassessment_lifecycle.py
+++ b/modules/oom_sakkie/rootline_reassessment_lifecycle.py
@@ -37,17 +37,18 @@ def reassess_rootline(*, owner_user_id: str, chat_id: str, trigger: str,
         f"{owner_user_id}|{chat_id}|{material}".encode()
     ).hexdigest()
     legacy_identity = "OOM-ROOTLINE-REASSESS-" + legacy_binding[:24].upper()
-    observation = _typed_observation(current, owner_user_id, chat_id, material)
-    observed = state_store("record_observation", observation["identity"], observation)
-    if not isinstance(observed, Mapping) or observed.get("success") is not True:
-        return _contained("rootline_reassessment_observation_unproven")
-    delivered = state_store("load_delivered", f"{owner_user_id}|{chat_id}", None) or {}
-    current_identity = state_store("load_identity", identity, None) or {}
     current_answer = compose_daily_rootline_plan(current, language=language)
     current_owner_plan_reassessment = _owner_plan_reassessment(
         current.get("next_reassessment"))
     current_owner_plan_fingerprint = _owner_plan_fingerprint(
         current_answer, current_owner_plan_reassessment)
+    observation = _typed_observation(current, owner_user_id, chat_id, material,
+        owner_plan_fingerprint=current_owner_plan_fingerprint)
+    observed = state_store("record_observation", observation["identity"], observation)
+    if not isinstance(observed, Mapping) or observed.get("success") is not True:
+        return _contained("rootline_reassessment_observation_unproven")
+    delivered = state_store("load_delivered", f"{owner_user_id}|{chat_id}", None) or {}
+    current_identity = state_store("load_identity", identity, None) or {}
     # A fresher generation remains durable observation evidence, but is not
     # owner-notification material by itself. Daily and change rails share the
     # date + material identity and stay silent when the supported action did
@@ -187,7 +188,8 @@ def _typed_zone_projection(result):
     return sorted(rows, key=lambda row: row["zone_id"])
 
 
-def _typed_observation(current, owner_user_id, chat_id, material):
+def _typed_observation(current, owner_user_id, chat_id, material, *,
+                       owner_plan_fingerprint=""):
     operating_date = str(current.get("operating_date") or "")
     evidence_generation = str(current.get("generation") or current.get("evidence_cutoff") or "")
     evidence_cutoff = str(current.get("evidence_cutoff") or "")
@@ -204,7 +206,10 @@ def _typed_observation(current, owner_user_id, chat_id, material):
         "evidence_generation": evidence_generation,
         "evidence_cutoff": evidence_cutoff,
         "next_reassessment_at": _declared_next_due(current),
-        "zones": _typed_zone_projection(current), "delivery_state": "observation_only"}
+        "zones": _typed_zone_projection(current),
+        "owner_plan_fingerprint_version": OWNER_PLAN_FINGERPRINT_VERSION,
+        "owner_plan_fingerprint": str(owner_plan_fingerprint or ""),
+        "delivery_state": "observation_only"}
 
 
 def _contained(status):
