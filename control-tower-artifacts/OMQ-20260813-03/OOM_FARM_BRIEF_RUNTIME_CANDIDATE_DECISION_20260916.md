# Oom Sakkie actionable brief and irrigation correction decision

**Local candidate:** `86bcee2709374353b7daf8d68bda839bac69337c`
**Tree:** `8582052f2cab5bf1a7abc4d26c27ebb085c6e23b`
**Parent and current deployed web revision:** `3d321e4932cf205d03e759cc71a26edd411bc9d2`
**Status:** QUALIFIED LOCALLY; UNPUBLISHED; EXACT RUNTIME-CANDIDATE APPROVAL REQUIRED.

This is a bounded runtime correction for existing mission `OMQ-20260813-03`.
It is not a fixture-only successor and does not inherit the fixture exception used
for PR #1336. No candidate branch, admission, pull request, merge or deployment
has been published for this commit.

## Concrete change

The candidate changes eight application files and ten tests. It does not change
a migration, workflow, selector, permission, billing setting, provider binding or
n8n workflow.

The morning manager now:

- produces one distinct sow item for Mysikind and one for Mona;
- removes internal wording such as `canonical litter` and `status reconciliation`;
- asks one bound question for the highest-value missing farm fact;
- keeps the exact answered task retired while leaving a different legitimate
  question available;
- says explicitly that the brief did not record or complete a farm operation;
- creates weighing work only from an explicit
  `individual_weighing_due_now` lifecycle signal;
- preserves historical mortality evidence but presents only a current
  seven-day event or an exact open individual lifecycle;
- reads the real manager case/event rail and distinguishes absent, queued,
  running, failed, delivered, checked-with-no-new-work and deadline-deferred
  worker outcomes;
- excludes scheduler timestamps from the material digest so polling alone does
  not replace or redeliver a card.

ROOTLINE now keeps routine reassessments silent unless a genuine owner question
or failed lifecycle requires attention. Verified controller boundaries use one
concise message each:

`🟢 C Camp irrigation ON — controller verified at 00:35:16 SAST.`

`⚪ C Camp irrigation OFF — controller verified at 02:04:24 SAST.`

The morning manager may include an overnight line such as
`Overnight irrigation: C Camp 🟢 ON 00:35; ⚪ OFF 02:04`. It does not calculate
watering duration or volume from those boundaries.

No n8n business logic is added. The backend remains the business owner, the
existing interim transport owner remains unchanged, and all physical-control
holds remain in force.

## Reconciled production evidence

The 2026-09-16 backend snapshot contains 74 currently eligible, tagged pigs and
zero weights in the observation window. It contains zero exact
`individual_weighing_due_now` signals. Historical membership cannot be rebuilt
from that current snapshot, so the 74 pigs are not a weighing worklist and do not
all need weighing.

The current mortality read contains 42 attributable historical candidate events:
14 stillbirth, 26 individual death and 2 crushed. There were zero candidate
events in the last seven days, five in the last 30 days and 40 in the last 90
days. Those records remain preserved. A current event or an exact open
individual lifecycle is the missing evidence needed to show mortality as present
farm work; an unchanged archive total is not a morning priority.

For C Camp execution
`ROOTLINE-EXECUTION-0C182359F30AFE1ABE0D2F6F`, the authoritative controller
readings are ON at 00:35:16 SAST and OFF at 02:04:24 SAST. The stored 59.9833
minutes is a planned/native segment budget. Its stop deadline was 01:35 SAST,
while OFF was observed at 02:04:24. The evidence does not establish when water
flow began or ended, so it supports neither an actual watering duration nor
delivered volume.

The nine relevant overnight/morning deliveries comprise eight ROOTLINE provider
messages and Charl's morning brief. Six ROOTLINE deliveries were routine
reassessments that alternated forecast/live rain and dry evidence; the direct
verified ON/OFF rail already existed. The separate recipient projection is
recorded but does not establish Anton's acceptance.

## Qualification and retained failure

The combined modified non-PostgreSQL selection passed 179 tests and eight
subtests. The broader hosted-equivalent morning selection passed 206 tests. The
real manager case/event PostgreSQL integration selection passed its added test
with 57 deselected.

The first fresh disposable-PostgreSQL herd attempt bootstrapped all 85 migrations
and completed 271 tests plus all 27 subtests. One assertion failed because it
expected the entire question queue to be empty after an answer, even though a
different legitimate litter question remained. The test was corrected to require
only the exact answered task to retire.

A second independently bootstrapped disposable PostgreSQL database then passed
all **272 herd tests and 27 subtests**, with zero skips and one warning, in
317.77 seconds. The retained log hashes are:

- first attempt:
  `FB0D410DD1F86723AE466F50D94F925075BC0A1DDC84C03C368611EAC30C27A0`;
- passing rerun:
  `471435406B4077CB68C9D9F8150344C9CD819634FF200A2EE5983F13C05B9D02`.

Python compilation and `git diff --check` passed.

## Additional independent shutdown-visibility gate

C Camp's planned stop deadline was 01:35 SAST and controller OFF was observed at
02:04:24. That discrepancy remains unresolved. Silencing routine reassessment
noise must not silence a missed shutdown deadline or a shutdown that has not
been controller-verified.

The retained 272-test/27-subtest qualification does not contain a named
regression that proves both of those exception states remain owner-visible.
Before this exact candidate can receive a runtime release decision, an
independent reviewer must verify the implemented classification and exercise
both paths. If the current code does not meet the condition, any repair requires
an exact successor, refreshed evidence and its own applicable runtime approval.
Notification wording alone cannot classify the C Camp timing discrepancy as
resolved.

## Required release controls

Exact approval of this commit and tree is required before publication because it
changes runtime behavior. After approval, continue the existing mission and sole
release coordinator through refreshed admission, every required hosted candidate
check, all 272 herd tests and 27 subtests, every later selector and browser job,
serialized merge and every exact-merge check. Preserve the failed local attempt
and all existing gates.

Promotion remains web-only and must use the next genuinely eligible fresh-date
window under the existing start, cancellation, identity and delivery-history
controls. The current deployed revision
`3d321e4932cf205d03e759cc71a26edd411bc9d2` is the scoped rollback target for
this candidate and must be reverified before promotion. Do not deploy or change
any other service, transport, cron, workflow, migration or provider.

Migration, weaning, treatment, native voice, permission, physical-operation,
ROOTLINE and CHARLIE holds remain unchanged. Live Linda conversation, Anton
acceptance, confirmation, cancellation and a genuinely needed permitted farm
update remain separate business acceptance work.
