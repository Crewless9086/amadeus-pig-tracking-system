# Green 0.3.1 technical read-only zero-job preflight decision

Status: `ACTION REQUIRED NOW — ONE PROTECTED READ-ONLY AUTHORITY DECISION`.

Owner outcome achieved: `NONE`. Usable now: `NO`.

```json
{"contract_version":"core_mission_outcome_handover_v1","handover_id":"GREEN-031-TECHNICAL-ZERO-JOB-DECISION-20260822","mission_id":"DMQ-20260816-01","reporting_actor_type":"control_tower","terminal_disposition":"protected_read_only_decision_packet_ready","requested_lifecycle":"PROTECTED_BOUNDARY","technical_milestones":["merged","deployed","health_passed","preflight_packet_ready"],"applicability":{"operational_evidence":{"state":"not_applicable","reason_code":"DECISION_PACKET_ONLY","reason":"No technical/device access occurred","authority":"DMQ-20260816-01","audit_ref":"b11f5d4d"}},"evidence":{},"next_safe_stage":"execute once only after exact authority","hold":{"type":"PROTECTED_BOUNDARY","owner":"Charl","reason":"Read-only access to private Home Assistant/network/printer metadata","wake_condition":"Exact approval text below","automatic_continuation_trigger":"Control Tower dispatches bounded technical maintainer window and validates redacted receipt"}}
```

## Bound current truth

- Authoritative/live web revision: `b11f5d4dd2775d5e298e09d9a23fa433cc05ed21`.
- Valid immutable Green 0.3.1 digest:
  `sha256:b660fffbc7985f7b5d8f2550f2dbbf779966e5167d51e063051fb1890a10bdd5`.
- Already supplied owner facts—do not repeat: 0.3.1 installed; Stopped; Start on
  boot, Watchdog and Auto update OFF; never auto-started; aarch64.
- Supervisor-resolved digest/containment, private canonical route/TLS, printer IPPS
  and protected registry identities remain Unknown.
- Invalid 0.3.0 remains quarantined: never install, use, attest further,
  overwrite, delete or reuse.

## Largest safe single approval

One bounded technical maintainer window may perform read-only metadata and
handshake operations across the existing private Home Assistant, canonical HTTPS
and printer IPPS surfaces. It may collect no secret/private value into chat,
source, screenshots or ordinary logs and must make zero configuration, queue,
job, document, database or hardware changes.

This is one approval because every operation has the same reversible effect class:
read-only technical evidence with no application start and no business/provider
submission. Any write-capable or ambiguous tool is excluded.

## Exact operator and tools

Operator: the authorised technical maintainer using the existing authenticated
local Home Assistant administrator session and a governed maintenance workstation
on the intended private LAN. Charl does not repeat observations, enter commands,
or relay private values.

### 1. Supervisor image and containment

Use only the authenticated Supervisor **GET** operation
`/addons/amadeus_green_print_bridge/info` through approved local admin tooling.
The official endpoint reports app state/version, architecture, boot, watchdog,
auto-update, protection/AppArmor and privilege/network metadata. Record a redacted
field/result table and the raw-response SHA-256 in the secure inventory; do not
retain or display `options` because it may contain secrets.

Required proof: Stopped; 0.3.1; aarch64; boot manual/OFF; watchdog/auto-update OFF;
protected/AppArmor enabled; no privileged access, published ports, host network or
unexpected mappings; resolved image digest equals the valid digest above if the
governed interface exposes it.

If the digest remains `NOT EXPOSED`, record Unknown and stop that sub-step. Do not
install/start Terminal/SSH, use Docker socket/host shell, expose tokens or broaden
Supervisor privileges to recover it. Such escalation requires a new exact decision.

### 2. Private canonical DNS, route and TLS

On the governed workstation, enter the canonical hostname and pinned private IP
interactively from the secure inventory so they do not enter shell history. Run:

- private DNS resolution and compare its private answer set to the approved pinned
  endpoint;
- a TCP/TLS handshake to port 443 with SNI set to the canonical hostname;
- certificate-only inspection for subject, SAN, issuer, SHA-256 fingerprint and
  expiry;
- one unauthenticated read-only `GET /health` (or the already documented public
  health path) using verified TLS and explicit hostname-to-private-IP resolution.

No bearer credential, application endpoint, POST/PUT/PATCH/DELETE, database query
or fallback address is permitted. Required results: private route only; exact
hostname SAN; approved issuer/CA; non-expired certificate; healthy read-only status;
no public fallback. This workstation proof does not prove the stopped app's future
container route; that remains a zero-job commissioning acceptance row.

### 3. Printer IPPS TLS and capability handshake

Enter the reserved printer private IP and candidate IPPS URI interactively from the
secure inventory. First perform TLS certificate inspection on private port 631 with
IP identity verification and the proposed private CA. Then run exactly the standard
CUPS `get-printer-attributes.test` through `ipptool` against the candidate `ipps://`
URI, with verified TLS, bounded timeout and verbose output redirected directly into
the secure inventory.

Only IPP `Get-Printer-Attributes` is allowed. Prohibited test files/operations
include `create-job.test`, `print-job.test`, `Send-Document`, subscriptions,
Identify-Printer, cancellation and diagnostic/test pages.

Required evidence: responsive exact IPPS resource; HP OfficeJet Pro 8123/8120-series
identity reconciliation; IPP version/capabilities; certificate subject/SAN/issuer/
SHA-256 fingerprint/expiry; reserved private IP present in SAN; private CA verifies
the chain. Ordinary IPP success is not a substitute.

### 4. Secure inventory handling

Use the already approved secure Control Tower inventory only. If no approved secure
inventory is available, stop before collecting private facts. Store private IPs,
hostnames, serials, certificate metadata, raw responses and paths there; store no
credential, private key or CA private material in the evidence packet.

The returned Control Tower receipt contains only: evidence IDs; PASS/FAIL/Unknown;
observation times; tool versions; redacted endpoint classes; certificate/response
hashes; and exact blockers. Never include private IPs, tokens, full certificates,
serials, option values or command transcripts in chat/source/CI.

## Stop and rollback

Stop immediately on any unexpected app start/state change, automatic-control drift,
image mismatch, privilege/port/mapping deviation, secret exposure, non-private DNS
answer, TLS/SAN/CA/expiry mismatch, IPPS ambiguity, write-like IPP operation,
unexpected queue/job or printer activity.

Because no writes are authorized, normal rollback is termination plus evidence
containment. If any accidental state change occurs: stop the app; keep boot/watchdog/
auto-update OFF; do not repair configuration ad hoc; do not cancel an Unknown job;
revoke only a credential proven exposed; preserve content-free evidence; report one
exact blocker. Never delete `/data`, overwrite/delete registry artifacts, change
printer firmware, weaken TLS or switch to ordinary/public IPP.

## Exact approval text

> I approve one bounded Green 0.3.1 technical read-only zero-job preflight under
> `DMQ-20260816-01`. The authorised technical maintainer may use the existing local
> authenticated Home Assistant admin session and governed private-LAN workstation
> to read Supervisor app metadata, validate private canonical DNS/route/TLS with no
> credential or write, and perform certificate inspection plus exactly one IPPS
> `Get-Printer-Attributes` capability request. Keep Green Stopped with Start on boot,
> Watchdog and Auto update OFF. Store private values only in the approved secure
> Control Tower inventory and return redacted evidence IDs/hashes. Do not start or
> configure the app, expose tokens/private values, create identities/credentials/
> CA/queue/job, submit a document, print, migrate or write production. Stop on any
> mismatch, ambiguity or unexpected effect.

## Completion and continuation

Owner action required now: approve or reject the exact text above. No fact or
observation is requested from Charl.

After approval, Control Tower dispatches one bounded technical operator and validates
the redacted receipt. A PASS permits preparation—not execution—of the separate
zero-job commissioning decision. Any Unknown/FAIL remains one technical blocker.

Target owner outcome: a genuine natural request produces exactly one correct
physical weekly weighing sheet with canonical/CUPS/physical agreement, cleanup,
automatic follow-up and a later terminal-independent cycle.

Owner outcome achieved: `NONE`. Usable now: `NO`.

## Control Tower check receipt

```text
Governance: PASS — merged packet b11f5d4d and current doctrine
Feedback freshness: current — release/health/CI and known owner facts
Terminal truth: decision preparation released
Runtime truth: Green dormant/authority-disabled; no worker/cycle
Mission: DMQ-20260816-01 PROTECTED_BOUNDARY — physical page remains
Owner workload delta: unchanged; no repeat observations
Release lane: free; technical evidence lane awaits one exact approval
Collision: clear — one Green lineage, no register/device write
Owner repetition: prohibited; fact request NONE
Register: serialized addendum available to designated writer
Dispatch: bounded technical read-only zero-job preflight after approval only
```
